# MODEL_CARD · KKBox 訂閱流失預測

**這張卡回答的是「這個模型不該被拿來做什麼」。**

`GET /model` 回傳的 `artifact.json` 已經是機器可讀的 provenance（哪一份模型、哪些
特徵、什麼假設、哪一版程式）。這份文件不重複那些欄位，它寫的是 JSON 承載不了的
東西：限制、盲區、以及每一個限制底下的實測數字。

> ⚠️ **每一個數字都標了它是哪個模型、在哪個 cohort 上量的。** 本專案有**三份**
> artifact（見 §1），它們的 cohort 成員、特徵集與提前天數都不同，**分數不可互相比
> 大小**（[SPEC §7.15](SPEC.md) / [§7.19](SPEC.md)）。沒有特別標的數字一律是上線那一份
> （`catboost_lead7d`）。

---

## 1. 模型基本資料

| 項目 | 值 |
|---|---|
| 用途 | 預測訂閱用戶在**到期後 30 天內不會續訂**的機率，並據此決定挽回優惠投放名單 |
| 上線的 artifact | **`catboost_lead7d`** —— `cutoff = 到期日 − 7 天`（`cutoff_definition: expire_date_minus_7d`） |
| 演算法 | CatBoost（ordered target statistics 處理高基數類別），停在第 1,963 輪 |
| 特徵 | 61 欄（交易史 23 + 收聽行為聚合 38），4 欄類別特徵 |
| 訓練 cohort | `feb_t7` 991,774 人，流失率 6.3566% |
| 評估 cohort | `mar_t7` 969,386 人，流失率 8.8947%（**時間外**，全程不參與訓練） |
| 主指標 | Mar log loss **0.17921**（常數基準 0.30494，改善 41.2%） |
| 機率校準 | **未校準**，見 §5 |
| 服務 | `POST /predict`（FastAPI），機率與 Top-3 中文原因碼即時計算 |
| 監控 | `make drift` → `reports/drift.json`，**有已知盲區**，見 §9.5 |
| 授權 | 程式碼 MIT；資料 © KKBOX Group，依 WSDM Cup 2018 競賽規則使用，不隨 repo 散布 |

**本專案有三份 artifact，而它們是三個模型**（cohort 成員、特徵集、提前天數都不同，
**分數不可互相比大小**）：

| artifact | cutoff 規則 | 特徵 | 分數 | 用途 |
|---|---|---|---|---|
| `catboost_lead0d` | 到期日 | 61 | 0.15367（`mar`） | 離線基準，**不可上線** |
| **`catboost_lead7d`** | 到期日 − 7 天 | 61 | **0.17921**（`mar_t7`） | **上線的那一份**（服務預設） |
| `catboost_fixed` | 上個月最後一天（提前 1~31 天） | 62 | 0.17342（`mar_fixed`） | Kaggle 管線（§9.8） |

`lead0d` 在到期日當天評分，而挽回優惠那時已經來不及寄出（[SPEC §4.3](SPEC.md)），
所以它的分數只是離線參考。每一份 artifact 的 `deployable` 旗標與每一筆 `/predict`
回應都會講出這件事。

`catboost_fixed` 多一個特徵 `days_to_expire`：在固定評分日的設計下「還有幾天到期」
是變動且**在評分時點看得到**的量；提前固定天數的設計裡它是常數，加了只是多一欄零
gain（[SPEC §7.19](SPEC.md)）。

---

## 2. 預期用途

**主要用途**：對「即將到期的訂閱用戶」排序，產生挽回優惠的投放名單。名單的定義是
`p_churn > p*`，而 `p* = C_offer / (r_save × LTV_saved)` —— 一個由業務假設決定的
門檻，不是「前 5%」這種比例（[SPEC §7.14](SPEC.md)）。

**使用者**：營運／CRM 團隊（拿名單與原因碼）、資料團隊（維護與重訓）。

**每一筆預測附帶的東西**（`/predict` 回應）：

- 機率、`above_threshold`（該不該投放）、`expected_net`（這一位的期望淨收益）
- 最多 3 句中文原因碼，每句帶語意分組、SHAP 貢獻（**單位是 log-odds，不是機率**）
  與**量測時點**
- 被呈現門檻壓下的候選（帶原因，供稽核）
- `cutoff_definition` / `deployable` / `calibrated` —— 這個機率是誰算的

---

## 3. ⛔ 不可用於

**一、不可當成「投放優惠能不能改變他的行為」。**

模型預測的是**會不會流失**，不是**投放的因果效應**。後者需要 uplift modeling 與
A/B 實驗，而本資料集沒有實驗組／對照組結構，無法回答。

實務後果很具體：名單上機率最高的人可能是**已經決定要走、優惠也留不住**的人（他
最後一筆交易就是取消），而真正該投放的是「會被優惠改變決定」的人。這兩群不是同
一群，本專案量不出後者。`r_save = 0.15` 是一個**假設**，不是估計值（§6）。

**二、不可用它決定優惠的金額大小、價格、或任何差別待遇。**

模型吃了 `gender_code` / `bd_clean` / `city`（來自 `members_v3` 快照），而本專案
**沒有做群組公平性評估**（§10）。在那之前，這個模型只能用來決定「誰先收到挽回
優惠」這種**只增不減**的干預。

**三、不可把它的分數與 Kaggle 排行榜比大小。**

官方私榜（🥇 0.07974 / 第 20 名 0.10834）算在 **Apr cohort** 上，本專案的分數算在
**Mar cohort** 上 —— 不同的資料集，直接比較沒有意義（[SPEC §3.3](SPEC.md)）。唯一
可比的數字是 Apr cohort 的 late submission，**公榜分數待回填**（§9.8）。

**四、不可拿來對「現在」評分，除非另接一份線上交易表。**

服務的 `msno` 介面查的是**固定 cutoff 的歷史 cohort 快照**（回應的 `feature_source`
會講明那個 cutoff）。真正的部署要以「現在」為時點重算 as-of 特徵，那需要一份線上的
transactions 表，本專案沒有。完整 payload 介面不受此限 —— 但呼叫端要自己保證那些
欄位是 as-of 正確的。

**五、不可外推到其他產業或其他年份。**

資料是 2015–2017 年的 KKBOX（音樂串流）歷史快照。訂閱制的流失結構高度依賴付款
方式與自動續訂的普及率 —— 實測 `last_is_auto_renew` 佔上線模型 20.5% 的 gain。
換平台、換國家、換年份都要重新驗證。

---

## 4. 訓練資料與時間結構

| 項目 | 值 |
|---|---|
| 來源 | Kaggle `kkbox-churn-prediction-challenge`（WSDM Cup 2018） |
| 規模 | 交易 22,978,755 列、收聽日誌 410,502,905 列、會員 6,769,473 列（解壓後約 34 GB） |
| 標籤 | 官方定義：**到期後 30 天內沒有新的有效訂閱交易** |
| 切分 | 時間外：訓練 2017-02 到期的 cohort → 驗證 2017-03 到期的 cohort |
| 評分時點 | 每位用戶的 `cutoff = 該用戶的到期日 − 7 天`（逐人計算，不是全體同一天） |

**as-of 截斷是這個專案的核心防線**：所有進入特徵的 `transaction_date` 與
`user_logs.date` 必須 `<= cutoff`，由 `src/data/cohort.py` 與 `src/features/logs.py`
的守門函式在**每次執行**時驗證（不是只在測試裡）。八條紅線的清單與狀態見
[SPEC §5](SPEC.md)；**M6 補上最後一條（紅線 4）之後八條全部實作**，見 §9.8。

**訓練用的標籤只有兩個月**。這限制了很多事：沒有第三個月可以當真正的測試集、
沒有辦法量「跨季的季節性」、也沒有辦法用「最近一個完整月份」校準（§5）。

---

## 5. 機率的可信度：**未校準，而且系統性低估**

`calibrated: false`。**這不是漏做，是量完之後的決定**（[SPEC §7.10](SPEC.md)）：
isotonic 校準器 fit 在有標籤的月份上，看不到下一個月的基準率漂移，實測套上去之後
log loss、Brier、ECE **每一項都變差**。

上線模型在 `mar_t7` 的實測偏差：

| 群 | 人數 | 平均預測 | 實際流失率 | 相對偏差 |
|---|---|---|---|---|
| 全體 | 969,386 | 6.33% | 8.89% | **−28.8%** |
| 重複用戶 | 881,235 | 4.18% | 5.85% | −28.6% |
| 新進用戶 | 88,151 | 27.88% | 39.34% | −29.1% |
| **投放名單**（`p > p*`） | 37,174 | 77.00% | 83.28% | **−7.5%** |

**兩件事必須一起讀**：

1. **成因是 cohort 之間的基準率漂移**（6.36% → 8.89%，相對 +39.9%），不是模型
   壞了。三個群的相對偏差幾乎相同（−28.6% ~ −29.1%），那正是基準率平移的形狀。
2. ⚠️ **全體的 −28.8% 不可以往下套到名單上。** 名單那一段只低估 7.5%，而期望淨
   收益公式裡 `C_offer` 是固定成本、不隨機率縮放 —— 所以「機率低估 28.8%」**不
   等於**「淨收益低估 28.8%」。要用在金額上就必須量那一段自己的偏差
   （[SPEC §7.13](SPEC.md) 修正過這個錯）。

**能修掉它的條件本專案沒有**：需要一塊與部署期同分布的標籤資料，而 Apr cohort 的
標籤是 Kaggle 測試集，本地取不到。可行的方向是 M6 建議的「以最近一個完整 cohort
校準、預測次月」，本專案無法驗證。

---

## 6. 決策門檻：三個假設的地位完全不同

    p* = C_offer / (r_save × LTV_saved) = 150 / (0.15 × 2067.6) = 0.4836

| 參數 | 值 | 地位 |
|---|---|---|
| `LTV_saved` | 2,067.6 元 | **由資料推導** —— 月費 131.4 元（cohort 實測 `price_per_day` × 30.4）× 續訂月數 15.73（= 1 / 訓練 cohort 月流失率） |
| `C_offer` | 150 元 | **業務決策** —— 不是估計值，是「我們打算送什麼」（一個月免費） |
| `r_save` | 0.15 | **純假設** —— 本資料集沒有實驗組／對照組，無法估計（§3 第一條） |

在這組假設下，上線模型的名單是 **37,174 人（3.83%）**，命中率 83.28%、
lift 9.36。

**改業務假設會改變名單成員，但不會改變任何人的機率與原因碼**；改模型或特徵則三者
全變。這個區別寫在 `/predict` 的回應與名單 manifest 裡，否則下次名單變了，沒有人
分得出是假設動了還是模型動了。

⚠️ 曲線上的金額**全是模擬**。真實的只有 Mar 的流失標籤；`r_save` 與 LTV 都是假設，
所以圖 12 的兩條線叫「期望模擬淨收益」與「標籤結算模擬淨收益」—— 都不是賺到的錢。

---

## 7. 效能

**上線模型**（`catboost_lead7d`，評估於 `mar_t7`）：

| 分群 | 人數 | 佔比 | 實際流失率 | log loss |
|---|---|---|---|---|
| 全體 | 969,386 | 100% | 8.89% | **0.17921** |
| 重複用戶 | 881,235 | 90.9% | 5.85% | 0.14997 |
| 新進用戶 | 88,151 | 9.1% | 39.34% | **0.47155** |

常數基準（用訓練 cohort 的流失率對所有人預測）0.30494。

**與離線基準的對照**（同一批 969,386 人、同一組標籤 —— 唯一合法的比法）：

| | 共同子集 |
|---|---|
| 到期日評分（`lead0d`，不可上線） | 0.15173 |
| **提前 7 天評分（上線的那個）** | **0.17921** |
| | **退步 +18.11%** |

模型選擇的依據：三個套件在同一份特徵、同一個切分、同一塊 early stopping 驗證集上
比較，並用 8 個配對 seed 確認（CatBoost 對 LightGBM 平均勝 0.00292，95% CI
[0.00259, 0.00326]，8/8 全勝）—— 詳見 [SPEC §7.12](SPEC.md)。

---

## 8. 可解釋性

每一位用戶最多 3 句中文原因碼，來自 **CatBoost 原生 TreeSHAP**（精確演算法，不是
取樣近似）。做法與已知邊界：

- **先依語意分組相加再取前 3 組**，不是取前 3 個特徵 —— 61 欄裡有 4 個窗口 × 8 個
  收聽量，直接取特徵會拿到三句同義的話
- **加總恆等式 `sigmoid(base + Σshap) == predict()` 逐列驗證**，`/predict` 每一筆
  請求都驗。它抓的是「原因碼很通順但講錯人」這一整類不會報錯的錯
- ⚠️ **SHAP 的單位是 log-odds**。貢獻 +0.8 不等於「流失率多 80%」，而 log-odds 到
  機率的換算不是線性的 —— 所以原因碼只能排序，不可印「這一項讓他的流失率增加幾個
  百分點」
- **弱到不像理由的句子不呈現**（相對第 1 名 < 5%），但**留在稽核輸出裡**帶著原因。
  「這個人只有一個理由」與「他的第三個理由太弱所以沒印」是不同的事
- ⚠️ **原因碼解釋的是模型，不是用戶。** 它回答「模型為什麼把他排在這裡」，不是
  「他為什麼想走」。後者要問用戶

---

## 9. 已知限制

### 9.1 唯一修不掉的洩漏：`members_v3` 是一份 2017-11-13 的快照

它不是 as-of 各 cohort cutoff 的狀態。一位用戶若在 2017 年年中搬家或更新資料，
Feb cohort 的特徵會拿到 cutoff 之後才成立的值。

受影響欄位與 gain 佔比（**M2 模型 LightGBM 61 特徵量的**，跨套件的 gain 不可比，
只當量級參考）：

| 欄位 | gain 佔比 |
|---|---|
| `city` | 0.365% |
| `registered_via` | 0.255% |
| `bd`（`bd_clean` + `bd_valid`） | 0.253% |
| `in_members` | 0.028% |
| `gender` | 0.024% |
| **合計** | **0.93%** |

`registration_init_time` 不受影響（註冊日不會事後變動）。**為什麼修不掉**：資料集
沒有提供屬性的歷史版本，無從還原 cutoff 當下的值。0.93% 是上界且很可能高估甚多。

處理過的部分：註冊日晚於自己 cutoff 的用戶（Feb 6 人 / Mar 2 人），**整列** members
屬性退回缺失 —— 在評分時點，他們確實還不在 `members_v3` 裡。

### 9.2 上線模型結構性地看不到風險最高的那一小撮人

提前 7 天評分時，「到期前 7 天內才第一次交易」的人截斷之後一列都不剩，整個掉出
cohort。實測（Mar）：

| | 人數 | 佔比 | 流失率 |
|---|---|---|---|
| 掉出 cohort | **1,573** | 0.162% | **70.25%** |
| 全體 | 970,959 | 100% | 8.99% |

**流失率是全體的 7.8 倍**（訓練 cohort 同樣現象：1,138 人、36.82%）。

這**不是可以修的 bug**：評分時點那些人真的還沒有可用的歷史。它是一個部署約束，
需要**另一個機制**接住 —— 到期日當天補評一次，或用首購事件觸發。只報「掉了
0.16%」會給人完全相反的印象。

### 9.3 新進用戶的分數是全體的 2.6 倍差

新進用戶（不在上一期 cohort 裡）的 log loss 0.47155，全體 0.17921。他們只佔 9.1%
但流失率 39.34%。模型對這一群的預測明顯較差，**名單上的新客要另眼看待**。

（順帶：提前 7 天評分讓這一群反而**變準** −6.25%，方向與預先登記的假設一致，但
成因未查證，不可當結論引用 —— [SPEC §7.15](SPEC.md)。）

### 9.4 缺失是訊號，所以上游改變會改變預測

模型刻意把「查不到」當成一種值：

- **11.66%** 的 cohort 用戶不在 `members_v3` 裡，而那群人的流失率 5.02%，低於整體
  6.39% —— 「查不到」本身有訊號
- **約 18%** 的用戶在 90 天窗口內沒有任何收聽紀錄（上線 cohort 的收聽特徵覆蓋率
  81.58% / 82.04%）
- 同一天多筆交易而付款方式衝突時，`last_*` 欄位刻意給 null（「不知道」是唯一誠實的
  值）

**後果**：一次上游的 join 改動、某個欄位停止供應、或收聽日誌的延遲，都會改變預測
分布，而模型不會報錯。服務的 `/predict` 因此在 payload 省略 `logs` 時**回警告**
—— 那等於告訴模型「這個人 90 天沒聽歌」，是一個主張而不是中性預設。

### 9.5 ⚠️ 監控的盲區：PSI 回報「穩定」不等於模型還準

實測（`feb_t7` → `mar_t7`，[SPEC §7.17](SPEC.md)）：

| 監控 | 值 | 慣例判讀 |
|---|---|---|
| 特徵 PSI | 59/61 欄 < 0.10 | 穩定 |
| 分數 PSI | 0.0254 | 穩定 |
| 名單大小 | 3.89% → 3.83%（−1.3%） | 幾乎沒動 |
| **實際流失率** | 6.36% → 8.89%（**+39.9%**） | ← 部署時拿不到 |

**三個部署時拿得到的監控全部回報一切正常，而機率低估了將近三成。** PSI 比的是
特徵與分數的**分布**，而這裡發生的是「同一種人、行為變了」—— 定義上就看不到。

**所以**：基準率漂移必須靠**標籤到齊後的回測**（每月把 30 天前的名單結算一次），
或一個**外生指標**（例如當月整體續訂率、支付通道的成功率）來接。只看 PSI 的儀表板
會在最需要示警的時候是綠的。

另外兩個判讀規則：慣例門檻 0.10 / 0.25 **沒有樣本數修正**（同一份無漂移資料在
49 萬對 49 萬下的 PSI 是 0.000074，所以「幾倍雜訊地板」比「落在哪一帶」有意義）；
`epsilon_floored = true` 的欄位有單邊空箱，那時 PSI 的大小由 epsilon 決定，只能讀成
「出現了參考期沒有的東西」。

### 9.6 ⚠️ 用這個模型對「下個月要到期的人」評分時，cutoff 不得超出資料涵蓋範圍

這一條是做 Kaggle 管線時發現的（[SPEC §7.19](SPEC.md)）：交易與日誌都只到
**20170331**，而 Apr cohort 要預測 4 月到期的人。若沿用 `cutoff = 到期日 − 7 天`，
**77.64%** 的用戶（到期日在 4/8 之後）的 cutoff 會落在資料結束之後 —— 那時 as-of
截斷變成空操作，特徵改由**資料集的結尾**決定：

    days_since_last_tx    最多被放大 23 天（上線模型 13.6% 的 gain）
    近 7 天收聽窗口        整段不存在 → 模型讀成「這個人沒在聽歌」（假的主張）

**八條紅線一條都不會叫**（`last_tx <= cutoff` 必然成立）。第九條守門
`assert_data_covers_cutoffs()` 因此存在，而正解是換設計（固定評分日），不是放行。

部署時的對應意義：**評分批次的 cutoff 不能晚於你手上資料的最後一天**。上游延遲一天
就會讓一批人的特徵少一天，而那不會報錯。

### 9.7 標籤定義本身的邊界

`is_churn` 是「到期後 30 天內沒有新的有效訂閱交易」。所以：

- **第 31 天回來的人算流失**。這是官方定義，不是我們的選擇，但用它做業務決策時要
  知道「流失」在這裡不等於「永久離開」
- 標籤要**等 30 天**才知道，這是 §9.5 那個監控盲區的根本原因
- 官方標籤的移植邏輯（`WSDMChurnLabeller.scala`）本專案驗證未過，因此**一律使用
  官方提供的標籤檔**，不自行重算（[SPEC §7.7](SPEC.md)）

### 9.8 尚未完成的驗證

| 項目 | 狀態 | 影響 |
|---|---|---|
| Apr cohort 的 Kaggle late submission | **提交檔已產生，公榜分數待回填** | 907,471 列在 `reports/kaggle/`（`make kaggle`）。**與 0.10834 可比的唯一數字仍然缺席** —— 事前登記的預期是 **0.17342**（結構相同的本地對照 `feb_fixed → mar_fixed`）。分數回來要填進 README 與 SPEC §3.3，無論結果如何 |
| 紅線 4：合併 cohort 時的 `GroupKFold(groups=msno)` | **已實作**（`make final`，[SPEC §7.20](SPEC.md)） | 合併 `feb_fixed + mar_fixed` 重訓的 `catboost_fixed_full` 用四段群組切分產生。違規對照組實測**每折驗證集有 75.2% 的用戶模型已見過**，而兩臂 log loss 只差 0.008 倍折間標準差 —— **量不到，但不表示規定可以省**（理由見 §7.20 第二點）|
| `catboost_fixed_full` 沒有時間外分數 | **結構性缺席** | 兩個帶標籤的 cohort 都拿去訓練了，本地算不出新的時間外估計。該 artifact 帶 `eval_is_out_of_time: false` 與指回 `catboost_fixed` 的 `out_of_time_reference`。實測**同分布 CV 比時間外樂觀 13.0%**（0.15087 vs 0.17342）—— 引用分數時必須看這一欄 |
| 群組公平性評估 | 未做 | 見 §10 |
| Docker 部署與線上 Demo | **已交付** | 容器部署於 Hugging Face Spaces：視覺化首頁、`POST /predict`、Swagger 文件。使用平台中立的 `Dockerfile` 部署（見 `deploy/README.md`）|

---

## 10. 敏感屬性與公平性

**模型使用了這些個人屬性**：`gender_code`、`bd_clean` / `bd_valid`（年齡）、
`city`（居住城市）、`registered_via`（註冊管道）。資料集不含族裔、宗教、收入等
欄位。

**本專案沒有做群組公平性評估。** 這是一個**已知的空白**，不是「評估過沒問題」。
該做而未做的是：逐群的校準偏差與 log loss、名單命中率、以及「同樣風險的人是否
同樣容易進名單」。

三件已知的事實，供決定要不要在評估前就先移除這些欄位：

1. **它們的貢獻很小。** 上表（§9.1）合計 0.93% gain，其中 `gender` 只有 0.024%
   —— 移除性別幾乎是免費的。
2. **「有沒有填」比「填什麼」有訊號得多。** 實測性別缺失者流失率 4.86%，而男
   8.84% / 女 8.64% 兩者幾乎沒有差異。也就是說模型從 `gender` 學到的主要是
   「這個人有沒有填資料」，不是性別本身。
3. **原因碼實測不靠會員屬性。** T=0 名單（48,853 人）的 Top-1 原因分組是續訂設定
   46.6%、取消紀錄 46.0%、訂閱資歷 5.5%、方案與金額 1.9% —— **會員資料組一位都
   沒有**。

**建議**：在完成群組評估之前，(a) 只把模型用於「誰先收到挽回優惠」這種只增不減的
干預；(b) 若營運會直接讀到原因碼，把會員屬性類的句子關掉（實測沒有人的 Top-1 靠
它，代價接近 0）；(c) 若合規上對使用性別有疑慮，直接移除該欄重訓 —— 代價已量過，
是 0.024% 的 gain。

---

## 11. 監控與重訓

**每月要看的三件事**（`make drift` → `reports/drift.json`）：

1. 特徵 PSI 的**倍雜訊地板**（不是慣例帶），以及 `epsilon_floored` 有沒有新亮的欄位
   —— 後者通常代表出現了訓練時沒見過的類別，或某欄的缺失率變了
2. 分數 PSI 與**名單大小**的變化 —— 名單突然變大／變小是營運會先感覺到的事
3. **30 天前那批名單的結算**（PSI 看不到的那一半，§9.5）：實際流失率、名單命中率、
   以及平均預測 vs 實際

**重訓前必須確認的三件事**：

1. **紅線 4**：若要合併多個 cohort 重訓，切分必須 `GroupKFold(groups=msno)`。已由
   `scripts/final_model.py` 實作（四段切分全部綁 `msno`，守門每折都跑）—— 合併重訓
   走那一支，不要自己切（§9.8）。**合併之後就沒有時間外驗證集了**，該 artifact 的
   本地分數不可與單一 cohort 那幾份比大小
2. **`cutoff_definition`**：重訓 T−7 版本要用 `--lead-days 7`，`export_model.py` 會
   把它寫進 artifact，而服務會拒絕載入指紋不符的 artifact
3. **快取的邏輯指紋**：特徵程式改了就要重建快取。`artifact.json` 記了匯出當時的
   指紋，`load_artifact()` 在指紋不符時**拒絕啟動**（§7.11 的教訓：程式改好了、
   資料還是舊的）

**只改業務假設**（`C_offer` / `r_save`）時不必重訓：`export_model.py --reuse` 重算
`p*` 與分數再寫一次 metadata。**改假設不會改變任何人的機率與原因碼。**

---

## 12. 如何重現

```bash
make artifact-t7   # 匯出上線的 artifact（約 5 分鐘；匯出後自己載回來對答案）
make serve         # 起 /predict，文件在 http://127.0.0.1:8000/docs
make drift         # 漂移監控報告 + 圖 16（不重訓）
make final         # 合併 cohort 重訓最終模型 + 紅線 4 對照（約 55 分鐘）
make test          # 324 passed · 0 skipped（八條紅線到齊）
```

模型與 metadata 在 `<data_root>/artifacts/catboost_lead7d/`（**不進 git** —— 二進位
且每次重訓都變）。要帶到別的機器就整個目錄複製，並用環境變數 `MODEL_ARTIFACT`
指路。

完整的實驗紀錄、每一個決定的理由與被否決的方案在 [SPEC.md §7](SPEC.md)；結果與
發現的敘述在 [README.md](README.md)。

---

## 13. 版本

| 日期 | 內容 |
|---|---|
| 2026-08-10 | 首版。對應 artifact `catboost_lead7d`（`git 159625b` 之後匯出）、`reports/drift.json`、SPEC v0.11 |
| 2026-08-10 | 補 Kaggle 管線（§9.6 的資料涵蓋範圍限制、§1 的三份 artifact 對照、§9.8 的提交狀態）。三份 artifact 都在 SPEC v0.13 的程式上重新匯出過，`lead0d` / `lead7d` 的分數逐位重現（0.15367 / 0.17921）|

⚠️ **這張卡與 artifact 綁在一起。** 重新匯出模型之後，§1 / §5 / §6 / §7 的數字都要
重新量 —— 特別是 §5 的校準偏差與 §6 的名單大小。
