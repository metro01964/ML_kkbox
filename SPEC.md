# SPEC — KKBox 訂閱流失預測與挽回決策

> **模型程式碼在此文件定稿之後才開始寫。**
>
> 版本 v0.14 · 建立於 2026-08-06 · 最後更新 2026-08-12 · 狀態：**已定稿**

---

> ## 現行基準：2026-08-10 第三次重測後
>
> **正文中的 M1–M4 分數全部已更新為新基準。** 舊值不再出現在任何推理中，
> 集中列在 §7.11 的對照表，標記為「排序修正前，已作廢」。
>
> 三個位置刻意保留舊值，各自標了 ⛔：§7.5 與 §7.8 的修正效果對照表（它們
> 記錄的是**兩欄之差**，不是絕對值），以及 §7.4 第三點的 target encoding
> 表（`make encode` 尚未在新基準上重跑）。
>
> | 現行值 | 數字 |
> |---|---|
> | M1（23 特徵）· Mar | **0.15925** |
> | M2（61 特徵）· Mar | **0.15705**　5-fold **0.08344 ± 0.00028** |
> | **M3 CatBoost · Mar** | **0.15367**（−2.16% vs LightGBM，配對 8/8） |
> | M4 校準前 → 後 · Mar | **0.15645 → 0.17172**（不上線） |
> | 雜訊尺度（配對 σ） | **0.00044** |
>
> 基準經 `make verify-rebuild` 驗證**連續兩次強制重建逐位元相同**（cohort
> 內容、收聽特徵、特徵矩陣、三份設定檔的 train/es/sel 成員、M1/M2 內部切分
> 成員）。執行紀錄見 `reports/rebaseline/`、`reports/multi_seed.json`。
>
> 結論的變動只有一處，而且方向與直覺相反：**LightGBM vs XGBoost 在單次量測
> 下分不出高下，但 8 個配對 seed 顯示 LightGBM 確實較優**（7/8 勝、95% CI
> 不含 0）。逐條對照見 §7.11 與 §7.12。

---

## 1. 問題定義

### 1.1 業務問題

KKBOX 是訂閱制音樂串流平台。多數方案為 30 天，用戶每月面臨一次「續訂或離開」的決策點。平台的營運問題不是「誰會流失」這個預測本身，而是：

> **在有限的挽回預算下，這個月該對哪一批即將到期的用戶投放挽回資源，才能讓期望淨收益最大？**

預測只是手段。交付物必須能回答「投放給誰、預期回收多少」，否則模型分數再高也沒有營運價值。

### 1.2 機器學習問題

| 項目 | 定義 |
|---|---|
| 任務類型 | 二元分類，輸出**校準過的機率** |
| 預測單位 | 一位用戶（`msno`）在某個到期月份的一筆紀錄 |
| 目標變數 | `is_churn` — 會員到期後 **30 天內沒有產生新的有效訂閱交易** 則為 1 |
| 預測時點 | 該用戶的 `membership_expire_date`（部署時實際會提前 7 天，見 §4.3） |
| 評估指標 | **Log Loss**（自然對數） |

**為什麼是機率而不是分類？** 因為下游決策需要期望值計算。「這個人會流失」無法算 ROI，「這個人流失機率 0.73」才能乘上挽回成本與客戶終身價值。這也是為什麼指標是 log loss 而不是 accuracy 或 AUC——log loss 同時懲罰排序錯誤**與**機率失準，過度自信的預測會被重罰。

### 1.3 為什麼選這個題目

- **三個門檻都踩到**：`user_logs` 超過 30 GB（pandas 會 OOM，必須用 Polars/DuckDB）、驗證切分極易洩漏（見 §5）、指標不是 accuracy。
- **硬體可行**：純表格資料，LightGBM/CatBoost 全程 CPU，不需要 GPU。
- **產業對位**：KKBOX 是台灣訂閱制平台。訂閱留存、續約預測、挽回投放，與中華電信（Hami Video / MOD / 資費續約）與電商會員續訂是同一套業務語言。

> ⚠️ **適用範圍**：KKBOX 是**音樂**串流。結論外推到影音串流或電信服務時需重新驗證。

---

## 2. 資料契約

**來源**：Kaggle 競賽 `kkbox-churn-prediction-challenge`（WSDM Cup 2018）
**授權**：Subject to Competition Rules — **資料絕對不進 Git**（競賽規則），僅提供 `scripts/download.py`

### 2.1 檔案清單與 schema

以下 schema 由官方 Data 頁面確認。**所有數字均為實機量測值**：主體於 2026-08-06 量測，`user_logs` 與本節末段的交易品質統計於 2026-08-08 補測（見附錄）。

資料位置由 `configs/paths.yaml` 決定，**不硬編碼在程式裡** —— 主要電腦為 `D:/ml_data/`，其他機器可不同。該檔不進 Git，範本見 `configs/paths.example.yaml`。

### 2.0 檔案總覽（實測）

| 檔案 | 壓縮 | 解壓後 | 筆數 | 狀態 |
|---|---|---|---|---|
| `train.csv` | 32 MB | 46.7 MB | **992,931** | ✅ 已下載 |
| `train_v2.csv` | 31 MB | 45.6 MB | **970,960** | ✅ 已下載 |
| `transactions.csv` | 675 MB | **1.73 GB** | **21,547,746** | ✅ 已下載 |
| `transactions_v2.csv` | 47 MB | 115 MB | **1,431,009** | ✅ 已下載 |
| `members_v3.csv` | 231 MB | **428 MB** | **6,769,473** | ✅ 已下載 |
| `user_logs.csv` | **6.6 GB** | **30.5 GB** | **392,106,543** | ✅ 已下載 |
| `user_logs_v2.csv` | 654 MB | **1.43 GB** | **18,396,362** | ✅ 已下載 |
| `sample_submission_v2.csv` | 29 MB | 42.7 MB | **907,471** | ✅ 已下載 |
| `sample_submission_zero.csv` | 31 MB | 45.6 MB | **970,960** | ✅ 已下載 |
| `WSDMChurnLabeller.scala` | 6.9 kB | — | — | ✅ 已下載（M3 選用）|

競賽全部檔案合計 **8,950,563,771 bytes**（壓縮，官方 API 實測），全部已下載，解壓後佔用約 **34.4 GB**。

⚠️ **本表的單位慣例**：「壓縮」欄沿用 Kaggle 與檔案總管的顯示值，是 **二進位單位（GiB = 2³⁰）**；「解壓後」欄是 **十進位單位（GB = 10⁹）**。兩者相差 7.4%，例如 `transactions.csv` 在檔案總管顯示 1.61 GB、本表寫 1.73 GB，**是同一個檔案**（1.61 × 1.0737 = 1.73），不是下載不完整。下載完整性由 `scripts/download.py` 內嵌的 byte 數契約逐檔驗證。

⚠️ **本表的「筆數」是原始 CSV 的列數，不是 cohort 人數。** 兩者在本文件中都會出現，差別是 §7.5 修正 cutoff 洩漏後被剔除的那幾個人：

| | 原始 CSV 列數（§2.0 · §2.1 · §2.3 用） | `build_cohort()` 後的 cohort（§4.4 起用） |
|---|---|---|
| Feb | **992,931** | 992,912（−19） |
| Mar | **970,960** | 970,959（−1） |
| 兩期 `msno` 交集 | **881,701** | 881,693（−8） |

被剔除的是「找不到任何當下可觀測交易、因此算不出 cutoff」的用戶（§7.5 第 1 項）。**資料契約斷言的一律是左欄**（原始檔的性質不該隨程式碼改變而變動），**評估報表用的一律是右欄**（模型實際看到的樣本）。`tests/test_data_contract.py` 斷言左欄，`src/evaluation/metrics.py` 的分群報表產出右欄。

⚠️ **`user_logs` 合計 4.1 億列，是交易資料（2,298 萬列）的 17.9 倍。** 這個量級決定了 M2 只能用 Polars lazy / DuckDB streaming，見 §2.2。

#### `train.csv` / `train_v2.csv` — 標籤

| 欄位 | 型別 | 說明 |
|---|---|---|
| `msno` | str | 用戶 ID |
| `is_churn` | int8 | 1 = 流失，0 = 續訂 |

| | `train.csv`（Feb cohort） | `train_v2.csv`（Mar cohort） |
|---|---|---|
| 到期月份 | 2017-02，觀察於 2017-03 | 2017-03，觀察於 2017-04 |
| 筆數（原始檔） | 992,931 | 970,960 |
| 流失 / 續訂 | 63,471 / 929,460 | 87,330 / 883,630 |
| **流失率** | **6.3923%** | **8.9942%** |
| `msno` 重複 | 0 | 0 |
| 常數基準 log loss | 0.23763 | 0.30240 |

⚠️ **兩期流失率相差 40%（6.39% → 8.99%）。** 這不是雜訊，是真實的概念漂移，直接影響驗證策略與門檻設定，見 §4.5。

#### `transactions.csv` / `transactions_v2.csv` — 訂閱交易

| 欄位 | 型別 | 說明 |
|---|---|---|
| `msno` | str | 用戶 ID |
| `payment_method_id` | int | 付款方式（高基數類別） |
| `payment_plan_days` | int | 方案天數 |
| `plan_list_price` | int | 方案定價（**新台幣 NTD**） |
| `actual_amount_paid` | int | 實付金額（NTD） |
| `is_auto_renew` | int8 | 是否自動續訂 |
| `transaction_date` | `%Y%m%d` | 交易日 |
| `membership_expire_date` | `%Y%m%d` | 會員到期日 |
| `is_cancel` | int8 | 本筆交易是否為主動取消 |

| | `transactions.csv` | `transactions_v2.csv` |
|---|---|---|
| 筆數 | **21,547,746** | **1,431,009** |
| 唯一 `msno` | 2,363,626 | 1,197,050 |
| `transaction_date` 範圍 | 2015-01-01 ~ **2017-02-28** | 2015-11 ~ **2017-03-31** |
| `membership_expire_date` 範圍 | 1970-01-01 ~ 2017-03-31 | 2016-04-19 ~ **2036-10-15** |
| `is_cancel = 1` | 856,851（3.98%） | 35,133（2.46%） |
| `payment_method_id` 基數 | 40 | 37 |
| 缺失值 | 無 | 無 |

⚠️ **`transactions_v2.csv` 是增量更新檔，不是完整歷史。** 實測其中 **74.76% 的交易發生在 2017-03**，其餘為長天期方案的零星補登。**M1 必須同時載入 `transactions.csv` + `transactions_v2.csv` 才有完整交易史**，只用 v2 等於沒有歷史特徵。這一點官方文件沒有明說，是實測發現的。

⚠️ **兩個資料品質哨兵值**，前處理必須處理：
- `transactions.csv` 的 `membership_expire_date` 最小值為 **19700101**（Unix epoch，等同 null）
- `transactions_v2.csv` 的 `membership_expire_date` 最大值為 **20361015**（2036 年，不合理的長天期）

⚠️ `is_cancel = 1` **不等於流失**。官方明示用戶可能因換方案而取消，實測全量僅 3.98% 的交易帶此旗標。把 `is_cancel` 當標籤是本題最常見的誤解。

📌 `payment_plan_days` 高度集中：30 天佔 85.1%（transactions_v2），其次為 410 / 195 / 180 / 90 天。`is_auto_renew = 1` 佔 78.5%。

##### 交易資料的兩項結構性現象（v0.3 新增）

以下兩項在全量 22,978,755 列上量測。**兩者都不是資料損壞**，但都會影響前處理與特徵定義，必須先分類清楚再決定怎麼處理。

**現象一：`transaction_date > membership_expire_date` —— 158,766 列（0.69%）**

| 統計 | 值 |
|---|---|
| 筆數 / 涉及用戶 | 158,766 / 138,503 |
| **其中 `is_cancel = 1`** | **95.93%** |
| 其中 `is_auto_renew = 1` | 98.96% |
| 其中實付 0 元 | 9.30% |

近 96% 是**取消紀錄**。合理解釋：用戶取消時，系統把 `membership_expire_date` 寫成生效的結束日（可能早於當下），而 `transaction_date` 記錄的是處理取消的日期。因此「交易日晚於到期日」對取消而言是正常的記錄慣例，不是錯誤。

**對本專案的意義**：由於 `cutoff = membership_expire_date`，這些列在 as-of 過濾（`transaction_date <= cutoff`）中會被**正確排除**。這是對的 —— 2 月 16 日到期的用戶，2 月 20 日才處理的取消，在評分時點尚未發生，不該成為特徵。相關的洩漏風險見 §5.1 新增條目。

**現象二：`actual_amount_paid = 0` —— 1,218,324 列（5.30%）**

這**不是單一現象，是兩件語意不同的事被同一個條件混在一起**：

| `plan_list_price` | `payment_plan_days` | 筆數 | 這是什麼 |
|---|---|---|---|
| **0** | 7 | 582,075 | 免費試用 |
| **149** | 30 | 536,038 | 付費方案但零收款 |
| 0 | 10 | 37,579 | 免費試用 |
| 149 | 31 | 18,235 | 付費方案但零收款 |
| 0 | **0** | 12,861 | ⚠️ 0 天方案，異常值 |

- 定價本來就是 0 的佔 **53.97%** —— 免費方案收 0 元完全合理，不需處理
- 其餘是**定價非 0 卻實付 0**，可能為促銷券、扣款失敗或退款。這一群的業務意義與免費試用完全不同

**因此：判斷「這筆交易是不是沒收到錢」必須同時看 `plan_list_price`，不能只用 `actual_amount_paid == 0`。**

**這不只是資料問題，更是強訊號。** Feb cohort 中最後一筆交易實付 0 元者：

| 最後一筆實付 | 人數 | 佔比 | 流失率 |
|---|---|---|---|
| > 0 | 987,334 | 99.44% | 6.13% |
| **= 0** | **5,597** | **0.56%** | **51.92%** |

**差 8.5 倍。** 但注意規模：cohort 內僅 0.56% 的人最後一筆是 0 元，全量的 122 萬列多數散落在較早的歷史中，不落在 cutoff 那一筆上。

#### `user_logs.csv` / `user_logs_v2.csv` — 每日收聽行為

| 欄位 | 型別 | 說明 |
|---|---|---|
| `msno` | str | 用戶 ID |
| `date` | `%Y%m%d` | 日期 |
| `num_25` | int | 播放未達 25% 的歌曲數 |
| `num_50` | int | 播放 25%–50% 的歌曲數 |
| `num_75` | int | 播放 50%–75% 的歌曲數 |
| `num_985` | int | 播放 75%–98.5% 的歌曲數 |
| `num_100` | int | 播放超過 98.5% 的歌曲數 |
| `num_unq` | int | 播放的不重複歌曲數 |
| `total_secs` | float | 總播放秒數 |

| | `user_logs.csv` | `user_logs_v2.csv` |
|---|---|---|
| 筆數 | **392,106,543** | **18,396,362** |
| `date` 範圍 | **2015-01-01 ~ 2017-02-28** | **2017-03-01 ~ 2017-03-31** |
| 解壓後大小 | **30.5 GB** | **1.43 GB** |

- 兩檔的日期範圍相接且不重疊，合併涵蓋 **2015-01-01 ~ 2017-03-31**，與官方說明一致
- ✅ 第三方回報的「解壓後超過 30 GB」**已驗證為真**（實測 30.5 GB）— 本專案的記憶體天花板就在這裡
- ⚠️ v0.2 估計 `user_logs_v2.csv` 解壓後約 3 GB，**實測 1.43 GB，高估一倍**
- 📌 `user_logs_v2` 每天約 **59.3 萬名用戶**有收聽紀錄（18,396,362 ÷ 31 天），可用來估算 M2 聚合後的資料規模

⚠️ **掃描快不代表聚合快。** 實測整份 `user_logs.csv`（30.5 GB）掃完僅 **26.7 秒**，但那是 Polars 的 projection pushdown 只解析了 `date` 一欄的結果。M2 的真實工作是讀全部 8 欄、對 4 億列做每人一組的 as-of 聚合，需要維護分組狀態，是完全不同的量級。不可以用掃描速度推估 M2 的可行性。

#### `members_v3.csv` — 用戶屬性

| 欄位 | 型別 | 說明 |
|---|---|---|
| `msno` | str | 用戶 ID |
| `city` | int | 城市代碼 |
| `bd` | int | 年齡 — ⚠️ **官方明示含 -7000 ~ 2015 的離群值** |
| `gender` | str | 性別（大量缺失） |
| `registered_via` | int | 註冊管道 |
| `registration_init_time` | `%Y%m%d` | 註冊日 |

**實測**：6,769,473 列，`msno` 全部唯一，無重複。

- ✅ **已驗證 `expiration_date` 欄位確實不存在** —— v3 版本乾淨，紅線 3 的前提成立
- `registration_init_time` 範圍：2004-03-26 ~ 2017-04-29
- ⚠️ **`bd`（年齡）範圍 -7168 ~ 2016，只有 32.83%（222 萬筆）落在合理的 10~100 歲區間**，其餘 455 萬筆是離群值。這個欄位的處理方式必須做對照實驗
- ⚠️ **`gender` 缺失 65.43%**（442.9 萬筆）
- `city` 21 種、`registered_via` 18 種 —— 兩者都是低基數類別，可直接 one-hot
- 🚫 **禁用 `members.csv`**，理由見 §5 紅線 3

##### ⚠️ 覆蓋率更正（v0.3）

v0.2 原文寫「members 涵蓋 677 萬用戶，遠多於任一 cohort 的 99 萬，**join 後不會有大量缺失**」。**這個推論方向是錯的** —— 「members 有 677 萬人」不蘊含「cohort 那 99 萬人都在其中」，集合大小不決定包含關係。

實測 `train.csv` 全體（原始檔層級，與斷言 12 同一把尺）：

| | 人數 | 佔比 | 流失率 |
|---|---|---|---|
| 在 `members_v3` 中 | 877,161 | 88.34% | 6.57% |
| **查不到** | **115,770** | **11.66%** | **5.02%** |
| 全體 | 992,931 | 100% | 6.3923% |

**兩個後果：**

1. **那 11.66% 的 `city` / `bd` / `gender` / `registered_via` 全部是 null。** 前處理不得假設 join 完就有值。特別是不能用 `df.fillna(df.mean())` 這種全表補值 —— 除了違反紅線 5，也會把「這個人沒有屬性資料」這個事實抹掉。
2. **「查不到」本身有訊號**（5.02% vs 整體 6.39%），應保留為一個布林特徵而非視為雜訊。`src/data/cohort.py` 產出的 `in_members` 欄位即為此用途，並刻意採 left join 而非 inner join，避免把這 11.6 萬人整批丟棄。

本項已列為 §2.3 斷言 12，由 `tests/test_data_contract.py::test_members_coverage_of_feb_cohort` 守門。

#### 其他

- `sample_submission_zero.csv`（2017-03 到期）／`sample_submission_v2.csv`（2017-04 到期）— 測試集用戶清單。兩檔欄位皆為 `msno` + `is_churn`，後者在提交檔中為預測值。
  - **實測 `sample_submission_zero.csv` = 970,960 列，與 `train_v2.csv` 完全相同。** 這證實它就是當年競賽的第一版測試集（Mar cohort），標籤公布後便成為 `train_v2.csv`。本專案不需要它。
  - **實測 `sample_submission_v2.csv` = 907,471 列**，即 §4.2 所指的最終測試集（Apr cohort，標籤不可見）。注意它比 Mar cohort 的 970,960 人**少 6.5%**，代表 cohort 規模本身逐月變動，計算投放預算時不能假設每月人數相同。
- `WSDMChurnLabeller.scala`（7.05 kB）— 官方標籤產生器，可用來自行建立更多歷史 cohort 的訓練標籤（M3 進階選項）

### 2.2 磁碟與記憶體預算

| 資源 | 現況（主要電腦） | 判定 |
|---|---|---|
| RAM | 32 GB | `user_logs` **實測 4.1 億列 / 30.5 GB**，無法全量進記憶體 → **強制使用 Polars lazy / DuckDB streaming** |
| GPU | GTX 1050 Ti 4 GB | 本專案全程不使用 |
| 磁碟 | C: 201.8 GB · D: 427.3 GB 可用 | 充裕。**資料一律放專案資料夾外**，主要電腦為 `D:/ml_data/` |

**實測磁碟佔用**（全部 10 個檔案下載並解壓後）：

| 目錄 | 內容 | 大小 |
|---|---|---|
| `archives/` | Kaggle 下載的 `.7z` | 8.95 GB |
| `raw/` | 解壓後的 CSV | 34.4 GB |
| `interim/` | as-of 特徵表 parquet 快取（Feb + Mar）| 84 MB |
| | **合計** | **約 43.4 GB** |

`interim/` 可隨時整個刪除重算，不算資產。兩個 cohort 各約 40 MB —— 99 萬列 × 20 欄壓成 40 MB，是 parquet 相對 CSV 的壓縮效果。

⚠️ **資料根目錄是設定值，不是常數。** 上表的 `D:/ml_data/` 是主要電腦的路徑；實際位置由 `configs/paths.yaml` 的 `data_root` 決定，該檔不進 Git。所有程式一律經 `src/config.py` 取得路徑，因此換機器時 `src/`、`scripts/`、`notebooks/`、`tests/` 均不需修改。範本見 `configs/paths.example.yaml`。

### 2.3 資料契約斷言（M0 交付）

`tests/test_data_contract.py` 必須斷言：

| # | 斷言 | 期望值（2026-08-06 實測） |
|---|---|---|
| 1 | 欄位名稱與型別與 §2.1 一致 | 欄位漂移直接失敗 |
| 2 | `train.csv` 筆數 | 992,931 |
| 3 | `train_v2.csv` 筆數 | 970,960 |
| 4 | `transactions.csv` 筆數 | 21,547,746 |
| 5 | `transactions_v2.csv` 筆數 | 1,431,009 |
| 6 | `members_v3.csv` 筆數 | 6,769,473 |
| 7 | `train` / `train_v2` 的 `msno` 無重複 | 各 0 筆重複 |
| 8 | `is_churn` 取值集合 | 恰為 {0, 1} |
| 9 | `members_v3` **不含** `expiration_date` 欄位 | 紅線 3 的守門測試 |
| 10 | `train` × `train_v2` 的 `msno` 交集 | **881,701** |
| 11 | 流失率 | Feb 6.3923% · Mar 8.9942% |
| 12 | **`members_v3` 對 `train.csv` 的覆蓋率** | **查不到 115,770 人（11.66%）** |

筆數容忍度設 **0 筆**——這是靜態的歷史資料集，數字變了就代表下載出錯或檔案被改動，不該容忍。

**斷言 12 為 v0.3 新增**，用意是把「members 有大量缺失」這個事實鎖進測試，避免 §2.1 那條被更正的敘述再度回到文件裡。

✅ **實作狀態**：以上 12 條全數實作於 `tests/test_data_contract.py`，另加兩項延伸斷言：

- `user_logs.csv` / `user_logs_v2.csv` 的筆數（392,106,543 / 18,396,362）
- 標籤欄位型別（`is_churn` 為整數、`msno` 為字串）

驗證方式：

```bash
uv run pytest tests/test_data_contract.py -q
```

原始資料不在本機時，測試會 **skip 而非 fail** —— 剛 clone 完 repo 的人不應該看到滿螢幕紅字。

---

## 3. 評估指標

### 3.1 主指標

**Log Loss**（官方指標，自然對數）：

```
logloss = -(1/N) Σ [ y·ln(p) + (1-y)·ln(1-p) ]
```

提交前預測值會被 clip 為 `max(min(p, 1-1e-15), 1e-15)`。**本地評估必須套用同樣的 clip**，否則本地分數與 LB 對不起來。

### 3.2 參考錨點（官方私榜，2017 年最終成績）

| 位置 | Log Loss |
|---|---|
| 🥇 第 1 名 | **0.07974** |
| 🥈 第 2 名 | 0.08926 |
| 🥉 第 3 名 | 0.09344 |
| 第 10 名 | 0.09886 |
| 第 20 名 | 0.10834 |

私榜以約 60% 的測試資料計算。

### 3.3 各里程碑的驗收門檻

| 階段 | 門檻 | 理由 |
|---|---|---|
| M1 | **在 Mar cohort 上 log loss < 0.30746** ✅ **實測 0.15925（−48.2%）** | 這是「用訓練集流失率 6.3923% 對所有人做常數預測」在驗證集上的實測分數。打不贏它的模型沒有存在意義 |
| M3 | ~~進入 Top-20 區間（≈ 0.108）~~ → **相對 M2 的 Mar 分數不得退步** ✅ **實測 0.15367（−2.16%）** | ⚠️ **v0.4 修正。** 0.10834 是官方私榜成績，算在 **Apr cohort** 上；本專案回報的分數算在 **Mar cohort** 上，兩者是不同資料集，直接比大小沒有意義。M3 的四個實驗只有換套件通過雙向驗證與配對 multi-seed（−2.16%），其餘三個不是變差就是只有單次量測 —— **模型這一側的空間約 2%**，這本身就是結論 —— 見 §7.4、§7.12 |
| M6 | **Apr cohort 的 Kaggle late submission 分數** | 這才是與 0.10834 可比的數字。**推論管線已交付**（`make kaggle`，§7.19）：提交檔 907,471 列已產生，**公榜分數待回填**。事前登記的預期是 **0.17342**（結構相同的本地對照 `feb_fixed → mar_fixed`），分數回來之後填進這一列，無論結果如何 |
| — | **不追求擠進前三** | 一個沒有人能用、沒有人能解釋、無法上線的分數，再低 1% 也沒有意義。刷分的邊際效益遠低於把服務做出來 |

### 3.4 輔助指標（M4 交付）

| 指標 | 用途 |
|---|---|
| Brier score · Reliability diagram ✅ | 機率是否可信。log loss 已隱含此要求，但必須畫圖驗證 —— **已交付**（圖 09/10/11）。畫出來的結論是校準器不該上線，見 §7.10 |
| PR-AUC | 流失為少數類，PR 曲線比 ROC 更能反映挽回名單品質 |
| Lift @ K% · Precision @ K% ✅ | 「只投放前 K% 名單」時的實際命中率 —— 最佳點命中率 85.69%、lift 9.53 |
| **期望淨收益曲線** ✅ | 見 §6，這是本專案真正的業務交付物 —— **已交付**（圖 12/13），見 §7.13 |

---

## 4. 驗證策略

> **這是全案唯一的一票否決項**：出現資料洩漏而未察覺，其他做得再好都不算數。以下每個決策都必須有理由。

### 4.1 為什麼不能用隨機 KFold

標籤定義為「到期後 30 天內是否有新交易」。資料集中的 `transactions_v2.csv` 涵蓋到 2017-03-31——對於 2 月到期的用戶而言，**3 月的交易紀錄就是標籤本身**。任何跨越到期日的隨機切分，都會讓答案直接出現在特徵裡。

隨機切分還會破壞第二件事：真實部署是「用過去的 cohort 訓練，預測下個月的 cohort」。隨機切分等於假設訓練與預測同期，這在上線後不成立。

### 4.2 採用的切分：時間外驗證（Out-of-Time）

```
訓練  ── train.csv     (2017-02 到期 cohort)  → 觀察期 2017-03
驗證  ── train_v2.csv  (2017-03 到期 cohort)  → 觀察期 2017-04
測試  ── sample_submission_v2 (2017-04 到期) → 觀察期 2017-05  [標籤不可見]
```

**為什麼這樣切**：訓練→驗證的關係（Feb→Mar）與驗證→測試的關係（Mar→Apr）在時間結構上完全同構。驗證分數因此可以直接外推到測試表現，這是唯一對應真實部署情境的切分方式。

**次要檢查**：在 Feb cohort 內部另做 5-fold StratifiedKFold，用途**僅限**估計模型變異數（回報標準差）。**模型選擇一律以時間外驗證分數為準**。若兩者差距過大，該差距本身就是要寫進報告的發現（概念漂移），不是要調掉的問題。

### 4.3 特徵的 as-of 截斷

每一筆樣本的特徵計算截止點：

```
cutoff(user) = membership_expire_date(user)
```

所有進入特徵的 `transaction_date` 與 `user_logs.date` 必須 **≤ cutoff**。

**為什麼用「每位用戶自己的到期日」而不是統一日期**：真實部署時，系統是在用戶即將到期時才對他評分並決定要不要投放挽回資源。2 月 1 日到期的人和 2 月 28 日到期的人，可用的歷史長度本來就不同，這是部署現實的一部分，不該被抹平。

**M6 追加** ✅：另做一版 `cutoff = expire_date - 7 天` 的模型並比較分數。實務上挽回優惠要提前寄出才來得及，7 天前的預測才是真正能用的模型。分數必然下降——**這個下降幅度要誠實寫進 README**，它展示的是部署思維。

> **已交付**（`make lead-time`，§7.15）：共同子集上 **0.15173 → 0.17921，退步 +18.11%**。兩個附帶發現都比這個數字重要：①掉出 cohort 的 1,573 人流失率 **70.25%**（全體的 7.8 倍）—— 能上線的模型看不到風險最高的一小撮人；②損失最大的訊號是 `days_since_last_tx`（48.4% → 13.6% gain）而不是 `last_is_cancel`。

### 4.4 群組結構

> ⚠️ **本節於 v0.2 依實測結果改寫。** v0.1 原本寫「採用分 cohort 切分時，群組重疊問題不存在」——實測證明這句話是錯的。

**實測結果**：

| | 數量 | 佔 Mar cohort |
|---|---|---|
| Feb cohort 唯一 `msno` | 992,912 | — |
| Mar cohort 唯一 `msno` | 970,959 | 100% |
| **兩期交集** | **881,693** | **90.81%** |
| 僅出現在 Mar（新進 cohort） | 89,266 | 9.19% |
| 交集用戶中兩期標籤翻轉 | 45,990 | 佔交集的 5.22% |

**這代表什麼**：採用 §4.2 的 Feb→Mar 切分時，驗證集有 **90.81% 的用戶模型在訓練時已經看過**。這**不是傳統意義的洩漏**（標籤來自不同時間窗，且真實部署本來就是每月對同一批訂戶重複評分），但它有兩個必須處理的後果：

1. **驗證分數會偏樂觀。** 用戶層級特徵（城市、註冊管道、註冊時間、慣用付款方式、年資）在相隔一個月時幾乎不變，而 94.78% 的交集用戶標籤也沒變。模型只要記住特徵簽章就能拿分，這不等於它學會了泛化。
2. **必須分群回報。** 見 §4.5。

**因此**：

- `tests/test_no_leakage.py` **不得**斷言「訓練集與驗證集 `msno` 交集為空」——那在本專案的主要切分下必然失敗
- 應斷言的是：**同一 (msno, cohort) 組合不得同時出現在訓練與驗證**
- **若為了增加樣本量而合併多個 cohort 做 KFold，必須用 `GroupKFold(groups=msno)`** ——此時 90.81% 的重疊會變成真正的洩漏。紅線 4 **確認啟用**

> **已交付**（`make final`，§7.20）：M6 合併 `feb_fixed + mar_fixed` 重訓最終模型，四段切分（train/es/sel/cal）全部綁 `msno`。違規對照組實測**每折驗證集有 75.2% 的用戶模型已經見過**，而兩臂的 log loss 差只有 0.008 倍折間標準差 —— **量不到**。為什麼量不到、以及為什麼這不推翻這條紅線，見 §7.20 第二點。

### 4.5 Cohort 組成分析（M0 實測發現，本專案最重要的結構特性）

把 Mar cohort 依「是否出現在 Feb cohort」拆開，流失率差異極端：

| 分群 | 人數 | 佔比 | **流失率** |
|---|---|---|---|
| 重複用戶（也在 Feb cohort） | 881,693 | 90.81% | **5.87%** |
| 新進用戶（僅在 Mar cohort） | 89,266 | 9.19% | **39.84%** |
| 全體 Mar cohort | 970,959 | 100% | 8.99% |

**兩群人的流失率差 6.8 倍。**

**為什麼會這樣**：能在連續兩個月都出現在到期名單上的用戶，多半是 30 天月租的長期訂戶——他們已經用行為證明了自己會續訂。而只在 3 月出現的，包含首次訂閱期滿的新客與非月租方案到期者，兩者都是高流失族群。

**這對建模的三個影響**：

1. **「用戶歷史長度／是否為新客」會是壓倒性的主力特徵。** 但它必須用 as-of 截斷正確計算（只看 cutoff 之前的交易史），不能用「有沒有出現在上一期標籤檔」這種取巧作法——測試集（Apr cohort）沒有對應的上一期標籤檔可查，那樣算出來的特徵在部署時不存在。
2. **兩期整體流失率從 6.39% 漲到 8.99%，主因很可能是 cohort 組成變化而非用戶行為惡化。** 這個歸因要寫進報告——把組成效應誤讀成「用戶忠誠度下降」是典型的分析錯誤。
3. **M4 的挽回名單必須分群檢視。** 若模型只是學會「新客風險高」，那挽回策略就退化成「對所有新客發優惠」，不需要機器學習。**真正的價值在於能否在同一分群內做出有效排序**。

**因此 M1 起的每一次評估都必須回報三個數字**，而不是只有一個總分：

```
log loss @ 全 Mar cohort        ← 對應官方指標，headline 數字
log loss @ 重複用戶子群 (90.81%)  ← 主力營收來源
log loss @ 新進用戶子群 (9.19%)   ← 高風險、高不確定性
```

三者的差距本身就是要寫進 README 的發現。只報總分會掩蓋模型在小分群上的失效。

**實作**：`src/evaluation/metrics.py` 的 `segment_report()` 產生這張表，`repeat_vs_new()` 負責分群。以常數預測（M1 基準線）為例，實測輸出：

| 分群 | 人數 | 佔比 | 實際流失率 | 平均預測機率 | log loss |
|---|---|---|---|---|---|
| 新進用戶 | 89,266 | 9.19% | 39.84% | 6.39% | **1.1353** |
| 重複用戶 | 881,693 | 90.81% | 5.87% | 6.39% | **0.2237** |
| 全體 | 970,959 | 100% | 8.99% | 6.39% | 0.3075 |

**新進用戶的 log loss 是重複用戶的 5.1 倍**，而 headline 的 0.3075 完全看不出這件事。「實際流失率」與「平均預測機率」並列也是刻意的：39.84% 對 6.39% 一眼就能看出校準失效，等於 M4 reliability diagram 的簡易版。

⚠️ **`repeat_vs_new()` 的結果只能用於評估切分，不得作為特徵**，理由見 §5.1 —— 測試集沒有上一期標籤檔可查。用於切報表是事後診斷，模型看不到；用於特徵則是部署時算不出來的東西。這條限制寫在該函式的 docstring 裡。

---

## 5. 🚫 紅線清單

> 以下任何一條被違反，該版本作廢重做。每一條都要有對應的**會失敗的測試**。

| # | 紅線 | 為什麼 | 對應測試（`tests/test_no_leakage.py`） | 狀態 |
|---|---|---|---|---|
| 1 | 任何 `transaction_date > cutoff` 的交易**不得**進入特徵 | 到期日之後的交易**就是標籤**。這是本題的頭號洩漏 | `test_red_line_1_guard_catches_violation` 等 4 項；另由 `assert_cutoffs_within_window()` 守住「cutoff 自己屬於哪個 cohort」（§7.9） | ✅ |
| 2 | 任何 `user_logs.date > cutoff` 的日誌**不得**進入特徵 | 到期後的收聽行為是結果，不是原因 | `test_red_line_2_guard_catches_post_cutoff_logs` 等 4 項；另由 `assert_logs_match_cohort()` 守住「這份日誌是哪個 cohort 算的」（§7.9） | ✅ |
| 3 | **禁用 `members.csv`，只能用 `members_v3.csv`** | `members.csv` 的 `expiration_date` 是快照欄位，官方在 2017-11-13 特地發布 v3 就是為了**移除這個洩漏欄位** | `test_red_line_3_no_code_reads_members_csv` 等 2 項 | ✅ |
| 4 | 合併多 cohort 時必須 `GroupKFold(groups=msno)` | **實測 90.81% 的用戶跨兩期出現**，隨機切分會讓同一人同時在訓練與驗證集 | `test_red_line_4_guard_catches_crossing_msno` 等 5 項，另 `tests/test_grouped_split.py` 15 項 | ✅ M6（§7.20）|
| 5 | 所有 imputation / scaling / encoding 統計量**必須在 fold 內計算** | 全表 `fillna(df.mean())` 會把驗證集資訊倒灌進訓練集。AI 產生的程式碼幾乎必犯 | `test_red_line_5_feature_builder_is_stateless` | ✅ |
| 6 | Target encoding 必須 out-of-fold | `payment_method_id` 是高基數類別，直接 target encode 會讓 CV 飆高、實測崩盤 | `test_red_line_6_target_encoding_is_oof` 等 3 項 | ✅ |
| 7 | 不得使用 2017-04（測試觀察期）之後的任何資料 | 未來資訊 | `test_red_line_7_no_future_dates` 等 2 項 | ✅ |
| 8 | 本地評估必須套用官方的 `clip(1e-15, 1-1e-15)` | 否則本地分數與 LB 不可比 | `test_red_line_8_clip_prevents_infinity` 等 3 項 | ✅ |

**「會失敗的測試」是字面意思。** 證明「目前的資料通過檢查」證明不了任何事 —— 一個永遠回傳 `None` 的空函式也會通過。因此紅線 1 的守門邏輯被抽成 `src/data/cohort.py` 的 `assert_asof_respected()`，測試餵給它一張**確實違規**的表（某用戶的 `last_tx` 晚於自己的 `cutoff`），斷言它會 raise。邊界值 `last_tx == cutoff` 必須通過 —— 用戶在到期日當天交易是合法的，那筆資料在評分時點確實看得到。

該守門函式**在 `build_cohort()` 中每次都會執行**，不是只在測試裡。洩漏一旦發生，寧可整支中斷，也不要靜靜產出一張錯的特徵表。

⏸ 的那幾條依賴尚未存在的程式碼。**為不存在的東西寫測試只能寫出假的通過**，因此以 `@pytest.mark.skip(reason=...)` 保留在清單中，並於 `pyproject.toml` 設定 `addopts = "-ra"`，使每次 `pytest` 都重新列出它們與各自的阻塞里程碑。測試套件因此同時是一份活的待辦清單。

**M6 之後八條全部 ✅，`tests/test_no_leakage.py` 一條 skip 都不剩。** 紅線 5 在 M1 由「讓特徵建構完全無狀態」滿足 —— 一個不計算統計量的函式不可能把驗證集資訊倒灌進訓練集；紅線 6 在 M3 由 `src/features/encoding.py` 滿足，並用三個實測違規的控制組量出違規的代價；紅線 4 在 M6 由 `src/models/grouped.py` 滿足（阻塞里程碑曾由 M3 改判為 M6，理由見 §7.4 第七點）。

⚠️ **紅線 4 的違規對照組量到的代價是 0.008 倍折間標準差 —— 也就是量不到**（§7.20）。這不推翻這條紅線：`msno` 不是特徵、同一個人的兩列不是重複列、深度 6 的樹沒有記住個人的容量，三者合起來讓這個模型兌現不了那個洩漏。**規定值不值得遵守與這次量到多少無關** —— 遵守的成本是一行 `groups=msno`，而不遵守時沒有任何跡象會告訴你這次兌現了沒有。

**紅線 8 的一個實作細節**：`clip` 的兩側並不對稱。`1 - 1e-15` 在 float64 中無法精確表示，最接近的 double 與 1 的距離約 9.992e-16 而非 1e-15，因此 `-ln(1-(1-eps)) ≈ 34.5396`，而低端是 `-ln(eps) ≈ 34.5388`。官方計分（sklearn）用相同的 clip、有相同的不對稱，故 `src/evaluation/metrics.py` **照抄而不「修正」**。改用 `log1p` 讓兩側對稱在數值上較佳，卻會使本地分數與 LB 產生系統性偏差 —— 正好違背本條紅線的目的。

### 5.1 避雷清單（非紅線，但幾乎每個人都會踩）

- **`bd`（年齡）實測範圍 -7168 ~ 2016，只有 32.83% 落在 10~100 歲。** 直接當數值特徵會毀掉模型。處理方式（截斷／分箱／視為缺失）必須做對照實驗，並在 README 記錄選擇理由。同理 `gender` 缺失 65.43%，「缺失」本身可能就是一個有訊號的類別。
- **`is_cancel` 不是流失。** 官方文件明示用戶可能因換方案而取消，實測全量僅 3.98% 帶此旗標。把它當標籤或當強特徵前，先確認它與 `is_churn` 的實際關係。
- **⚠️ 算「歷史取消次數」時，聚合必須在 as-of 過濾之內。**（v0.3 新增）實測 158,766 列交易的 `transaction_date` 晚於自己的 `membership_expire_date`，其中 95.93% 是取消紀錄（見 §2.1）。對某位用戶跑 `is_cancel.sum()` 時若忘了先套 `transaction_date <= cutoff`，就會把「到期日之後才發生的取消」算進特徵——那是紅線 1 的直接違反，而且不會有任何錯誤訊息，只會讓 CV 分數莫名變好。`src/data/cohort.py` 的 `n_cancel_hist` 是在過濾**之內**聚合的，新增任何歷史統計量時比照辦理。
- **判斷「沒收到錢」不能只看 `actual_amount_paid == 0`。**（v0.3 新增）實測 122 萬列實付 0 元，其中 53.97% 的 `plan_list_price` 本來就是 0（免費試用），與「定價 149 卻收 0 元」是兩件事。必須兩欄一起看，見 §2.1。
- **只載入 `transactions_v2.csv` 等於沒有交易歷史。** 實測它 74.76% 是 2017-03 的交易，必須與 `transactions.csv`（2,154 萬列）合併使用。
- **別讓「是否出現在上一期標籤檔」變成特徵。** 它在訓練集上威力驚人（見 §4.5 的 6.8 倍差距），但測試集沒有對應的上一期標籤檔，部署時算不出來。要用 as-of 的交易史長度來代理。
- **不要一開始就跑全量。** 先做 `make smoke`（1% 抽樣）把整條 pipeline 跑通。`user_logs` 30 GB，最後一天才發現跑不完是歷年最常見的翻車原因。
- **SMOTE 先做對照再決定。** 本題流失率不屬極度不平衡，且 log loss 對機率分布敏感，過採樣通常會破壞校準。
- **log loss 的優化方向與 AUC 不同。** AUC 只看排序，log loss 同時看機率絕對值。一個 AUC 更高但過度自信的模型，log loss 會更差。**校準在本題不是加分項，是必需品。**

---

## 6. 業務指標轉換

技術指標必須翻譯成營運決策 —— 一個不能換算成行動的機率，對營運單位等於沒有。

### 6.1 決策模型

對即將到期的用戶投放挽回優惠，單一用戶的期望淨收益：

```
E[淨收益] = p_churn × r_save × LTV_saved − C_offer
```

| 參數 | 說明 | 來源 |
|---|---|---|
| `p_churn` | ~~模型輸出的**校準後**流失機率~~ → **未校準的模型輸出**。整體低估約 27%，但**該比例不可套用到名單或金額**（§6.3 第二點） | 模型。⚠️ **v0.5 修正**：校準器實測反而讓每一項指標變差，理由見 §7.10 |
| `r_save` | 投放後的挽回成功率 | 假設值，做敏感度分析 |
| `LTV_saved` | 挽回一位用戶的客戶終身價值 | 由 `plan_list_price` × 預期續訂月數推估 |
| `C_offer` | 每次投放的成本 | 假設值 |

### 6.2 交付圖表（M4）

1. **期望淨收益 vs 投放比例曲線** ✅ — 橫軸「投放給預測流失機率最高的前 K% 用戶」，縱軸期望淨收益。曲線的極大值就是最佳投放門檻。這張圖是整個專案的核心交付物。**已交付**（圖 12，`make eval`）：基準情境的最佳投放比例是 **5.0%**，見 §7.13。
2. **Reliability diagram** ✅ — 沒有校準，上面那條曲線算出來的錢是假的。**實測結果比預期糟：低估的成因不是校準，是 cohort 之間的基準率漂移，而校準器修不掉它**（§7.10）。因此第 1 張圖用未校準機率繪製，並在圖上標註低估的方向與幅度。
3. **參數敏感度熱圖** ✅ — `r_save` × `C_offer` 的網格上，最佳投放比例如何變動。展示結論的穩健性。**已交付**（圖 13）：48 組假設裡 42 組的標籤結算模擬淨收益為正。

⚠️ 第 1 張圖的橫軸「前 K%」預設**排序是連續可切的**。這個假設在未校準機率上成立（97 萬人有 81.6 萬個相異值），但套上 isotonic 之後會失效 —— 機率最低的 59% 被壓成同一個值，**K > 41% 之後門檻就落在那個區塊裡**，等於要從 57 萬個完全同分的人中挑出誰該投放。這是選擇不上線校準器的第二個理由，見 §7.10 第三點。

### 6.3 必須誠實寫出的限制

**一、模型預測的是「會不會流失」，不是「投放優惠能不能改變他的行為」。** 後者需要 uplift modeling 與 A/B 實驗，本資料集不具備實驗組／對照組結構，無法回答。這一點已寫進 [`MODEL_CARD.md`](MODEL_CARD.md) §3 第一條（「⛔ 不可用於」的第一項）——把相關性當因果是這類專案最常見的越界，主動指出它反而是專業判斷力的展示。

**二、投放曲線上的金額全是模擬，而且模型的機率偏差不能當成單一數字往下套。**

先講**模擬**：真實的只有 Mar 的流失標籤。`r_save`（挽回成功率）與 `LTV_saved` 仍然是假設，本專案沒有實驗組／對照組，**無法宣稱任何投放真的改變了行為**（見第一點）。因此曲線上的兩條線分別叫「期望模擬淨收益」與「標籤結算模擬淨收益」，**兩條都不是賺到的錢**。

再講**偏差**：CatBoost 在 Mar **全體 cohort** 的平均預測流失率低估約 27%。這是一句關於**全體平均**的話，**不能**套到：

| 套到哪裡 | 為什麼不行 |
|---|---|
| 個別用戶 | 每個人的偏差不同 |
| 個別風險區間 | §7.10 的 reliability 曲線顯示各段偏差差很大 |
| 前 K% 投放名單 | 那是高機率子集，偏差與全體無關 —— 實測見 §7.13 |
| 淨收益 | `p × r × LTV − C` 只有第一項隨 p 縮放，`C_offer` 是**固定成本** |

最後一項最容易漏掉：**機率低估 27% 不等於淨收益低估 27%。** 投放名單自己的偏差必須自己量，`src/evaluation/decision.py::subset_calibration()` 就是做這件事的，§7.13 有實測數字。

整體偏差的成因是 Feb → Mar 的基準率漂移（6.39% → 8.99%，+40.7%），不是機率曲線的形狀 —— 模型在自己月份的乾淨保留集上校準得幾乎完美。校準器只能 fit 在已知標籤的月份，因此**看不到、也修不掉下一個月的漂移**（§7.10）。

能修掉它的條件是「一塊與部署期同分布的標籤資料」，本專案沒有：Apr cohort 的標籤是 Kaggle 測試集，本地取不到。這一條已寫進 [`MODEL_CARD.md`](MODEL_CARD.md) §5。

---

## 7. 里程碑

M0–M6 的里程碑骨架如下。

| 階段 | 產出 | 驗收標準 |
|---|---|---|
| **M0** | Repo 骨架 + `scripts/download.py` + 資料契約測試 + CI | `make setup && make test` 綠燈；§2.3 的 12 項斷言全部通過 |
| **M1** ✅ | `transactions` + `transactions_v2` + `members_v3` 的 LightGBM baseline | Mar cohort log loss **< 0.30746**；**依 §4.5 分三群回報**；回報 5-fold 標準差 —— **全數達成，實測 0.15925**，見 §7.2 |
| **M2** ✅ | `user_logs` 聚合特徵模組（Polars lazy / DuckDB） | **4.1 億列 / 30.5 GB** 在 32 GB RAM 內完成聚合；近 7/14/30/90 天收聽行為、完播率、活躍天數、趨勢斜率；`make features` 可重複執行 —— **全數達成**，見 §7.3 |
| **M3** ✅ | LGBM / XGB / CatBoost 三方比較 + null importance 特徵篩選（另加 target encoding 對照與超參數搜尋） | 比較表 + 篩選前後對照 —— **已交付**，實測 **0.15367（−2.16%，CatBoost）**，並經配對 multi-seed 8/8 確認（§7.12）。§3.3 的 0.108 門檻**無法在本地量測**，改列 M6，理由見 §7.4 第六點 |
| **M4** ✅ | 機率校準 + 業務指標 | Reliability diagram、Brier、期望淨收益曲線、敏感度熱圖 —— **全數交付**。校準：診斷（圖 09/10）+ isotonic 校準器（圖 11），結論是**不上線**（§7.10）。業務指標：期望淨收益曲線與敏感度熱圖（圖 12/13），最佳投放比例 5.0%（§7.13） |
| **M5** ✅ | SHAP 解釋 + 流失原因碼生成 | 任一用戶可輸出 Top-3 流失原因 —— **已交付**，`make explain`（§7.14）。名單 48,853 人（`p > p*`），逐人 Top-3 中文原因碼 + 圖 14，輸出分營運呈現與底層稽核兩份。加總恆等式最大差 6.9e-15。⚠️ 例句「由 22 降至 4」的兩時點差特徵集裡沒有，改用 `(log90 − log30) / 2` 的對照並標明它是解釋用脈絡；**每句原因碼帶量測時點**，`last_is_cancel` 標為到期日訊號、不可沿用到 M6 的 T−7 版本（影響 48.79% 的人、42.35% 的解釋強度） |
| **M6** ✅ | FastAPI 服務 + PSI 漂移監控 + 線上 Demo | Docker 起服務，`/predict` 回傳機率與原因；含 `cutoff = expire_date − 7d` 版本的分數對照 —— **`lead_days` 這一塊已交付**（`make lead-time`，§7.15）：共同子集退步 **+18.11%**，並量出「看不到的那批人流失率 70.25%」這個部署約束。**`/predict` 與模型 artifact 也已交付**（`make artifact-t7` + `make serve`，§7.16）：兩種介面、機率與 SHAP 即時算、artifact 必填 `cutoff_definition`、載入時比對特徵程式的邏輯指紋。**PSI 漂移監控也已交付**（`make drift`，§7.17）：三個部署時拿得到的監控全部回報「穩定」，而基準率動了 +39.9% —— 那條限制本身就是交付物。**`MODEL_CARD.md` 已交付**（§7.18）：五條「不可用於」、監控盲區、以及群組公平性這個**明講的空白**。**Kaggle 推論管線也已交付**（`make kaggle`，§7.19）：新增第三種 cutoff 規則（固定評分日）與第九條守門（cutoff 不得超出資料涵蓋範圍），提交檔已產生，公榜分數待回填。**紅線 4 也已交付**（`make final`，§7.20）：合併 `feb_fixed + mar_fixed` 重訓最終模型，四段切分全部綁 `msno`，八條紅線到齊；違規對照組量到的代價**小於實驗解析度**，而同分布 CV 比時間外分數樂觀 **13.0%**。**Docker 與線上 Demo 也已交付**：容器部署於 Hugging Face Spaces，使用平台中立的 `Dockerfile` 部署（見 `deploy/README.md`）。唯一未結的是 Kaggle late submission 的公榜分數（§3.3） |

### 7.1 M0 完成度（2026-08-08）

| 項目 | 狀態 |
|---|---|
| SPEC.md · README.md | ✅ |
| 資料下載腳本 `scripts/download.py`（內嵌 byte 數契約、可重複執行） | ✅ |
| 全部 10 個檔案下載並實測 | ✅ |
| 環境：uv · Python 3.12 · editable 套件 · `configs/paths.yaml` 路徑設定 | ✅ |
| `src/` 基礎模組：`config` · `data.cohort`（as-of 截斷）· `evaluation.metrics` | ✅ |
| EDA `notebooks/eda_01_overview.py` + `reports/figures/` 8 張圖 | ✅ |
| `tests/test_data_contract.py` —— §2.3 全 12 條斷言 | ✅ |
| `tests/test_no_leakage.py` —— 紅線 1 / 3 / 7 / 8（另 4 條以 skip 保留，見 §5） | ✅ |
| `Makefile` —— `make help` 列出全部目標 | ✅ |
| `.github/workflows/ci.yml` —— ruff + pytest | ✅ |

**M0 完成。** 目前測試狀態：**39 passed · 4 skipped**（skip 的 4 條為紅線 2 / 4 / 5 / 6，依賴尚未存在的程式碼，見 §5）。

#### CI 的驗證範圍限制

CI runner 上**沒有原始資料**（依競賽規則不進 Git），43 條測試在該環境只有 7 條能執行。**一個全部 skip 的套件同樣會顯示綠燈**，這與 §4.5 指出的「只報總分會掩蓋失效」是同一類錯誤。

因此不需資料的純邏輯測試被標記為 `nodata`，CI 單獨執行該子集並要求**全過且零 skip**：

| CI 實際驗證 | 涵蓋 |
|---|---|
| 紅線 1 | `assert_asof_respected()`，含餵入違規資料確認會 raise |
| 紅線 3 | 程式碼不得引用 `members.csv` 的靜態掃描 |
| 紅線 8 | clip 行為、手算對照、錯誤輸入處理 |
| Lint | `ruff check` + 格式檢查 |

§2.3 的 12 條資料契約斷言、紅線 7、以及 M1 基準線 0.30746 都需要本機資料，**只能在下載過資料的機器上執行**（`make test`）。CI 綠燈不蘊含資料正確，這條界線必須在報告中講清楚。

### 7.2 M1 完成度（2026-08-08）

**三項驗收標準全數達成。** 執行 `make train`（實作 `src/models/train.py`，設定 `configs/model_lgbm.yaml`）。

> 本節數字為 §7.11 修正後重測（現行基準，`reports/rebaseline/`）。
> 23 特徵的 M1 對照來自 `make ablation` 的 `txn_only`，61 特徵的 M2 來自
> `make train`。

| 驗收標準 | 結果 |
|---|---|
| Mar cohort log loss < 0.30746 | ✅ **0.15925**（23 特徵，改善 48.2%） |
| 依 §4.5 分三群回報 | ✅ 見下表 |
| 回報 5-fold 標準差 | ✅ **0.08344 ± 0.00028**（61 特徵） |

#### 本階段最重要的兩個發現

**一、fold 內分數與時間外分數差 262 個標準差。**

| | log loss |
|---|---|
| Feb cohort 內部 5-fold | 0.08344 ± 0.00028 |
| 時間外（Mar cohort） | **0.15705** |
| 差距 | **+0.07361 ＝ 262 σ** |

模型在 Feb 內部極度穩定，換一個月就掉將近一倍。§4.2 早已預告：「若兩者差距過大，該差距本身就是要寫進報告的發現（概念漂移），不是要調掉的問題。」現在有了數字 —— **只報 fold 內的 0.083 會讓模型看起來好一倍**。

這也回答了 §4.2 為何堅持以時間外分數作為唯一的模型選擇依據。

**二、模型系統性低估流失率。**

| 分群 | 人數 | 佔比 | 實際流失率 | 平均預測 | log loss |
|---|---|---|---|---|---|
| 新進用戶 | 89,266 | 9.19% | 39.84% | **27.28%** | **0.53746** |
| 重複用戶 | 881,693 | 90.81% | 5.87% | **4.43%** | 0.11854 |
| 全體 | 970,959 | 100% | 8.99% | **6.53%** | 0.15705 |

預測平均 6.53% 對實際 8.99%：模型學到的是訓練集 Feb 的基礎流失率 6.39%。**§6.1 的期望淨收益公式以 `p_churn` 相乘，低估 27% 會讓投放門檻設得過於保守。**

⚠️ **這個發現原本的標題是「M4 校準的必要性已可量測」，M4 做完之後證明那只對了一半。** 低估是真的，但成因不是校準器修得掉的那一種 —— 模型在 Feb 自己的乾淨保留集上校準得幾乎完美（ECE 0.00038），低估全部來自 Feb → Mar 的基準率漂移。實測見 §7.10。

新進用戶的 log loss 是重複用戶的 4.5 倍，而 headline 貼近重複用戶那組（佔 90.81%）—— §4.5 要求分三群回報的理由在此得到驗證。

#### 特徵重要度

| 特徵 | gain 佔比 |
|---|---|
| `last_is_cancel` | 35.2% |
| `last_is_auto_renew` | 22.6% |
| `days_since_last_tx` | 15.5% |
| `last_payment_method_id` | 8.6% |
| 前四名合計 | **81.9%** |

與 M0 EDA 的三分群發現一致。⚠️ **分數高度依賴 `last_is_cancel`，而取消多半發生在到期日當天，即 cutoff 當天。** §4.3 要求的 M6 版本（`cutoff = 到期日 − 7 天`）會失去大部分此訊號 —— 那個下降後的分數才是能上線的分數。

#### 工程面

- **超參數全在 `configs/model_lgbm.yaml`**，程式碼無數字常數（§8）
- **Early stopping 用 Feb 內部切分，不用 Mar** —— 停在第幾輪是看著分數做的決定，用 Mar 決定會讓回報的 Mar 分數偏樂觀
- **5-fold 各 fold 用固定輪數**而非各自 early stopping，讓標準差只反映資料變異
- **`deterministic: true`**，兩次獨立執行的五個 fold 分數逐位相同
- **MLflow 追蹤已建立**（§8）：20 個 metric、22 個 param、模型與特徵重要度 artifact。後端用 SQLite —— MLflow 3.x 起檔案系統後端進入維護模式，直接使用會拋例外

### 7.3 M2 完成度（2026-08-08）

**驗收標準全數達成，但結果需要誠實解讀。** 執行 `make features && make train`，實作 `src/features/logs.py`。

| 驗收標準 | 結果 |
|---|---|
| 4.1 億列在 32 GB RAM 內完成聚合 | ✅ **31 GB RAM 全程無壓力**，見下方策略 |
| 近 7/14/30/90 天行為、完播率、活躍天數、趨勢 | ✅ 38 個特徵 |
| `make features` 可重複執行 | ✅ 兩層快取，中斷可續 |

#### 記憶體策略：先收斂，再聚合

| | 原始 | 收斂後 |
|---|---|---|
| 列數 | 410,502,905 | **68,993,949**（16.8%） |
| 大小 | 31.9 GB | **2.20 GiB** |
| 耗時 | — | **32 秒** |

關鍵洞察是 **§7 要求的最長窗口只有 90 天**，因此 2015 至 2016 上半年的日誌對任何特徵都沒有貢獻：

```
Feb cohort  cutoff 2017-02-01 ~ 02-28  →  只需要 2016-11-03 起
Mar cohort  cutoff 2017-03-01 ~ 03-31  →  只需要 2016-12-01 起
兩者聯集                                   2016-11-03 ~ 2017-03-31（149 天）
```

日期過濾與用戶 semi-join 都放在管線最前端，由 predicate pushdown 在讀取階段丟棄不需要的列，記憶體中永遠只有一個 batch。**順序反過來（先 join 再 filter）需要把 4 億列的中間結果實體化，那才是會 OOM 的寫法。**

#### 分數結果：投入與回報極不相稱

| | M1（23 特徵） | M2（61 特徵） | 變化 |
|---|---|---|---|
| **時間外（Mar）** | 0.15925 | **0.15705** | **−1.38%** |
| 重複用戶 | 0.12063 | 0.11854 | −1.73% |
| 新進用戶 | 0.54071 | 0.53746 | −0.60% |

> ⚠️ **§7.11 修正後這個數字腰斬：原本 −2.93%，現在 −1.38%。** 同日交易定序
> 的修正對 M1（23 特徵、更依賴 `last_*`）的幫助遠大於 M2，兩者的差距因此
> 縮小。**「投入與回報極不相稱」的結論不但沒有動搖，還更強了** —— 31.9 GB
> 現在只換到 1.38%。

| 特徵來源 | 特徵數 | gain 佔比 |
|---|---|---|
| 交易與用戶屬性 | 23 | **89.01%** |
| 收聽行為 | 38 | **10.99%** |

**處理 31.9 GB 只換來 3.15%。** 這不是實作缺陷，M0 的 EDA 已經預告：

| 近 30 天活躍天數 | Feb 流失率 | Mar 流失率 |
|---|---|---|
| 0 天 | 9.23% | 10.06% |
| 26–30 天 | **5.38%** | **9.51%** |

**活躍「水準」在 Feb 單調遞減、在 Mar 完全平坦** —— 訓練集有效而驗證集無效，模型學不到可外推的東西。只有「趨勢」兩邊方向一致（崩跌組 10.01% / 12.47%，上升組 5.13% / 8.31%），這印證了設計時採用趨勢比值而非絕對水準的判斷。

#### 兩個意料之外的觀察

**一、18.4% 的 cohort 用戶 90 天內完全沒有收聽紀錄，而這件事沒有訊號。**

| | Feb | Mar |
|---|---|---|
| 無任何紀錄 | 6.12% | 6.80% |
| 有紀錄 | 6.45% | 9.48% |

推測為自動續訂的休眠訂戶：不使用但持續扣款，因此不流失。模型也同意 —— `log_has_logs` 的 gain 為 **0**，一次都沒被用到。缺失一律保留為 null 而不補 0：「沒有紀錄」與「聽了 0 秒」是不同的兩件事。

**二、這個結果本身就是業務結論。** 對本資料集而言，**「付錢的意願」比「使用的程度」更能預測續訂** —— `last_is_cancel` 與 `last_is_auto_renew` 合計 54.4% 的 gain，全部收聽行為加起來只有 11%。這是要寫進報告的發現，不是要掩蓋的失敗。

**對 M3 的意涵**：38 個特徵僅貢獻 11%，其中多數很可能是雜訊，null importance 特徵篩選因此具備明確動機。

#### 消融實驗（`scripts/ablation.py`）

為判斷是否值得繼續投資收聽特徵，將其拆為 Recency / Frequency / Intensity / Trend 四組，各自與交易特徵合併訓練一次，並額外訓練一次「完全不含交易特徵」的對照。全部以時間外 Mar 分數比較，不跑 5-fold（消融比較的是分數之間的差異，Feb 內部變異數對此無貢獻）。

| 實驗 | 特徵數 | Mar log loss | 相對 M1 | 每特徵效率 |
|---|---|---|---|---|
| `txn_only`（M1） | 23 | 0.15925 | — | — |
| **`log_only`** | 38 | **0.29724** | **+86.6%** | — |
| txn + trend | 26 | 0.15878 | −0.30% | 0.098% |
| txn + frequency | 31 | 0.15809 | −0.73% | 0.091% |
| txn + recency | 25 | 0.15813 | −0.70% | 0.352% |
| txn + intensity | 47 | 0.15726 | −1.25% | 0.052% |
| `all`（M2） | 61 | 0.15705 | −1.38% | 0.036% |

**一、收聽行為沒有獨立訊號。** `log_only` 為 0.29724，常數基準為 0.30746 —— 38 個特徵僅勝出 3.3%。單獨使用時，收聽資料幾乎不含可用資訊。

**二、四組高度重疊。** 各組單獨貢獻合計 2.98%，全部併用僅 1.38%，重疊約一半。四組實際上都在衡量同一件事：該用戶是否仍在使用服務。

**三、EDA 的預測與實測相反，這是本階段最重要的方法論教訓。**

依 §7.3 的單變量分析，預期 trend 最強（唯一在兩個 cohort 方向一致者）、intensity 最弱（在 Mar 完全平坦）。實測恰好相反：intensity −3.26% 為最強，trend 與 frequency 並列最弱（−1.60%）。

原因在於單變量分箱衡量的是「該特徵單獨能否區分」，而模型使用它時**已具備 23 個交易特徵**。單變量表現好可能只是重複了交易特徵已有的資訊；單變量平坦也可能在條件上仍具區分力。

**結論：單變量 EDA 不足以預測多變量的增量貢獻，唯有消融實驗能回答「這組特徵值不值得」。** 此結論同時修正了 §7.3 依 EDA 所作的歸因。

**決策**：依此結果停止投資 M2 的特徵工程，資源轉向 M3。

#### 紅線 2 已實作

`assert_logs_within_cutoff()` 與紅線 1 的守門同構：抽成獨立函式以便餵入違規資料驗證它會 raise，並在 `build_log_features()` 中每次執行。八條紅線目前僅餘 4 與 6 待 M3。（**M3 後續**：紅線 6 已實作，紅線 4 改判為 M6，見 §7.4。）

### 7.4 M3 完成度（2026-08-08，數字於 2026-08-09 重測）

**四個實驗，只有一個帶來可信的改善：換模型（CatBoost −2.16%）。**

> ⚠️ **本節數字重測過三次。** 第一次因 §7.5 的 cutoff 洩漏，第二次因 §7.8 的
> 列順序不決定性，第三次因 §7.11 的同日交易定序。**第二次改變了結論**（原本
> 判為不可信的 CatBoost 優勢變成可信），**第三次強化了它**（優勢從 0.86%
> 擴大到 2.16%，並經 §7.12 的配對 multi-seed 8/8 確認）。舊數字集中列在
> §7.11，不再出現在本節推理中。

| 實驗 | 指令 | 最佳 Mar log loss | 相對自身基準 | 判定 |
|---|---|---|---|---|
| **三方模型比較** | `make compare` | **0.15367**（CatBoost） | **−2.16%** | ✅ **可信**（§7.6 雙向 + §7.12 配對 8/8） |
| 超參數隨機搜尋 | `make tune` | 0.15543 | −0.87% | ⚠️ 單次量測 |
| Target encoding 對照 | `make encode` | 0.15579（OOF TE） | −0.64% | ⚠️ 單次量測，且屬教學對照實驗 |
| Null importance 特徵篩選 | `make select` | 0.15879 | **+1.28%（變差）** | ❌ 三次重測方向一致 |

M2 的基準是 **0.15705**。調參與篩選的基準是 **0.15679**（訓練集少了切給 `sel` 的 15%，見各節說明）。作為對照，M1 的交易特徵一步就是 −48.2%。

**採用的模型：CatBoost，Mar log loss 0.15367。** 它比 LightGBM 慢 21 倍（233 秒 vs 11 秒），換到 2.16% —— 比修正前的 0.86% 好得多，因此成本取捨的天平也移動了。M6 應依服務的重訓頻率重新評估。

#### 一、三方比較：CatBoost 勝出，雙向驗證與配對 multi-seed 皆通過

同一份 61 個特徵、同一個 Feb→Mar 切分、同一塊 Feb 內部 early stopping 驗證集（`src/models/compare.py`）。三家皆未調參。

| 模型 | 輪數 | Mar log loss | 相對 LightGBM | 重複用戶 | 新進用戶 | 秒 |
|---|---|---|---|---|---|---|
| **CatBoost** | 1404 | **0.15367** | **−2.16%** | **0.11660** | **0.51978** | 233 |
| 三者平均 | — | 0.15459 | −1.57% | — | — | — |
| LightGBM | 498 | 0.15705 | — | 0.11854 | 0.53746 | **11** |
| XGBoost | 399 | 0.15726 | +0.13% | 0.11878 | 0.53737 | 26 |

**三家極差 0.00359（2.34%），而配對 σ 最大 0.00044**（§7.12 實測）。差距約 8σ，且 §7.6 的反向驗證與 §7.12 的 8 個配對 seed 都確認排序成立。

**CatBoost 的優勢集中在新進用戶**：對 LightGBM 在重複用戶上贏 1.64%，在新進用戶（9.19%、流失率 39.84%）上贏 3.29%。SPEC §8 當初選它的理由是「對高基數類別有原生處理」，這裡看到的可能是同一件事 —— 新進用戶交易史短，類別特徵的相對權重更高。

**集成沒有贏過 CatBoost**（0.15459 vs 0.15367）。三家的特徵重要度排名前四完全一致，錯誤高度相關，等權平均只是把 CatBoost 的優勢稀釋掉。

**LightGBM 對 XGBoost 只差 0.13%**，單次量測分不出來（0.47σ）；但 §7.12 的
8 個配對 seed 顯示 LightGBM 確實較優（7/8 勝、95% CI 不含 0）。**單次量測沒有
解析度，不等於沒有差別。**

> ⚠️ 這張表比的是「三個套件在各自預設體質下、拿到相同資料時的表現」，不是「同一個模型的三種實作」。LightGBM 是 leaf-wise、XGBoost 是 depth-wise、CatBoost 是對稱樹，三者不存在等價的超參數。取捨寫在 `configs/model_comparison.yaml`。

#### 二、Null importance：22 個特徵確實是雜訊，但砍掉之後 Mar 變差

方法：真實標籤訓練 1 次取得 actual gain，把標籤整欄打亂重訓 20 次取得 null 分布，每個特徵跟**自己的** null 分布比（`src/models/selection.py`）。

**門檻在 Feb 內部選，Mar 只看兩次。** Feb 切成 train(70%) / es(15%) / sel(15%)；null importance 只跑在 train 上，門檻由 sel 決定，凍結之後才在 Mar 上算「篩選前」與「篩選後」。

| 門檻 | 特徵數 | Feb-sel log loss | 相對全特徵 |
|---|---|---|---|
| 全特徵（未篩選） | 61 | 0.07850 | — |
| > null p50 ~ p100 | 39 | 0.07851 | +0.02% |

⚠️ **§7.11 修正後，六個門檻收斂成同一組 39 個特徵。** 修正前不同分位數會切出
36~39 個不等，現在 p50 到 p100 選出完全相同的一組 —— 真實 gain 與 null 分布
的分離變乾淨了，中間地帶消失。這本身是修正有效的旁證。

篩選在 Feb 內部**幾乎沒有效果**（+0.02%，遠小於任何雜訊尺度），門檻凍結在
p50（39 個特徵）。然後：

| 階段 | 特徵數 | Feb-sel | **Mar** | 重複用戶 | 新進用戶 |
|---|---|---|---|---|---|
| 全特徵（未篩選） | 61 | 0.07850 | **0.15679** | 0.11840 | 0.53598 |
| > null p50（凍結） | 39 | 0.07851 | **0.15879** | 0.11945 | 0.54739 |
| | | +0.02% | **+1.28%** | | |

**Feb 內部改善 0.42%，時間外反而退步 0.78%。方向相反，而且兩次重測都是這個方向。**

被砍掉的 25 個裡有 22 個是收聽特徵，而 §7.3 的消融已經測到收聽特徵在 Mar 上確實有 2.93% 的貢獻。「在 Feb 內部贏不過自己的雜訊」與「在 Mar 上沒用」是兩件事。

**結論：不採用 null importance 篩選，61 個特徵全留。** 若 M6 為了推論成本要砍到 36 個，代價是 +0.78%，那是工程取捨不是模型取捨。

#### 三、Target encoding：紅線 6 已實作，並量出違規的真實代價

`src/features/encoding.py` 實作 out-of-fold target encoding 與守門函式 `assert_encoding_is_oof()`。`scripts/target_encoding.py` 跑五個變體，其中**三個是刻意寫錯的控制組**：

> **本表已在新基準上重跑**（第三次重測之後，`make encode`）。五個變體共用
> 同一份 Feb 三段切分、同一組超參數、同一個 seed —— 差異只有編碼方式。
>
> ⚠️ **這是洩漏教學／對照實驗，不參與選模。** 最終模型仍由 §7.12 的配對
> multi-seed 決定（CatBoost）。違規變體的分數再漂亮都不得改變模型選擇、
> 不得寫入模型 artifact、不得進入 headline —— 本腳本也確實不產生任何 artifact。

**合規路徑逐條落實五條規定**：先切 train/es/sel 再編碼；train 用 5-fold OOF
（每列只用其他折的標籤）；es/sel/Mar 的 mapping 只 fit 在 train；smoothing、
global prior、類別平均全部只從 train 算；未見類別回退 train 的 global prior。
`msno` 由 `assert_not_identifier()` 在合規路徑上擋掉。

**內部分數報 Feb-sel 而不是 Feb-es**：`es` 是 early stopping 的依據，拿它當
內部分數會偏樂觀；`sel` 全程沒被任何決定看過。

| 變體 | 合規 | 輪數 | Feb-sel | Mar 時間外 |
|---|---|---|---|---|
| A 原生類別（對照） | ✅ | 533 | 0.07850 | 0.15679 |
| **B OOF TE · `last_payment_method_id`** | ✅ | 367 | 0.07871 | **0.15579** |
| C Naive TE · `last_payment_method_id` | ⛔ | 389 | 0.07868 | 0.15587 |
| D Naive TE · `msno`（切分後） | ⛔ | 2 | 0.22097 | 0.28517 |
| **E Naive TE · `msno`（切分前）** | ⛔ | 2000 | **0.00000** | **1.31996** |

**E 是紅線 6 的完整示範。** 先對整個訓練 cohort 做 target encoding、之後才切驗證集 —— 內部 log loss 是 **0.00000**（完美），時間外是 **1.31996**，比常數基準 0.30746 還差 **4.3 倍**。這就是紅線 6 說的「CV 飆高、實測崩盤」，一字不差。

**C 與 B 只差 0.00008（0.18σ），這是本實驗最有價值的發現：同樣的違規在低基數欄位上量不出來。** `last_payment_method_id` 只有 33 種取值、每種平均兩萬多列，把自己那一列的標籤算進去只讓類別平均值動 1/24000。重測後兩者更接近了 —— 這個結論比修正前更強。

**因此紅線 6 的門檻必須訂在「作法」而不是「分數有沒有變差」** —— 靠看分數抓這個錯誤，在 C 那種情況下抓不到，而同一段程式碼換到高基數欄位就是 E。

**B 相對 A 改善 0.64%（2.3σ），但只有單次量測。** 它與 CatBoost 的優勢很可能是同一件事（CatBoost 的原生類別處理就是內建的 ordered target statistics），兩者不宜相加。**要採用需先照 §7.12 的配對 multi-seed 協定重測** —— §7.12 已經示範過單一切分的解析度極限。

#### 四、超參數搜尋：符號翻轉過一次，方向目前穩定但仍是單次量測

31 次訓練（1 次基準 + 30 次隨機抽樣），9 維搜尋空間（`configs/tuning.yaml`）。Feb 切成三塊，**Mar 全程不參與搜尋，只在最後看一次**。

| | sel（Feb 內部） | Mar（時間外） |
|---|---|---|
| 基準設定（M1/M2） | 0.07850 | 0.15679 |
| 最佳 trial 4 | **0.07815** | **0.15543** |
| 變化 | −0.45% | **−0.87%（3.1σ）** |

**⚠️ 這個結果在列順序修正之前是相反的**：當時量到的是 Mar **退步 0.99%**。§7.8 修正後變成改善 0.65%，§7.11 修正後是改善 0.87% —— 後兩次同向。

即便如此，三次都只有**單一 split**，而 §7.12 已經示範了單次量測的解析度極限。**判定：暫不採用，需照 §7.12 的配對協定重測。** 沿用 `configs/model_lgbm.yaml` 的未調參設定，搜尋結果保留在 `<interim>/m3_artifacts/best_params.yaml` 供追溯。

最佳參數的方向是「更慢、更深」（`learning_rate` 0.05 → 0.018、`num_leaves` 31 → 203）。

#### 五、M3 的總結論

**分數的天花板確實不在模型這一側，但也不是完全沒有空間。**

四個實驗裡：

- **換套件是唯一通過雙向驗證與配對 multi-seed 的改善**（−2.16%，8/8 勝），代價是 21 倍的訓練時間
- **兩個「在 Feb 內部做選擇」的實驗**（篩特徵、調參）**都不可信**：篩選三次都在 Mar 上退步；調參的符號翻轉過一次，且三次都只有單一 split
- **Target encoding 的改善方向穩定（−0.64%）但只有單次量測**，且它是洩漏教學實驗，不參與選模

對照組是 M1：加上交易特徵一步就是 −48.2%。**M3 的四個實驗加起來約 2%。**

**這改變了後續的資源配置**：M4（校準）、M5（SHAP 原因碼）、M6（服務）都不是為了提高分數，而是為了讓這個分數**可用** —— M3 的結果把「別再刷分」從一個價值判斷變成一個有數據支持的決定。

**同時它留下一個反覆出現的方法論教訓**：「這個差距是不是雜訊」的答案，取決於你拿什麼當雜訊尺度 —— 而這件事在本專案已經出錯三次：

| 次數 | 用錯的 σ | 後果 | 記於 |
|---|---|---|---|
| 一 | Feb 內部 5-fold 的 0.00084 | 把真實的 CatBoost 優勢判成「不可信」 | §7.6 |
| 二 | 混入列順序不決定性的 0.00048 | 該值本身高估了雜訊 | §7.8 |
| 三 | **邊際** σ 用來判**配對**差 | 把真實的 LightGBM > XGBoost 判成「雜訊」 | §7.12 |

**σ 不是一個常數，是一個要跟著「你在比什麼」而變的量。**

#### 六、§3.3 的 M3 門檻無法在本地量測（SPEC 需修正）

§3.3 訂 M3 的門檻是「進入 Top-20 區間（≈ 0.108）」。**這個門檻寫錯了。**

0.10834 是官方**私榜**成績，算在 **Apr cohort**（907,471 人，標籤不可見）上。本專案回報的分數算在 **Mar cohort**（970,959 人）上。兩者是**不同的資料集**，直接比大小沒有意義。

**要真正檢驗這個門檻，唯一的方法是對 Apr cohort 產生預測並提交 Kaggle 的 late submission。** 那需要 M6 的推論管線，因此門檻的檢驗應移到 M6。

**建議修正**：§3.3 的 M3 門檻改為「相對 M2 的 Mar 分數不得退步」（實測 −0.86%，達成），並新增一列 M6 門檻「Apr cohort 的 Kaggle late submission 分數」。

#### 七、紅線狀態

| 紅線 | M3 之前 | M3 之後 |
|---|---|---|
| 6（target encoding OOF） | ⏸ 等 M3 | ✅ **已實作**，含 3 條單元測試 + 3 個實測違規控制組 |
| 4（合併 cohort 用 GroupKFold） | ⏸ 等 M3 | ⏸ **改等 M6** |

紅線 4 的重新判定：M3 的四個實驗**沒有任何一處合併兩個 cohort**，而本專案只有兩個帶標籤的 cohort，一旦合併就沒有時間外驗證集可用。真正會需要它的是 M6 —— 上線前通常會用 Feb + Mar 全部資料重訓最終模型，那時 90.81% 的重疊就變成真正的洩漏。

> **後續**（§7.20）：M6 如期觸發並實作。而「一旦合併就沒有時間外驗證集可用」這句話在那裡量成了數字：**同分布 CV 比時間外分數樂觀 13.0%**，比紅線 4 本身擋掉的（量不到）大三個數量級。

#### 八、工程面

- **四個實驗四個設定檔**（`model_comparison` / `feature_selection` / `tuning` / 沿用 `model_lgbm`），程式碼無數字常數（§8）
- **`configs/tuning.yaml` 不複製基準參數**，直接讀 `configs/model_lgbm.yaml` —— 對照組必須就是 M1/M2 跑的那一組，複製一份總有一天會不同步
- **`FeatureSet.take()` 讓切列停在 polars 這一層**：XGBoost 與 CatBoost 需要帶欄名與 dtype 的表格才分得出哪幾欄是類別特徵
- **`xgb_category_levels()` 只收一個 frame**：類別字典是「擬合出來的前處理狀態」，紅線 5 要求它只能在訓練資料內計算。簽名做成單一參數，讓 `xgb_category_levels(feb.X, mar.X, ...)` 這種寫法**根本寫不出來**
- **MLflow 新增 6 個 run**（3 個模型 + 集成 + 篩選 + 調參）
- **測試 101 passed · 1 skipped**，CI 的零 skip 檢查從 23 條升到 **67 條**

---

### 7.5 M3 期間發現並修正的 7 個洩漏風險（2026-08-08）

M3 完成後對特徵管線做了一次獨立的洩漏審查，發現 7 個問題並全部修正。**其中第 1 項導致 M1–M3 的全部分數重測**，因此單獨成節記錄。

| # | 問題 | 影響 |
|---|---|---|
| **1** | **cutoff 本身由未來交易推導** | **全部分數重測** |
| 2 | `build_features()` 的 join 邊界未重跑紅線 2 守門 | 無（防禦縱深） |
| 3 | cohort 快取命中時跳過 as-of 驗證 | 無（防禦縱深） |
| 4 | log 特徵快取命中時完全不驗證 | 無（防禦縱深） |
| 5 | 註冊日晚於 cutoff 者仍保留 members 快照屬性 | 8 人 |
| 6 | Mar 同時擔任選特徵與回報成績 | 篩選結論反轉 |
| 7 | XGBoost 類別字典納入 Mar 分布 | 1 列 |

#### 第 1 項：cutoff 是基準點，而基準點自己可以來自未來

`src/data/cohort.py` 原本這樣算 cutoff：

```python
tx.filter(membership_expire_date 落在本 cohort 的到期區間)
  .group_by("msno").agg(membership_expire_date.max())
```

它只要求「到期日落在區間內」，**沒有要求那筆交易在到期日當下已經發生**。實測有 158,766 筆交易的 `transaction_date` 晚於自己宣告的 `membership_expire_date`（95.93% 是取消紀錄，屬於補登慣例）。這種列在到期日當下**還不存在**，卻可以憑著它攜帶的到期日替用戶製造出一個 cutoff。

**這是紅線 1 的守門抓不到的洩漏形式。** `assert_asof_respected()` 檢查的是「`last_tx <= cutoff`」—— 而這裡出問題的是 **cutoff 這個基準點自己**。基準點錯了，所有以它為準的檢查都會通過，卻全部建立在未來資訊上。

**修正**：加一條 `transaction_date <= membership_expire_date`。這個條件不循環（不依賴尚未算出的 cutoff），而且一旦成立，`transaction_date <= cutoff` 也自動成立。

**代價與影響**：

| | 修正前 | 修正後 |
|---|---|---|
| Feb cohort | 992,931 | **992,912**（−19） |
| Mar cohort | 970,960 | **970,959**（−1） |
| cutoff 值改變 | — | Feb 156 人 · Mar 148 人 |
| M1（23 特徵） | 0.16335 | **0.16299** |
| M2（61 特徵） | 0.15821 | **0.15910**（+0.56%） |

> ⛔ 上表**兩欄都是在 §7.8 的列順序修正之前**量的，因此都帶著 ±0.0006 的不決定性；§7.11 之後更是**完全無法重現**（同日交易定序已改）。此表保留原值，因為它記錄的是**那一次 cutoff 修正的效果**，而那個效果是兩欄之間的差，不是任何一欄的絕對值。現行可重現值見 §7.11。
| M1 5-fold | 0.08546 ± 0.00049 | **0.08563 ± 0.00031** |
| 三方比較贏家 | LightGBM | **CatBoost** |

**M2 變差 0.56%，方向是對的** —— 拿掉一個洩漏本來就該掉分。找不到任何「當下可觀測」交易的用戶離開 cohort 也是正確的：部署時同樣算不出他們的 cutoff，硬留著等於假裝當時知道未來。

#### 第 6 項：修好選擇偏誤，讓一個相反的結論浮出來

原本 `scripts/select_features.py` 讓每個門檻都在 Mar 上評分再挑最低的 —— Mar 同時當了選模集與最終成績單。修正後改為在 Feb 內部三段切分上選門檻，凍結後才看 Mar 兩次。

**這個修正改變了結論**：舊作法挑出「全特徵最好、篩選沒用」；新作法挑出 p99（35 個特徵），而它在 Mar 上退步 0.57% —— 真正的發現不是「篩選沒用」，而是「**在 Feb 內部有效的篩選，換到 Mar 就失效**」。見 §7.4 第二點與第五點。

#### 第 5 項：快照屬性不只 `days_since_registration` 一欄

`members_v3.csv` 是快照，不受 §4.3 的 as-of 截斷保護。原本只把 `days_since_registration` 轉成 null（因為天數會是負的，很顯眼），卻留下同一列的 `city` / `gender` / `registered_via` / `bd` / `in_members`。**負數只是症狀，整列不該可見才是病因。** 修正後這幾位（Feb 6 人、Mar 2 人）的全部 members 屬性一律退回缺失。

#### 第 2–4 項：守門要放在邊界上，快取不是保證

三者是同一個模式：守門只寫在「正常路徑」上，而快取命中與公開 API 都繞過了它。修正後 `build_cohort()` / `build_log_features()` 在**快取命中時也跑守門**，`build_features()` 在 join 邊界上重跑紅線 2。

快取是一個 parquet 檔，不是一個保證 —— 它可能來自舊版程式、可能被手動覆蓋、可能來自另一台機器。只檢查欄位存在等於只驗證「形狀對」，而形狀對、內容洩漏的表會一路通到模型裡，且分數會**變好**，因此不會有人起疑。

> ⚠️ **尚未解決的相關問題**：守門驗證的是「洩漏」，不是「過時」。這次修正 cutoff 邏輯之後，舊快取依然能通過所有守門（它們確實沒有洩漏），只是算法已經不同 —— 本次是靠人工 `force=True` 重建的。真正的解法是在快取檔裡寫入一個涵蓋程式碼版本的指紋，命中時比對不符就重算。**列為 M4 的待辦。**

---

### 7.6 反向時間外驗證：CatBoost 的優勢站得住（2026-08-08，2026-08-09 重測）

**問題**：M3 的每一個結論都建立在**同一組** train/valid（Feb → Mar）上。分數是好是壞，分不清是模型的本事、還是 Mar 這個月剛好合它胃口。

本專案只有兩個帶標籤的 cohort，所以無法再找一個月來驗證（自建標籤的嘗試見 §7.7）。但**方向可以反過來**：用 Mar 訓練、在 Feb 驗證，就得到第二組獨立的 train/valid。

> ⚠️ **反向不是第二個部署估計。** 真實部署是「用過去預測未來」，Mar → Feb 是時間倒流。它唯一能回答的是：**換一組 train/valid，M3 的結論還成不成立。** headline 一律用正向的分數。

**為什麼這不是洩漏**：擔心的點是 Mar 的特徵（as-of 三月到期日）包含了決定 Feb 標籤的那些交易。但**評估時模型只看得到 Feb 的特徵**，那份特徵對 Feb 標籤是乾淨的；訓練學到的是「特徵 → Mar 標籤」，Feb 標籤全程沒進訓練。真正存在的偏誤是 §4.4 記錄過的 90.81% 重疊，它在兩個方向同時存在且量級相當，因此方向間的比較是公平的。

#### 兩個方向的結果

| 模型 | 正向 Feb→Mar | 正向排名 | 反向 Mar→Feb | 反向排名 |
|---|---|---|---|---|
| **CatBoost** | **0.15367** | 1 | **0.11499** | 1 |
| 三者平均 | 0.15459 | 2 | 0.11517 | 2 |
| LightGBM | 0.15705 | 3 | 0.11693 | 3 |
| XGBoost | 0.15726 | 4 | 0.11717 | 4 |

**§7.11 修正後，兩個方向的排名完全一致**（修正前 CatBoost 與三者平均在反向會換位）。

反向的絕對分數低很多，因為 Feb 的流失率只有 6.39%（Mar 是 8.99%），常數基準也從 0.30746 降到 0.24217。兩者不可直接比大小。

#### 判定必須跟雜訊尺度比，不能只看排名

「排名有沒有變」會把兩件完全不同的事混為一談：**差 0.1% 的兩個選項換位**，與**真正的翻轉**。前者本來就該換來換去。

因此判定一律把差距除以 **σ = 0.00044** —— **配對差的標準差**，由 §7.12 的配對 multi-seed 實測（三組配對 σ 取最大值）。

> ⚠️ **這個 σ 換過兩次，每次的理由都不同，而且都改變過結論。** 初版用 Feb 內部 5-fold 的 0.00084（量錯指標）；第二版用 Mar 的邊際 σ 0.00048（指標對了，但**邊際**不是**配對**）。完整脈絡見 §7.12。

| 對比 | 正向 | 反向 | 判定 |
|---|---|---|---|
| XGBoost vs CatBoost | −8.16σ | −4.95σ | **可信** |
| CatBoost vs LightGBM | **−7.69σ** | **−4.40σ** | **可信** |
| XGBoost vs 三者平均 | −6.07σ | −4.56σ | **可信** |
| LightGBM vs 三者平均 | −5.60σ | −4.00σ | **可信** |
| LightGBM vs XGBoost | −0.47σ | −0.56σ | **單次分不出**（§7.12 配對 8 seed 顯示差距是真的） |

> σ 倍數是同一批實測差距除以新常數，為精確換算，未重跑 —— 原始分數見上表與
> `reports/rebaseline/m3_reverse.log`。

#### 結論

**一、完整排序在兩個方向都成立**：CatBoost > 三者平均 > LightGBM > XGBoost。所有跨層級的比較都通過雙向 1σ 檢驗；唯一在單次量測下分不出的是 LightGBM 與 XGBoost，而 §7.12 的配對實驗補上了那一格（LightGBM 較優，7/8 勝）。

**二、M3 的模型比較結論因此成立。** CatBoost 對 LightGBM 的 2.16% 優勢不是單月特性，且 §7.12 的 8 個配對 seed 全數同向。

**三、但這個結論在 2026-08-09 之前是相反的。** 修正 §7.8 的列順序不決定性、並把 σ 從 0.00084 換掉之前，同一個比較被判為「存疑」（正向 1.54σ、反向 0.93σ），SPEC 一度據此建議「改以成本為準採用 LightGBM」。

**雜訊底線估錯，會把真實效果誤判成雜訊。** 這個方向的錯誤比一般擔心的「把雜訊當成效果」更隱蔽 —— 因為它看起來像是謹慎。`tests/test_reverse_validation.py::test_catboost_beats_lightgbm_in_both_directions` 把這個案例釘住，以免日後又被改回去。

**四、成本取捨仍然存在，但天平移動了。** CatBoost 慢 21 倍（233 秒 vs 11 秒），換 **2.16%**（修正前是 0.86%）。M6 應依服務的重訓頻率決定 —— 但那是一個**知道差距是真的、而且比原本以為的大 2.5 倍**之後的取捨。

#### 工程面

- `scripts/reverse_validation.py`（`make reverse`），兩個方向各跑一次三方比較，約 12 分鐘
- `load_cohort_features()` 與 `run_comparison()` 的 train/valid cohort 可參數化，預設仍是 §4.2 的 Feb → Mar
- 判定邏輯抽成純函式 `pairwise_stability()`，由 `tests/test_reverse_validation.py` 的 8 條 nodata 測試守門 —— 其中一條直接寫死本次實測值，讓判定規則的任何改動都能立刻對照到「當時那批數字會被判成什麼」，不必重跑 12 分鐘

---

### 7.7 自建歷史 cohort 的嘗試（失敗，2026-08-08）

**動機**：§7.6 指出 M3 的所有結論都建立在同一組 train/valid 上。最直接的解法是增加月份 —— §2.1 的 `WSDMChurnLabeller.scala` 正是為此提供的（列為「M3 進階選項」）。目標是自建 2016-12 與 2017-01 兩個 cohort。

**方法**：本機沒有 Java / Scala / Spark，因此把官方程式移植成 Polars（`src/data/labeller.py`）。移植比直接執行多一個好處：`train.csv` 與 `train_v2.csv` 就是官方跑出來的答案，可以當迴歸測試。**對得上才敢拿去生新月份。**

**結果：對不上。**

| 設定 | 候選人數（官方 992,931） | 標籤一致率 |
|---|---|---|
| 完全照抄（history = 2017-01） | 856,144 | 98.77% |
| history 放寬到全歷史 | 879,537 | 98.36% |
| 名單改用官方、只測標籤規則 | 990,836 | 97.91% |

Mar cohort 更差，只有 **95.09%**。兩個具體症狀：

1. **候選名單差 11%。** 官方 Feb 名單裡有 111,358 人，在 2017-01-31 當下的到期日並不落在二月 —— 其中 **85,398 人落在三月**。照這支 Scala 的定義，他們根本不該進 Feb cohort。
2. **流失率系統性偏低**（4.9~5.1% vs 官方 6.39%）。方向一致代表我們找到了官方認定不存在的續約，不是隨機噪音。

**結論**：官方發布的標籤**不是**由這支 sample Scala 產生的，或它讀的 `wsdm_transactions_20170331.csv`（路徑含 `wsdm_fix`）與本專案手上的兩個交易檔不是同一份資料。

**因此不採用自建標籤。** 用它生出來的 2016-12 / 2017-01 與官方的 2017-02 / 2017-03 不是同一個定義，跨月穩定性比較會變成用不同尺規量不同的東西 —— 那比只有兩個月更糟，因為差異會被誤讀成「模型在某個月不穩」。

程式碼與這五次實驗的負面結果一起保留在 `src/data/labeller.py`，讓下一個人不必重做。若日後要繼續，缺口是**定位那 11% 名單差異的成因**；在還原度達到 99.9% 之前，自建標籤不得進入任何回報的數字。

**替代方案**：§7.6 的反向時間外驗證（零額外資料成本），以及 M6 的 Kaggle late submission（唯一真正外部的裁判）。

---

### 7.8 方案變動特徵：假設被推翻，但抓到一個更嚴重的問題（2026-08-09）

**來源**：使用者提出的假設 —— 換過方案的人，**天數變長**的應該比較願意續訂。

#### 一、單變量：方向錯了，但「有沒有換」是強訊號

比較每位用戶 cutoff 之前**最後兩筆交易**的 `payment_plan_days`（取消交易也算，實測含不含結論相同）：

| 方向 | 人數 | 佔比 | 流失率 |
|---|---|---|---|
| 只有一筆交易 | 17,080 | 1.7% | 38.74% |
| **天數變長** | 13,934 | 1.4% | **46.47%** |
| **天數變短** | 9,185 | 0.9% | **44.91%** |
| 沒變 | 952,713 | 95.95% | **4.85%** |

**變長比變短還糟**，與假設相反。推測換成長天期方案的那群多半是一次性購買（M0 實測 395 天方案流失率 77.74%、90 天 57.44%），買完就走而非承諾更久。

但「**有沒有換**」是 9.3 倍的差距：那 2.3% 的用戶貢獻約 **17% 的總流失量**。

#### 二、多變量：整體未達門檻，但新進用戶 5/5 一致改善

新增兩個特徵 `plan_days_delta`（帶方向與幅度）與 `plan_days_changed`（旗標），在同一個 Feb→Mar 切分上做配對比較。

上表的五組 seed **是在第三點的列順序修正之後跑的**，因此可重現；發現問題的那次單一 seed 比較（−0.00063）則否，已作廢。

**門檻在實驗之前就訂好**：改善須 > 0.00084（當時採用的 σ，即 Feb 內部 5-fold 標準差）。§7.6 已證明低於 1σ 的差距換個方向就翻轉，事後看數字再定門檻就是同一個錯。

> ⚠️ **本節之後 σ 被修正為 0.00048**（見第三點與 §7.6）。用新的 σ，平均 −0.00050 會是 1.04σ —— 剛好跨過門檻。**但決策不變**，理由是這個實驗有比任何外部 σ 更直接的證據：**配對差值本身的分布**。5 次的差值標準差是 0.00071，平均的標準誤 0.00032，`平均 / 標準誤 = 1.58`，且 5 次裡有 2 次符號相反。用它自己的資料判斷，效果不顯著 —— 這個結論與選哪個 σ 無關。

| seed | 不含 | 含 | 差 |
|---|---|---|---|
| 42 | 0.15802 | 0.15819 | +0.00017 |
| 7 | 0.15872 | 0.15760 | −0.00112 |
| 2026 | 0.15864 | 0.15732 | −0.00133 |
| 314 | 0.15755 | 0.15775 | +0.00020 |
| 99 | 0.15839 | 0.15799 | −0.00040 |

**平均 −0.00050 ± 0.00071，5 次裡 2 次變差 → 未達門檻。**

分群（§4.5 規定每次評估都要報）：

| 分群 | 平均效果 | 改善次數 |
|---|---|---|
| 重複用戶（90.8%） | −0.00013 ± 0.00058 | 3/5 → 雜訊 |
| **新進用戶（9.2%）** | **−0.00413 ± 0.00266** | **5/5** |

**效果是真的，但只在新進用戶身上。** 機制說得通：新進用戶交易史短，一次方案變動佔他們可觀察行為的比重大得多；有 20 筆以上交易的老用戶，一次變動被稀釋掉。而 M4 的校準診斷顯示新進用戶正是校準最差的一群（ECE 0.124、MCE 0.304）。

**決策：不採用。** 依事先訂好的門檻辦。改成 M6 的預先假設 —— M6 的 `cutoff = 到期日 − 7 天` 會讓 `last_is_cancel`（35% 的 gain）大幅消失，模型將更依賴這類「歷史形狀」特徵，屆時它很可能會通過門檻。**重測時沿用本節的門檻與多 seed 設計，不得改成只看單一 seed。**

#### 三、真正的收穫：列順序不固定，讓所有分數都不可重現

這個實驗的比較過程中發現，`build_cohort` 的**輸出列順序在每次重建時都不同** —— polars 的 `group_by` 不保證順序。而下游的 `train_test_split` 是**依位置**切分的：順序一變，train / early-stopping 就換一批人。

**實測量級**：同一份設定、同一份原始資料，只因重建快取，M2 的 Mar log loss 在 **0.15853 ~ 0.15910** 之間跳動（差 0.00057，約 0.7σ）。

**這比特徵本身重要得多**：

1. **README 宣稱「每一個數字都可用對應的 `make` 指令重現」是假的。** 任何人今天跑 `make train` 得到的數字都與文件不同。
2. **0.7σ 的雜訊底線，和我們想量的效果（0.00050）同一個量級。** 在修掉之前，任何小於 1σ 的比較實際上是在量「重建快取的運氣」。§7.6 用 1σ 當判準是對的，但當時並不知道雜訊的來源之一是這個。

**修正**：`build_cohort()` 在寫入快取前 `.sort("msno")`。排 `msno` 而不是保留輸入順序，是因為 msno 是唯一鍵，排序結果與掃檔順序、執行緒數、polars 版本都無關。修正後兩次重建的輸出逐位元相同。

**代價**：M1–M4 的所有數字必須重測一次。這是第二次因為基礎設施問題重測全部 —— 第一次是 §7.5 的 cutoff 洩漏。

**教訓**：兩次都不是模型或特徵的問題，是**資料管線的決定論**出問題。而兩次都只在「想量一個小效果」時才暴露出來 —— 因為在那之前，沒有人需要相信小數點後第四位。

---

### 7.9 第二次洩漏審查：六個守門缺口（2026-08-09 發現，2026-08-10 修正）

§7.5 的第一次審查修掉了七個**已經發生**的洩漏。這一次審查問的是不同的問題：

> **守門擋得住嗎？**

作法是逐一檢視每個「資料進入模型」的邊界，對每個發現的缺口寫一條**會失敗的
測試**（`tests/test_leakage_audit.py`），再修到它變綠。

#### 共同成因：兩條紅線都只檢查內部一致性

| 守門 | 它檢查什麼 | 它**不**檢查什麼 |
|---|---|---|
| `assert_asof_respected`（紅線 1） | 最後一筆交易不晚於 cutoff | **cutoff 本身對不對** |
| `assert_logs_within_cutoff`（紅線 2） | 最近一筆日誌不晚於 cutoff | **這份日誌是不是別的 cohort 算的** |

兩者都假設「基準點是對的」。而三個快取（cohort / 收聽特徵 / 收斂檔）都只驗證
形狀，沒有任何一個記錄自己是用什麼參數算出來的 —— 快取檔名只帶 cohort 名稱，
不帶到期區間、不帶窗口設定。

**一份用錯參數產生的快取可以完整通過所有守門，而且分數會變好，因此不會有人
起疑。**

#### 六個缺口與修法

| # | 嚴重性 | 缺口 | 修法 |
|---|---|---|---|
| 1 | **高** | cohort 快取不檢查 cutoff 是否落在宣告的到期區間 | 新增 `assert_cutoffs_within_window()` |
| 2 | **高** | `build_features()` 接受任何 cohort 的收聽特徵 | 收聽特徵輸出帶 `cutoff` 欄，`assert_logs_match_cohort()` 逐人比對 |
| 3 | 中 | 收聽特徵快取只驗證一個欄位 | `expected_log_columns()` 由 `LOG_WINDOWS` 推導，缺欄位就重算 |
| 4 | 中 | `narrow_logs()` 快取命中時 `specs` 完全沒被使用 | 檔名帶日期範圍 `user_logs_window_{lo}_{hi}.parquet` |
| 5 | 中 | 校準集是 M3 已用過兩次的同一批人 | 獨立的 `configs/calibration.yaml`，seed 20260809 |
| 6 | 低 | `members_v3` 快照時點沒有被記錄 | 宣告 `MEMBERS_SNAPSHOT_DATE`，量化暴露面 |

#### 第 2 條：實測 71.89% 的用戶會被影響

這一條最值得細說，因為它示範了「守門擋不住自己想擋的東西」。

把 Mar 的收聽特徵接到 Feb 的 cohort 上：

```
Feb cohort 992,912 人，Mar 收聽特徵 796,298 人 → 可 join 上 713,835 人（71.89%）
build_features(Feb cohort, Mar 收聽特徵) → 沒有拋出任何例外，產出 (992912, 61)
交集用戶的 Mar cutoff 比 Feb cutoff 晚：中位數 28 天，最大 58 天
```

**紅線 2 必然放行** —— 它檢查 `log_min_days_before >= 0`，而那是相對於**日誌
自己那個** cutoff。Mar 的日誌對 Mar 的 cutoff 當然合法。

於是「二月到期的用戶」的特徵含了到期後 28 天的收聽行為 —— 而 Feb 的標籤正是
由那段時間決定的。整個過程沒有一行程式碼會抱怨。

**修法**：`build_log_features()` 在輸出裡帶 `cutoff` 欄（**出身證明，不是特徵**，
`build_features()` 驗證完就 drop），由 `assert_logs_match_cohort()` 逐人比對。
修正後同一個呼叫會擋下 713,835 位用戶的不符。

#### 一個順序上的教訓

第一版把 schema 檢查放在紅線 2 之前，結果弄壞了 §7.5 留下的
`test_log_feature_cache_hit_is_revalidated_before_use` —— **一份含洩漏的快取
會被「反正要重算」默默蓋過去**。

已改為 **先驗洩漏（raise），再驗過時（重算）**。兩者處置本來就不同：

- **洩漏**代表出了嚴重的事，必須整支中斷讓人看見
- **過時**只是要重算，是正常維護

把兩者混在同一個分支，等於讓嚴重的錯誤被例行處置吸收掉。

#### 第 5 條的殘餘風險（誠實記錄）

換 seed **不等於隔離**。校準集與 M3 的選擇集期望重疊仍有約 15%（原本是 100%）。

要完全不重疊需要四段切分（train / es / sel / cal），代價是再縮小訓練集並讓
M1–M4 全部重測。**由於 M3 的兩個選擇（null importance 門檻、超參數）最後都
沒有被採用**（§7.4），`sel` 對現行模型的實際影響為零，因此換 seed 是相稱的
處置。**M6 重訓最終模型時應改採四段切分。**

> **已交付**（§7.20）：`catboost_fixed_full` 用的就是四段切分，四段各對應一個
> 決定（權重／停點／要不要採用校準器／fit 校準器），且四段全部綁 `msno`。

#### 第 6 條沒有修任何東西

`members_v3.csv` 是 2017-11-13 的快照，不是 as-of 各 cohort cutoff 的狀態。
一位用戶若在 2017 年年中搬家，Feb cohort 的特徵會拿到 cutoff 之後才成立的值。

**這是本專案唯一一個已知、且無法從資料修復的洩漏** —— 資料集沒有提供屬性的
歷史版本。實測暴露面：

| 欄位 | gain 佔比 |
|---|---|
| `city` | 0.365% |
| `registered_via` | 0.255% |
| `bd`（clean + valid） | 0.253% |
| `in_members` | 0.028% |
| `gender` | 0.024% |
| **合計** | **0.93%** |

`registration_init_time`（0.43%）**不受影響** —— 註冊日不會事後變動。

0.93% 是**上界**且很可能高估甚多（多數人的城市與性別本來就不會變）。

**為什麼仍要寫下來**：§4 的評分條款是「出現資料洩漏**且未察覺**，該項直接
0 分」。這一條無法消除，能做的是把它量化並記錄 —— 一份沒有標註時點的快照，
讀者無從判斷屬性有多舊，也就無法評估風險。M6 的 `MODEL_CARD.md` 需要這個
數字。常數寫在 `src/features/build.py::MEMBERS_SNAPSHOT_DATE`。

#### 影響範圍：分數完全沒有變

六條修正**全部是守門，不動任何特徵定義**。修正後實測：

```
特徵數 61（cutoff 未進入特徵矩陣）
M2 Mar log loss 0.15821 —— 與修正前逐位元相同
```

這是刻意的驗收條件：一個「加了檢查卻改變了分數」的修正，代表它其實動到了
計算，那就必須先解釋為什麼。

#### 測試

`tests/test_leakage_audit.py` 六條。原始版本全紅（漏洞存在的證據），修正後
改寫成守門測試 —— **同樣的違規輸入，現在必須被擋下來**。全套件
**101 passed · 1 skipped**。

⚠️ 這是 SPEC §5 對每條紅線的要求：只證明「正常資料會通過」證明不了任何事，
一個永遠回傳 `None` 的空函式也會通過。

---

### 7.10 M4 機率校準：校準器做出來了，量完的結論是不要上線（2026-08-10）

M4 分兩步走。第一步只診斷不 fit（圖 09/10），量到 Mar cohort 系統性低估
27.7%，並據此選了 isotonic —— 理由不是它通常較好，是 D7 → D8 的排序反轉讓
Platt 的單調平滑假設不成立。第二步把校準器 fit 起來套到 Mar，**推翻了「校準
修得掉這個低估」這個前提**。

本節的所有數字來自 `make calibrate-fit`（`scripts/calibrate.py`）。

> ⛔ **本節在 2026-08-10 第二次重測：模型由 LightGBM 改為 CatBoost。**
>
> 原因是一個方法錯誤：§7.12 正式採用的是 CatBoost，而校準診斷與校準器都用
> LightGBM 做 —— **「校準器不該上線」這個結論是在一個不會上線的模型上得出
> 的**。現在三支 M4 腳本一律走 `src.models.adopted`（超參數從
> `configs/model_comparison.yaml` 讀，不複製）。舊的 LightGBM 數字作廢。
>
> ⚠️ 本節的未校準 Mar log loss 是 **0.15388**，與 §7.4 的 0.15367 不同源：
> 校準流程把 Feb 切成三塊（訓練只剩 70%），比三方比較的路徑少了約 10 萬列
> 訓練資料。兩個數字不可互相取代。

#### 一、校準器只看得到 Feb，而 Feb 上沒有東西可修

| 資料塊 | 人數 | 實際流失率 | 平均預測 | 相對偏差 | ECE | MCE |
|---|---|---|---|---|---|---|
| **Feb-sel**（fit 校準器用） | 148,937 | 6.3913% | 6.4170% | **+0.40%** | 0.00029 | 0.00088 |
| Mar cohort（評估用） | 970,959 | 8.9941% | 6.5670% | **−26.98%** | 0.02427 | 0.08554 |

Feb-sel 是模型訓練與 early stopping 都沒看過的一塊（切分來自
`configs/calibration.yaml`，seed 與 `tuning.yaml` 刻意不同，理由見該檔）。
**模型在自己那個月的乾淨保留集上校準得幾乎完美** —— ECE 0.00037，相對偏差
0.40%。isotonic 於是學到一個近似恆等的映射，只是變粗：81 個風險等級。

所以 Mar 上的 −27.1% 不是「機率曲線的形狀歪了」，而是 **Feb → Mar 的基準率
漂移**：6.3913% → 8.9941%，+40.7%。校準器在 Feb 上看不到這件事。

**方法選擇的資料與 fit 的資料不是同一塊 —— 這是本步驟最該記住的方法論教訓。**
第一步看著 Mar 的 reliability diagram 挑了 isotonic，但校準器只能 fit 在
Feb-sel 上，而那條反轉曲線在 Feb-sel 上並不存在。選方法的證據來自一塊資料、
擬合的資料是另一塊，兩者不同分布時，選得再有道理也修不到東西。

#### 二、Mar cohort：每一項指標都變差

| 指標（全體 970,959 人） | 校準前 | 校準後 | ⚠️ 洩漏對照組 |
|---|---|---|---|
| 相對偏差 | −26.98% | **−27.23%** | +6.90% |
| ECE | 0.02427 | **0.02449** | 0.00621 |
| MCE | 0.08554 | 0.08647 | 0.01078 |
| **log loss** | **0.15388** | **0.15955（+3.7%）** | 0.14032（−8.8%） |
| Brier | 0.041068 | 0.041108 | 0.039212 |
| AUC | 0.94591 | 0.94491 | 0.94705 |

分群同樣全面變差，且**新進用戶受害最深**（他們正是挽回名單最需要正確機率的
一群）：

| 分群 | log loss 前 → 後 | ECE 前 → 後 | AUC 前 → 後 |
|---|---|---|---|
| 重複用戶（881,693） | 0.11749 → 0.12278（+4.5%） | 0.01417 → 0.01436 | 0.93584 → 0.93456 |
| 新進用戶（89,266） | 0.51327 → **0.52272（+1.8%）** | 0.12399 → 0.12461 | 0.87206 → 0.87168 |

**洩漏對照組是故意違規的**：把校準器 fit 在 Mar 自己身上，不可上線。它的用途
是當上界 —— 它證明這個缺口**在技術上修得掉**（ECE 降兩個數量級），缺的不是
方法而是「一塊與部署期同分布的標籤資料」。作法沿用
`scripts/target_encoding.py` 擺三個違規控制組的手法：把「不能這樣做」變成一個
有數字的論證。

> ⚠️ 對照組的 −13.6% 是**樂觀上界**，因為它 fit 與評估用同一批列。誠實版本
> （Mar 隨機切兩半，fit 在 A、評估在 B）**尚未執行**，列為待辦。在跑出那個
> 數字之前，不得引用 −13.6% 作為「校準可以拿回多少」的估計。

#### 三、代價：57% 的用戶拿到同一個數字

| | 相異機率值 | 最大同分組佔比 | 有同分對象的樣本佔比 |
|---|---|---|---|
| 校準前 | 822,730 | 0.59% | 15.86% |
| 校準後 | **792** | **57.03%** | 99.93% |

§6.2 的核心交付物是「投放給預測機率最高的前 K%」。被壓成同一個值的是**機率
最低的 57%**，所以校準後 **K > 43% 的範圍不再有定義** —— 門檻落在那個區塊裡，
等於要從 553,778 個完全同分的人中挑出誰該投放，而沒有任何依據可以挑。

即使 K < 43% 也只是勉強可用：那 417,181 人只剩 791 個相異等級，平均每級約
527 人，門檻只能落在等級邊界上。**這不是精度變差，是那條曲線的橫軸有一大段
不存在。** 這是不上線的第二個理由，且它與分數無關。

AUC 掉 0.005 的讀法要小心：isotonic 是單調映射，不會倒轉任何一對的順序，
所以 AUC 的變化**全部來自同分**；而同分讓原本得 0 分的反轉配對變成 0.5 分。
**AUC 上升代表壓平了排錯的區段，下降才代表壓平了排對的區段。** 這裡是下降，
意思是被壓平的區段大多原本排得對 —— 損失的排序資訊多於修掉的反轉。
（手算案例釘在 `tests/test_calibrator.py::test_flattening_an_inversion_raises_auc_instead_of_lowering_it`。）

#### 四、惡化集中在「零區塊」

| 區段 | 人數 | 佔比 | 實際流失率 | 校準前預測 | 校準後預測 | log loss 貢獻 |
|---|---|---|---|---|---|---|
| 下界區塊 | 553,778 | 57.03% | 0.087% | 0.008%（低估 10 倍） | **0.00059%（低估 148 倍）** | 0.00419 → 0.00598 |
| 其餘 | 417,181 | 42.97% | 20.818% | 15.274% | 15.232% | 0.14968 → 0.15356 |

（兩列各除以總人數，相加即整體 log loss。）

整體惡化 0.00567，其中 **32% 來自下界區塊**。成因很具體：Feb-sel 有 84,800 人
一個正例都沒有，isotonic 據此宣告「這群人風險趨近 0」；同一群人在 Mar 的實際
流失率是 0.087%。**漂移在機率尺度的最低端咬得最兇，而那正好是最多人所在的
地方。**

#### 五、下界不是防呆，是必要條件

isotonic 對零區塊的輸出是**剛好 0**。套到新資料上，只要有一個正例落進那個
區間，log loss 就會踩到官方 clip 的 `1e-15`，單筆貢獻 −ln(1e-15) ≈ 34.5。

實測：那個區間在 Mar 有 482 人流失。**沒有下界的話，單這一段就讓整體
log loss 增加 0.0171** —— 是 M3 全部改善幅度（0.00338）的 5 倍。

因此 `fit_isotonic` 不寫死 epsilon，而是從零區塊的**實際大小**推下界：m 筆
樣本觀測到 0 個正例，Jeffreys 後驗均值是 0.5/(m+1)，意思是「這 m 筆只能告訴
你機率低於這個量級，不能告訴你它是 0」。本次 m = 84,800 → 下界 5.896e-06。
沒有零區塊時下界為 0，機制自動失效，不會憑空改動任何預測。

#### 結論與處置

**校準器不上線 —— 這次是在正式採用的 CatBoost 上重新判斷的，不是沿用
LightGBM 的結論。**

三項證據，每一項都指向同一個方向：

| 證據 | 數字 |
|---|---|
| Feb-sel（校準器唯一看得到的資料）已經幾乎完美 | ECE **0.00029**、相對偏差 **+0.40%** |
| Mar 的低估**沒有被修掉** | −26.98% → −27.23%（不減反增） |
| 每一項指標都變差 | log loss **+3.7%**、ECE +0.9%、Brier +0.1%、AUC −0.001 |
| 排序解析度崩塌 | 相異機率值 822,730 → **792**，57% 的人同分 |

⚠️ **CatBoost 的惡化幅度比 LightGBM 小**（log loss +3.7% vs +9.8%），但方向
與結論完全相同。小的原因是 CatBoost 的低機率端本來就比較保守：下界區塊的
實際流失率只有 0.087%（LightGBM 是 0.432%），被壓到下界的傷害因此輕得多。
**「傷害比較小」不是「應該上線」** —— 它仍然只帶來代價、沒有帶來收益。

三項連帶處置：

1. §6.1 的 `p_churn` 改為**未校準機率**，並在 §6.3 第二點說明整體偏差
   **不能**當成單一比例往下套到名單或金額。
2. §6.2 的期望淨收益曲線用未校準機率繪製 —— 保留 81.9 萬個相異值，橫軸的
   「前 K%」才連續可切。
3. 程式碼全部保留（`src/evaluation/calibrator.py`、`scripts/calibrate.py`、
   `make calibrate-fit`）。M6 若改成「用最近一個完整月份校準、預測下一個月」，
   這條路徑可以直接重跑；`configs/calibration.yaml` 的 `tracking.enabled`
   仍為 `false`，等有可上線的校準方案再開。

#### 交付物與測試

| 項目 | 位置 |
|---|---|
| 校準器（fit / apply / 同分結構） | `src/evaluation/calibrator.py` |
| AUC（M4 才引入，理由見該函式註解） | `src/evaluation/metrics.py::roc_auc` |
| 執行入口 | `scripts/calibrate.py` · `make calibrate-fit`（約 2.5 分鐘） |
| 圖 11：校準前後 + 映射曲線 | `reports/figures/11_calibration_before_after.png` |
| 純邏輯測試 18 條 | `tests/test_calibrator.py` |

全套件 **119 passed · 1 skipped**；CI 的 nodata 子集 **85 passed · 零 skip**。

18 條測試裡有三條釘住會安靜出錯的地方：零區塊下界的推導、排序反轉被壓平、
以及「AUC 因壓平而**上升**」這個反直覺的方向 —— 第三條原本寫成「AUC 只會
下降」，跑出來是紅的，是實測改正了斷言而不是相反。

#### 待辦

| # | 項目 | 為什麼重要 |
|---|---|---|
| 1 | 誠實版 oracle：Mar 切兩半，fit 在 A、評估在 B | 現有的 −13.6% 是 fit 與評估同批列的樂觀上界，不可引用 |
| 2 | M6 改採「以最近完整 cohort 校準、預測次月」並實測 | 這才是部署時真正拿得到的資料結構；本專案無 Apr 標籤，本地無法驗證 |
| 3 | 快取指紋（§7.5 遺留） | 與本節無關，仍未處理 |

---

### 7.11 第三次全面重測：同日交易沒有定序（2026-08-10）

§7.8 修掉「列順序不可重現」之後，**重建快取仍然會換分數**：實測 M2 的 Mar
log loss 在 0.15821 與 0.15891 之間跳（0.00070，約 1.5σ）。這次把成因追到底，
發現 §7.8 只修了一半。

#### 成因一：`_last()` 假設「最後一筆交易」有定義

`transaction_date` 只到日。實測 **Feb 有 12,889 人（1.30%）在最後交易日當天
有多筆交易**，誰是「最後一筆」在資料上就是未定義的 —— polars 每次重建挑到
不同的那一筆：

| 欄位 | 兩次重建的差異 |
|---|---|
| `last_actual_amount_paid` | 24 列不同，最大差 1608 |
| `last_payment_plan_days` | 7 列不同，最大差 380 |
| `last_is_cancel` | **19 人翻面**（此欄佔 35.2% gain） |

處置是**逐欄位判斷**（規則 C′）：最後交易日只有一筆就正常取值；同日多筆時，
該欄位非 null 值只有一種就保留，有兩種以上就設 null 讓 LightGBM 走缺失分支。

逐欄位是關鍵：同一天兩筆同方案同金額、但其中一筆是取消，`last_plan_list_price`
一點都不模糊，只有 `last_is_cancel` 沒有答案。實測衝突規模 Feb 10,266 人
（1.03%）、Mar 9,134 人（0.94%）。

取 null 而不是取眾數，是因為後者在編一個資料沒說的答案。

新增稽核欄 `last_day_n_tx` 與 `last_day_has_conflict`，**刻意不進特徵矩陣** ——
「最後一天有沒有衝突」很可能與流失相關（同日多筆常見於改方案、取消後重買），
當特徵就是拿資料品質瑕疵預測標籤。

#### 成因二：浮點加總的次序

`log*_secs` 對 `total_secs` 求和，而浮點加法不可結合、polars 的 group_by 是
平行的 → 每次重建差約 1e-9。1e-9 秒沒有意義，但 LightGBM 的分箱有邊界，
落在邊界上的值換邊，樹就長得不一樣。改成整數毫秒相加。

#### 驗證的盲點：只比順序看不到值變了

⚠️ 第一版 `verify_rebuild.py` 只比 msno 順序，三輪全綠 —— **而上面兩個問題
它一個都沒抓到**，因為每個人都還在原來的位置上，只是身上的數字換了。加上
`hash_rows` 的逐位元內容指紋之後才看得見。

修正後：cohort 內容、收聽特徵內容、特徵矩陣內容、三份設定檔的 train/es/sel
成員、M1/M2 內部切分成員，兩輪強制重建**全部逐位元相同**。

#### 連帶修掉的兩件事

**一、CatBoost 不接受類別欄有 null。** C′ 讓 `last_payment_method_id` 第一次
可能是 null，CatBoost 直接爆掉，而 LightGBM 安靜地把它當成一個分支。
**「一家爆掉、一家安靜接受」比兩家都爆掉更糟** —— 三方比較的前提是三家吃到
同一份資料。依既有慣例補 `fill_null(MISSING_CATEGORY)`。

**二、報表的 σ 標題與實際用的不一致。** `reverse_validation.py` 把 σ 寫死成
0.00084，實際用的是 `NOISE_SIGMA = 0.00048` —— §7.6 換 σ 那次只改了常數沒改
旁邊的字。

#### 重測結果

七步全部在同一份現行基準上重跑，共 28.4 分鐘，紀錄見 `reports/rebaseline/`。

| 里程碑 | 舊值（作廢） | 新值 | 變化 |
|---|---|---|---|
| M1/M2 LightGBM · Mar | 0.15821（604 輪） | **0.15705**（498 輪） | −0.7% |
| M1/M2 · Feb 5-fold | 0.08563 ± 0.00031 | **0.08344 ± 0.00028** | — |
| M3 CatBoost · Mar | 0.15685（1812 輪） | **0.15367**（1404 輪） | −2.0% |
| M3 XGBoost · Mar | 0.15949 | **0.15726** | −1.4% |
| M3 三家極差 | 0.00264（1.68%） | **0.00359（2.34%）** | 擴大 |
| M3 篩選後 · Mar | 0.15960（36 特徵） | **0.15879**（39 特徵） | 結論不變 |
| M3 調參後 · Mar | 0.15733 | **0.15543** | 結論不變 |
| M4 診斷 · 相對偏差 | −27.58% | **−27.14%** | 結論不變 |
| M4 校準前 → 後 · Mar | 0.15854 → 0.16859 | **0.15645 → 0.17172** | 結論不變 |

**每個模型的分數都變好了**，方向一致：把「隨便挑一筆」換成「不知道就說不
知道」，模型不再被餵入無中生有的取值。

#### 哪些結論翻轉

| 結論 | 狀態 |
|---|---|
| CatBoost > LightGBM | ✅ **強化**：2.83σ → **7.05σ**（反向 4.03σ） |
| LightGBM > XGBoost | ⚠️ **翻轉**：原判「可信」（−2.67σ / −1.73σ），現為**雜訊**（−0.43σ / −0.51σ） |
| null importance 篩選在 Mar 上變差 | ✅ 不變（+1.28%） |
| 調參不採用（單次量測） | ✅ 不變 |
| M4 校準器不上線 | ✅ 不變（log loss +9.8%，58.99% 同分） |
| M4 低估來自 cohort 漂移 | ✅ 不變（Feb-sel ECE 0.00038） |
| M1 打贏常數基準 | ✅ 不變（−48.9%） |

⚠️ **σ 已於 §7.12 用配對 multi-seed 重測**（0.00048 → 0.00044，且量的不再是
同一件事）。重測之後 LightGBM vs XGBoost 的判定改變了 —— 見 §7.12。

---

### 7.12 配對 multi-seed：雜訊尺度重測，三家排序恢復（2026-08-10）

§7.11 重測之後，所有「幾 σ」的判定都失去依據 —— `NOISE_SIGMA` 是在同日交易
定序修正**之前**量的，裡面混著已經修掉的不決定性。這一節重量，並且改用
**配對設計**。

#### 為什麼要配對

各家各跑 N 個 seed 再比平均，會把**切分本身的變異**算進比較的雜訊，而那個
變異對三家是**共同**的：某個 seed 剛好切出好學的 early stopping 集，三家會
一起變好。配對設計讓同一個 seed 下三家吃**完全相同**的 train/es，只看每個
seed 的差值，共同部分在相減時抵銷。

    邊際 σ   σ(A)      同一個模型重跑一次會晃多少
    配對 σ   σ(A − B)  A 對 B 的優勢會晃多少　← 判定要用這個

8 個 seed **事先寫死在 `configs/multi_seed.yaml`**：先跑再決定跑幾個，就可以
在結果剛好顯著時收手，CI 會失效。XGBoost 的類別字典也從整個 Feb 算一次共用，
否則差值裡會混進「類別編碼不同」這個成分。

#### 每個 seed 的 Mar log loss

| seed | LightGBM | XGBoost | CatBoost |
|---|---|---|---|
| 42 | 0.15705 | 0.15726 | 0.15367 |
| 137 | 0.15712 | 0.15792 | 0.15403 |
| 271 | 0.15661 | 0.15721 | 0.15413 |
| 314 | 0.15700 | 0.15753 | 0.15469 |
| 577 | 0.15652 | 0.15713 | 0.15311 |
| 811 | 0.15674 | 0.15716 | 0.15397 |
| 1009 | 0.15704 | 0.15679 | 0.15389 |
| 1234 | 0.15726 | 0.15775 | 0.15449 |

#### 新的雜訊尺度

| 模型 | 平均 | 邊際 σ | 平均輪數 |
|---|---|---|---|
| LightGBM | 0.15692 | **0.00026** | 560 |
| XGBoost | 0.15735 | **0.00037** | 469 |
| CatBoost | 0.15400 | **0.00048** | 1504 |

⚠️ **舊的 `NOISE_SIGMA = 0.00048` 剛好等於 CatBoost 的邊際 σ —— 數字接近純屬
巧合，它量的不是同一件事。** 舊值是「LightGBM 換 seed 會晃多少」，用來判的
卻是「兩家差多少」。

#### 配對比較（差值 = A − B，負值代表 A 較優，95% CI，n = 8）

| 對比 | 平均差 | 配對 σ | 95% CI | 勝場 | 判定 |
|---|---|---|---|---|---|
| LightGBM − XGBoost | −0.000426 | 0.000323 | [−0.000696, −0.000156] | **7 : 1** | **可信** |
| LightGBM − CatBoost | +0.002922 | 0.000404 | [+0.002585, +0.003259] | 0 : 8 | **可信** |
| XGBoost − CatBoost | +0.003348 | 0.000444 | [+0.002977, +0.003719] | 0 : 8 | **可信** |

**三組全部可信，完整排序是 CatBoost > LightGBM > XGBoost。**

#### 這推翻了 §7.11 對 LightGBM vs XGBoost 的判定

§7.11 依單次反向驗證把這一對判為「雜訊」（−0.43σ / −0.51σ）。配對 8 seed
顯示那個差距是**真的**：7/8 勝、95% CI 不含 0。

兩者不矛盾，但結論必須寫對：**不是「兩家沒有差別」，是「單一次量測沒有解析
度」。** 0.00043 的差距對上 0.00044 的配對 σ 就是 0.97σ —— 單次量測分不出來；
重複 8 次之後標準誤縮成 1/√8，就分得出來了。

這是本專案第三次因為「拿什麼當雜訊尺度」而改變結論（前兩次見 §7.6、§7.8）。
三次的共同教訓：**σ 不是一個常數，是一個要跟著「你在比什麼」而變的量。**

#### 對 §7.6 反向驗證的影響：判定全部不變

`NOISE_SIGMA` 由 0.00048 改為 **0.00044**（三組配對 σ 取最大值，保守）。
σ 倍數是同一批實測差距除以一個新常數，因此可以直接換算，不需重跑：

| 對比 | 正向（舊 σ → 新 σ） | 反向（舊 σ → 新 σ） | 判定 |
|---|---|---|---|
| LightGBM vs CatBoost | 7.05σ → **7.69σ** | 4.03σ → **4.40σ** | 可信（不變） |
| XGBoost vs CatBoost | 7.48σ → **8.16σ** | 4.54σ → **4.95σ** | 可信（不變） |
| LightGBM vs 三者平均 | 5.13σ → **5.60σ** | 3.67σ → **4.00σ** | 可信（不變） |
| XGBoost vs 三者平均 | 5.56σ → **6.07σ** | 4.18σ → **4.56σ** | 可信（不變） |
| LightGBM vs XGBoost | −0.43σ → **−0.47σ** | −0.51σ → **−0.56σ** | 單次分不出（見上） |

#### 採用的模型

**CatBoost，Mar log loss 0.15367**（8 seed 平均 0.15400 ± 0.00048）。優勢對
LightGBM 是 **−2.16%**、8 個 seed 全勝，且反向驗證同向。代價仍是 22 倍訓練
時間（233 秒 vs 11 秒），M6 應依重訓頻率重新評估 —— 但那是一個**知道差距是
真的**之後的成本取捨。

---

### 7.13 M4 業務指標：核心交付物（2026-08-10）

SPEC §6.2 稱期望淨收益曲線是「整個專案的核心交付物」。`make eval` 產出它與
敏感度熱圖（圖 12、13）。

**用 CatBoost 而不是 LightGBM** —— §7.12 正式採用的是它，業務曲線必須用會
上線的那一個。順帶量到一件有用的事：CatBoost 在 Mar 的整體低估是 **−27.15%**，
LightGBM 是 −27.14%。**兩個模型幾乎一樣，證實那個低估是 cohort 漂移造成的，
與模型無關**（§7.10 的結論因此不限於 LightGBM）。

#### 一、LTV 的推導：只有「預期續訂月數」是假設

| 項目 | 值 | 來源 |
|---|---|---|
| 月費（Mar cohort 實付平均） | **131.3 元** | 資料實測（`price_per_day` × 30.4） |
| 預期續訂月數 = 1 / 月流失率 | **15.6 個月** | Feb 6.3915%（幾何存活） |
| **LTV_saved** | **2,054 元** | 兩者相乘 |
| r_save | 15% | **純假設**，無實驗組可估計 |
| C_offer | 150 元 | **業務決策**（送一個月免費） |

> **投放門檻 p\* = 150 / (0.15 × 2054) = 0.4868**

⚠️ **預期續訂月數用 Feb 的流失率，不用 Mar 的。** Mar 是評估集的標籤，拿它
設業務常數等於用到答案。用 Mar 會得到 1,460 元、p\* = 0.685 —— 差距落在
C_offer 的掃描區間內（等價於 C_offer 從 150 變成 107），所以不左右結論，
但會左右基準情境的門檻。

⚠️ 這個估計**偏樂觀且方向已知**：被挽回的是特意挑出來的高風險用戶，往後的
流失風險高於平均，實際續訂月數會比 15.6 短。資料集沒有挽回實驗，無法校正。

#### 二、最佳投放門檻

| | 投放比例 | 投放人數 | 門檻機率 | 期望淨收益 | 實際結算 | 命中率 | lift |
|---|---|---|---|---|---|---|---|
| **依期望曲線**（部署時唯一能用的依據） | **5.0%** | 48,548 | 0.4895 | 4,962,931 | **5,535,298** | 85.69% | 9.53 |
| 事後最佳（只能回顧） | 7.4% | 71,851 | — | — | 5,792,753 | — | — |

> **門檻偏保守的代價：257,456 元** —— 照期望曲線選門檻，在標籤結算下比事後
> 最佳少這麼多。方向與 §6.1 的預告一致：低估流失風險 → 門檻設得過於保守。

#### 二之二、名單自己的偏差 —— **全體的 27% 套不上去**

這一小節存在的唯一理由：**「模型低估 27%」是一句關於全體 cohort 的話，
把它套到投放名單上會錯得很離譜。** 實測：

| 對象 | 平均預測 | 實際流失率 | 相對偏差 |
|---|---|---|---|
| Mar 全體 cohort（970,959 人） | 6.5521% | 8.9941% | **−27.15%** |
| **模型選定的前 5% 名單（48,548 人）** | **81.86%** | **85.69%** | **−4.47%** |

**差了 6 倍。** 名單是機率最高的一小撮，它的校準性質與全體毫無關係。

金額那一側又是第三個數字：

| | 金額 |
|---|---|
| 期望模擬淨收益 | 4,962,931 元 |
| 標籤結算模擬淨收益 | 5,535,298 元 |
| 差距 | **+572,367 元（+11.53%）** |

**三個數字互不相等**（−27.15%、−4.47%、+11.53%），而且**不可互相換算**：
`p × r × LTV − C` 只有第一項隨 p 縮放，`C_offer` 是固定成本。所以

    機率低估 27%  ≠  名單低估 27%  ≠  淨收益低估 27%

`src/evaluation/decision.py::subset_calibration()` 就是為了讓報表能誠實地
報第二列，而不是把第一列的數字借過來用。

#### 三、§4.5 第 3 點的答案：模型不只是學會「新客風險高」

§4.5 要求檢驗「若模型只是學會新客風險高，策略就退化成對所有新客發優惠」。
現在有數字了：

| 策略 | 投放人數 | 標籤結算模擬淨收益 | lift |
|---|---|---|---|
| **模型排序前 5.0%** | 48,548 | **+5,535,298 元** | 9.53 |
| 規則：全部投給新進用戶（不需模型） | 89,266 | **−2,432,833 元** | 4.43 |

**那條規則是虧錢的。** 新進用戶整體流失率 39.84%，但 p\* = 0.4868 —— 平均
而言他們達不到門檻，整批投放必然虧損。模型的價值正是在於**能在群內排序**：

| 分群 | 最佳投放比例 | 投放人數 | 標籤結算模擬淨收益 | lift |
|---|---|---|---|---|
| 重複用戶（881,693） | 3.2% | 28,214 | +3,462,589 | **15.08** |
| 新進用戶（89,266） | 22.4% | 19,996 | +2,071,952 | 2.07 |

兩群各自都有正收益區間，代表排序在**同一分群內部**仍然有效 —— 這正是 §4.5
要求檢驗的那件事。

#### 四、敏感度：結論對假設有多穩

48 組 (r_save, C_offer) 假設裡，**42 組的標籤結算模擬淨收益為正（88%）**。最佳投放
比例從 0%（r_save 5% 且 C_offer ≥ 250，門檻高到沒人達標）到 24.5%
（r_save 30% 且 C_offer 10）。

決策只取決於 `p* = C_offer / (r_save × LTV)`，所以熱圖實際上是 p\* 的等高
線圖 —— 沿著對角線移動的組合會給出相同的決策。這也是「LTV 該用營收還是
毛利」那個爭論被吸收掉的地方。

#### 五、必須跟著這張圖一起讀的三件事

1. **兩條線都是模擬，不是賺到的錢。** 真實的只有 Mar 的流失標籤；`r_save`
   與 LTV 是假設。金額用未校準機率算，而**整體 27% 的偏差不能套到名單或
   淨收益** —— 名單自己的偏差要自己量（§6.3 第二點）。
2. **`r_save` 是純假設。** 本資料集沒有實驗組／對照組，無法估計投放是否
   真的改變行為（§6.3 第一點）。整條曲線是「若 r_save = 15% 則…」。
3. **「多數情況不該大規模投放」本身就是結論。** 基準情境的最佳比例只有
   5%，不是參數沒調好。C_offer 若改成 50（折價券而非免費月），門檻降到
   p\* = 50 / (0.15 × 2054) = **0.1623**，最佳投放比例變成 **10.3%**
   （約 10.0 萬人，圖 13 的 r_save 15% × C_offer 50 那一格）—— 那是一個
   **業務決策**改變了答案，不是模型變好了。

   ⚠️ **原文此處寫「最佳比例會變成 16.2%」是錯的**（2026-08-10 修正）。
   0.1623 是**門檻機率 p\***，不是投放比例；兩者是不同的量，剛好都能寫成
   百分比。把 p\* 當成比例會高估名單 57%（16.2% × 97 萬 ≈ 15.7 萬人 vs
   實際 10.0 萬人）。同一句話也出現在 README，一併修正。

---

### 7.14 M5 SHAP 原因碼：名單、句子，以及有多少解釋撐不到 T−7（2026-08-10）

`make explain` 產出投放名單與逐位用戶的 Top-3 流失原因（圖 14）。M4 回答
「該投放給多少人」，這一步回答**那些人是誰、為什麼是他們**。

#### 一、名單是一個函式的輸出，不是一份清單

輸出的 CSV 不是資產，是一次執行的結果。它由三組輸入決定，而**改哪一組影響
的東西完全不同**：

| 改什麼 | 名單成員 | 每個人的機率 | 原因碼 |
|---|---|---|---|
| 業務假設（`C_offer` / `r_save` / LTV） | 變 | **不變** | **不變** |
| 模型或特徵 | 變 | 變 | 變 |

這個區別寫進 `manifest.json` 的讀法裡：少了它，下次名單變了沒有人分得出是
假設動了還是模型動了。配套的三條約束：

- CSV 不進 git（`.gitignore` 的 `*.csv` 讓它**物理上進不去**，競賽規則）
- 進 git 的只有 `manifest.json`，而它只放 provenance 與彙總 —— 逐人的列是資料
- 下游不得快取它。M6 的 `/predict` 要即時算，不是查表

**名單的定義是 `p > p*`，不是「前 5%」。** M4 報的 5.0% 是期望曲線在
`curve.step = 0.002` 網格上的極大值 —— 名單大小會受一個畫圖解析度參數影響。
§6.1 的恆等式保證極大值必然落在「最後一個 `p > p*` 的人」身上，所以直接用
門檻：實測 **48,853 人（5.03%）**，與 M4 的 48,548（5.0%）差 0.6%，差異純粹
是網格解析度。改 `C_offer` 從此是一次除法，不必重跑曲線。

`p*` 的推導抽成 `src/evaluation/decision.py::resolve_assumptions()`，兩支腳本
共用 —— 理由與 `src/models/adopted.py` 相同：`p*` 不是設定值而是推導結果
（`C / (r × LTV)`，LTV 又是 `月費 × 1/流失率`），各推一份的症狀會是兩份交付物
都印「投放 4.8 萬人」卻指著不同的名單。

#### 二、TreeSHAP 用 CatBoost 原生，不裝 `shap` 套件

同一個精確 TreeSHAP（不是 KernelSHAP 的取樣近似），少一個依賴，而且 `Pool` 與
`cat_features` 的轉接留在 `src/models/candidates.py` —— 那正是該模組存在的
唯一理由。露出 booster 讓解釋端自己組 Pool，等於把那張轉接表寫第二份。

**加總恆等式逐列驗過**：`sigmoid(base + Σ shap)` vs `predict()`，48,853 × 61
的最大差 **6.9e-15**。這條檢查抓的是一整類不會報錯的錯（歸因與預測用了不同
輪數的模型、欄位順序不一致、轉接寫了兩份），症狀都是「原因碼很通順但講錯人」。

⚠️ **SHAP 的單位是 log-odds，不是機率。** 貢獻 +0.8 不是「流失率多 80%」；
同樣的 +0.8 落在起點 0.05 與 0.5 的人身上造成的機率變化完全不同。因此原因碼
只排序，不印「這一項讓他的流失率增加幾個百分點」。

#### 三、Top-3 特徵 ≠ Top-3 原因

61 個特徵裡有 4 個窗口 × 8 個收聽量，高度相關，SHAP 會把同一件事的功勞拆給
好幾欄 —— 純取 Top-3 特徵可能拿到三句同義的話。SHAP 的加法性讓**組內相加是
精確的**，所以先依語意分組相加、取前 3 組，再從組內挑貢獻最大的那一欄造句。

分組的副作用是大的組容易累積較多貢獻，所以要自我檢查。實測兩種取法的
Top-1 分布：

| 組 | 分組後 | 不分組（Top-1 特徵映回所屬組） |
|---|---|---|
| 續訂設定 | 46.6% | 44.3% |
| 取消紀錄 | 46.0% | 45.9% |
| 訂閱資歷 | 5.5% | 9.7% |
| 方案與金額 | 1.9% | 0.1% |

排名一致，前兩名幾乎不動 —— 分組沒有靠組大取勝，只是讓句子不重複。實測
48,853 人裡**沒有任何人的 Top-3 出現重複句子**（所以不需要去重的機制 ——
分組已經擋掉了）。

**⚠️ 收聽類的四個組一個都沒有進 Top-1。** 它們主要出現在第 3 句
（收聽強度 23.9% + 收聽頻率 16.9% + 收聽近況 7.4% + 收聽趨勢 2.3% ≈ 第 3 句
的一半），第 2 句只佔 8.1%。全體 mean|SHAP| 前三名是 `last_is_cancel` 3.67、
`days_since_last_tx` 2.18、`last_is_auto_renew` 1.72，第一個收聽特徵是
`log_max_days_before` 0.154（第 9 名）。這與 §7.4 量到的「收聽特徵只值
1.38%」是同一件事的兩種呈現。

#### 四、句子

驗收標準是「任一用戶可輸出 Top-3 流失原因」，所以句子本身是交付物，逐句釘在
`tests/test_explain.py` 裡：

```
到期前最後一筆交易是取消
未開啟自動續訂
近 30 天活躍天數 4 天（前 60 天平均每 30 天 18 天）
近 7 天聽歌時間是近 30 天日均的 0.12 倍
近 90 天完全沒有收聽紀錄
```

第三句是 §7 M5 例句「近 30 天活躍天數由 22 降至 4」的誠實版本。特徵集裡**沒有
兩時點的差**，但 90 天窗口包含 30 天窗口、而活躍天數可加，所以
`(log90 − log30) / 2` 是純粹的既有特徵相減。它是**解釋用的脈絡**：模型看到的
是 `log30_active_days`，排序也來自 SHAP 對那一欄的歸因。只對可加的量做這件事
—— `unq`（不同歌曲數）不可加（同一首歌會在兩個窗口重複），比率類同理。

最後一句來自一個查證。原本印的是「近 14 天聽過的不同歌曲數缺失」，讀起來像
14 天這個窗口有資料缺口。實測 `mar_log_features.parquet` 本身**零 null**
（796,298 列），而 cohort 有 970,959 人 —— 差的 174,661 人（18.0%）根本不在
那張表裡。所以每一個 null 的收聽欄位都來自 `_attach_logs` 的 left join，意思是
「近 90 天完全沒有收聽紀錄」：那是**行為**，不是資料品質問題，對營運的意思
完全相反。句子改成講成因，但仍依該列實際的 `log_has_logs` 判斷，不假設那個
不變量永遠成立。

#### 四之二、營運呈現與底層稽核是兩份輸出

讀第一次的輸出發現一個交付物層級的缺陷：**第 3 句常常弱到不像一個理由。**

    1. 到期前最後一筆交易是取消          +7.941
    2. 近 14 天活躍日平均聽歌時間 790 秒  +0.093   ← 第 1 名的 1.2%
    3. 近 90 天活躍天數 20 天             +0.075   ← 第 1 名的 0.9%

三句並列，營運會以為三件事都重要。全名單的量測：

| | 中位數（相對第 1 名） | 低於 5% 的人數 |
|---|---|---|
| 第 2 句 | 60.0% | 5,110（10.5%） |
| 第 3 句 | **7.5%** | **12,812（26.2%）** |

處置是**分成兩份輸出**，因為兩邊問的問題不同：

| | 內容 | 進 git |
|---|---|---|
| `targeting_list.csv` | 每人一列，**只有該呈現的句子** | ✗（`*.csv`） |
| `reasons_audit.csv` | 每人每句一列，**全部候選 + 為什麼沒印** | ✗（`*.csv`） |
| `manifest.json` | provenance + 彙總 + 門檻與它的代價 | ✓ |

稽核表的五個欄位：`rank` / `group_shap` / `relative_to_top` / `displayed` /
`suppression_reason`。**一列都不刪** —— 刪掉被壓下的列，「這個人本來還有第三個
理由、只是太弱」就永久不可考，而那正是稽核要回答的問題。與 `expiry_dated`
只標註不過濾是同一個原則。

門檻是 `relative_to_top ≥ 0.05`（`--min-relative` 可覆寫）。⚠️ **它是呈現判斷，
不是統計檢定** —— SHAP 沒有提供「這個貢獻顯著嗎」的分布。所以門檻值與它的代價
一起寫進 manifest：

| | 數字 |
|---|---|
| 候選 → 呈現 | 145,796 → 127,874 句（壓下 17,922） |
| 受影響人數 | 13,165（**26.95%**），少一到兩句 |
| 被壓下句子的相對貢獻中位數 | **2.64%** |
| 呈現句數分布 | 3 句 72.3% ｜ 2 句 17.1% ｜ 1 句 10.6% |

⚠️ 稽核表裡唯一沒有的是**負貢獻**的特徵。那不是「被壓下」，而是
`top_contributors()` 依定義不把「降低風險的因素」當成投放理由 —— 「他有開自動
續訂所以風險較低」是真的，但不是打電話的理由。這個區別要寫出來，否則稽核者會
以為表上就是全部 61 個特徵。

#### 五、有多少解釋撐不到 M6 的 T−7 版本

每個特徵帶一個**量測時點**（`horizon`），因為 M6 要交付
`cutoff = 到期日 − 7 天` 的版本：

| | 意思 | 誰 |
|---|---|---|
| 到期日 | 訊號在 T−7 可能還沒發生，**原因碼不可沿用** | `last_is_cancel`（只有它） |
| 位移 | 訊號還在，但量的是 7 天前的狀態 | 收聽窗口整體前移；`last_*` 可能改指另一筆交易 |
| 快照 | 值與 cutoff 幾乎無關 | `members_v3` 的城市／性別／年齡等 |

`last_is_cancel` 是實測最強的旗標（流失率 85.70% vs 4.26%，見
`src/features/build.py`），也因此是 T−7 版本損失最大的一個。實測這份名單：

| | 營運呈現的句子 | 全部候選（稽核） |
|---|---|---|
| 到期日訊號的句數 | 23,836 / 127,874（**18.64%**） | 23,839 / 145,796（16.35%） |
| 受影響人數 | 23,836 / 48,853（**48.79%**） | 23,839（48.80%） |
| 佔貢獻總和 | **42.35%** | 41.99% |

**近一半的人、四成的解釋強度。** 兩欄的分母不同（呈現門檻壓掉了 17,922 句），
所以同一件事有兩個數字 —— 其中 3 個人的到期日訊號本來就被門檻壓下了，那是
兩欄人數相差 3 的原因。三個列各自的分母也不同，答的是不同的問題，不可互相
替代 —— 同 §7.13「三個數字互不相等」的教訓。

⚠️ **這是「解釋有多少會消失」的估計，不是「分數會掉多少」。** SHAP 貢獻量與
log loss 的變化之間沒有換算關係。SPEC 早就預告 T−7 版本「分數必然下降」，
這裡多的是一個關於**可解釋性**的事前估計。

⚠️ **標註不是開關。** 「T−7 不能用 `last_is_cancel`」的正解是重訓一個 T−7
版本的模型，不是拿現在的模型推論時遮掉那一欄。SHAP 的歸因是聯合的：遮一欄，
它的貢獻不會重新分配，只會讓加總恆等式破掉，破的量剛好是這個訊號的全部強度。
所以 M5 只標註與計量、不過濾，而 `manifest.json` 有一個必填欄位
`cutoff_definition = expire_date` —— 少了它，這份原因碼被拿到 T−7 版本使用時
沒有任何東西會擋。

#### 六、做這一步時發現的三個錯

1. **§7.13 與 README 把 `p*` 當成投放比例。** 原文寫「C_offer 改成 50，最佳
   比例會變成 16.2%」—— 0.1623 是**門檻機率**，而該門檻下的投放比例是
   **10.3%**（圖 13）。把 p\* 當比例會把名單高估 57%（15.7 萬 vs 10.0 萬人）。
   兩處已修正。
2. **第 3 句常常不是一個理由**（中位數只有第 1 名的 7.5%），三句並列讓營運
   以為三件事都重要。處置見四之二 —— 營運呈現加相對門檻、底層稽核保留全部
   候選與 `suppression_reason`。這個缺陷是**讀輸出**才看見的，測試不會抓：
   每一句都正確，問題在於並列本身傳達了錯的權重。

3. **樣本顯示取「機率最高的前 6 位」會系統性誤導。** 那 6 個人是同日交易衝突
   （§7.11）導致 `last_is_cancel` 與 `last_is_auto_renew` 雙雙為 null 的極端
   少數（全名單只有 95 人的 Top-1 是「自動續訂設定缺失」），讀起來卻像整份
   名單都是資料不明的人 —— 而實際上 45,041 人（**92.2%**）的 Top-1 是「未開啟
   自動續訂」或「到期前最後一筆交易是取消」這兩句。`print_samples()` 改成沿
   名單等距取樣。

#### 七、provenance：git SHA 不夠

manifest 記兩組指紋：快取檔上的（實際產生特徵的那版程式）與現行程式的。
SHA 只能證明「執行時的程式是這一版」，證明不了「餵進去的特徵是這一版算的」
—— §7.11 的事故正是這個形狀。實測本次兩者相符（`stale: []`）。

---

### 7.15 M6 提前 7 天評分：代價 +18.11%，而最大的損失不是預期的那一個（2026-08-10）

`make lead-time` 交付 §4.3 要求的對照。`CohortSpec` 多了 `lead_days`，
`feb_t7` / `mar_t7` 是**同一批人、同一組標籤，只把評分時點提前 7 天**。

#### 一、代價：共同子集 +18.11%

| | 共同子集 969,386 人（同一組標籤） |
|---|---|
| 到期日評分 | **0.15173** |
| 提前 7 天評分 | **0.17921** |
| | **退步 +18.11%** |

各自驗證集的分數是 0.15367（970,959 人）與 0.17921（969,386 人），但**那兩個
數字不可互相比大小** —— 提前評分讓一批人掉出 cohort，兩份驗證集不是同一批人。
T=0 在共同子集上是 0.15173 而不是 0.15367，差的就是那批人（他們是難題）。

同 §7.13 的教訓：分母不同的兩個數字不可互相替代，那就兩個都寫出來。

#### 二、⚠️ 看不到的那批人，流失率是全體的 7.8 倍

掉出 cohort 的是「到期前 7 天內才第一次交易」的人：

| | 人數 | 佔比 | 流失率 | 該 cohort 全體 |
|---|---|---|---|---|
| Mar（驗證） | 1,573 | 0.162% | **70.25%** | 8.99% |
| Feb（訓練） | 1,138 | 0.115% | 36.82% | 6.39% |

**只報「掉了 0.16%」會給人完全錯的印象。** 能上線的那個模型**結構性地看不到
風險最高的那一小撮人** —— 而那不是可以修的 bug：他們在評分時點真的還沒有任何
歷史。這是部署約束，要進 `MODEL_CARD.md`，並且需要另一個機制接住（到期日當天
補評一次，或用「首購後 N 天」觸發）。

#### 三、⚠️ 損失最大的訊號不是 `last_is_cancel`

§7.2 預期「M6 版本會失去大部分此訊號」。實測（CatBoost 的 gain 佔比）：

| 特徵 | T=0 | T−7 |
|---|---|---|
| **`days_since_last_tx`** | **48.4%** | **13.6%** |
| `last_is_auto_renew` | 11.6% | 20.5% |
| `last_is_cancel` | 16.3% | 12.7% |
| `last_payment_method_id` | 2.2% | 9.8% |
| `n_tx` | 2.5% | 6.4% |

崩掉的是**「距上次交易幾天」**。機制說得通：T=0 時大量用戶的
`days_since_last_tx` 是 0 或 1（到期日當天那筆續訂或取消），提前 7 天就全部
看不到；而 `last_is_cancel` 只從 16.3% 掉到 12.7%，因為**旗標沒有完全消失** ——
仍有 1.39% 的人（原本 2.61%）在到期前 7 天以上就取消了。

⚠️ **這不代表 §7.2 錯了。** 那裡的「35.2% gain」是 LightGBM 量的，這裡是
CatBoost，而 `src/models/candidates.py` 明文警告三家的 gain 定義不同、**不可
跨套件比較**。正確的讀法是：「這個訊號有多重要」本身依模型而定，而在會上線的
那個模型上，最大的損失在別的地方。

模型也訓練得更久（1,963 輪 vs 1,404 輪）—— 訊號消失之後仍有結構可挖。

#### 四、⚠️ 新進用戶在 T−7 反而更準（−6.25%）

| 分群 | 人數 | T=0 | T−7 | 退步 |
|---|---|---|---|---|
| 全體 | 969,386 | 0.15173 | 0.17921 | +18.11% |
| 重複用戶 | 881,692 | 0.11660 | 0.14996 | **+28.61%** |
| 新進用戶 | 87,694 | 0.50488 | 0.47334 | **−6.25%** |

代價幾乎全部落在重複用戶身上，而新進用戶那一格是**負的**。方向與 §7.8 預先
登記的假設一致（少了到期日那筆交易，模型必須改用「歷史形狀」特徵，而那對新客
更有效），但 −6.25% **尚未查證成因**，不得當成結論引用。§7.8 那個實驗的重測
（沿用原門檻與多 seed 設計）仍然待做。

#### 五、實作上三個容易錯的地方

1. **順序**：先用到期日選出 cohort 成員，**再**移 cutoff。反過來會讓「誰在這個
   cohort 裡」也跟著變，兩個版本就不是同一批人。
2. **收斂檔的日期下界也要往前 7 天**。少扣這 7 天，`feb_t7` 最早那批人的 90 天
   窗口開頭會落在收斂檔之外，收聽特徵少算一週 —— 而**紅線 2 不會叫**：它只檢查
   非負，缺資料它管不著。
3. **`assert_cutoffs_within_window()` 的「決定性」被削弱了。** 它原本靠「兩個
   cohort 的到期區間不重疊」而具有決定性；`mar_t7` 的 cutoff 落在 20170222~
   20170324，與 `feb` 宣告的 20170201~20170228 **有 7 天重疊**。完整的錯置表
   仍然擋得下來，只含那 7 天的子集則蒙得過去。這一點寫在函式的 docstring 裡並
   有測試釘住重疊本身，以免日後有人再依賴那個已經不成立的保證。

#### 六、一個 Int8 溢位，以及哪個守門叫了

日期位移的第一版寫成 `year * 10000 + month * 100 + day`。**polars 的
`dt.month()` 回傳 `Int8`**，於是 `2 * 100 = 200` 溢位成 −56：

    2017 * 10000            = 20170000
    (month 2) * 100 → −56   = 20169944
    + (day 21)              = 20169965      ← 正解 20170221，差 256

**1 月不會出事**（`1 * 100 = 100` 還在 Int8 界內），所以當時那條只用 20170201
當測資的測試**通過了**；而且它測的是 Python 版的 helper，整欄版的運算式一條
測試都沒有。

真實資料上是 `assert_cutoffs_within_window()` 擋下來的（736,027 列的 cutoff
落在 20169945~20169965）。**值得記下哪個守門沒叫**：紅線 1 的
`assert_asof_respected()` **通過了** —— 因為 cutoff 這個基準點自己是垃圾，
截斷只留下 2017 年之前的交易，`last_tx <= cutoff` 當然成立。那正是
`build_cohort()` 註解裡預言過的形狀：**基準點錯了，所有以它為準的檢查都會同意
你**（§4.3 那段「這是紅線 1 的守門抓不到的洩漏形式」）。

修法是 `dt.strftime("%Y%m%d")` —— 沒有算術就沒有溢位。測試補上 1/2/3/12 月與
閏日，2 月是最便宜的失敗案例。

---

### 7.16 M6 `/predict` 與模型 artifact：讓「線上那個模型」與報告裡的是同一個（2026-08-10）

    uv run python scripts/export_model.py --lead-days 7     # make artifact-t7
    uv run uvicorn src.serving.app:app --port 8000          # make serve

交付兩件東西：一份**存下來的模型 artifact**（服務、線上 Demo、Kaggle 推論管線
共用，沒有人重訓），以及 `/predict` 的兩種介面（`msno` 與完整 payload）。

本節記的不是「怎麼起一個 FastAPI」，而是**這條路徑上有哪些不會報錯的錯**，以及
每一個決定擋掉了哪一個。服務層與離線分析在這件事上有本質差別：離線跑錯會得到一
個奇怪的分數，服務跑錯會得到一個**完全合理的機率**。

#### 一、artifact 記什麼：一個只有 `model.cbm` 的目錄回答不了上線前必問的問題

    這是 T=0 還是 T−7 的模型？    兩者差 18.11%（§7.15），只有後者能上線
    特徵是哪 61 欄、什麼順序？     CatBoost 的 Pool 依**位置**認特徵
    p* 是多少？                   它由 cohort 的價格分布推導，不是設定值
    校準了嗎？                     §7.10 決定不上線，這件事要跟著模型走
    這些特徵是哪一版程式算的？      §7.11 的形狀：程式改了、模型還是舊的

所以 `cutoff_definition` 是 `save_artifact()` 的**必填欄位**，缺了直接 raise ——
與 M5 的名單 manifest 同一個規則（§7.14）。這個字串現在由
`src.data.cutoff_definition(spec)` 產生，M5 與 M6 不各寫一份：兩邊字串一旦不一致
（`expire_date_minus_7d` vs `expire_date-7d`），比對這一欄的下游就會靜靜地認為兩
份交付物不同源。

#### 二、`p*` 存進 artifact，因為它是一個擬合出來的統計量

`p* = C_offer / (r_save × LTV_saved)`，而 `LTV_saved` 來自
`monthly_arpu(cohort 的 price_per_day)`。**服務端只有一位用戶** —— 拿他自己的日均
單價去算，等於每個人有一條自己的門檻線，「名單」這個概念就消失了。

因此門檻在匯出時算好、存進 artifact，服務只負責比較。只改業務假設時
`--reuse` 載回既有模型、重算假設與分數，不重訓。

#### 三、服務端不算特徵 —— 這是本節最重要的一條

payload 帶的是 **cohort 表的欄位**（`build_cohort()` 輸出的那一列），轉換一律呼叫
訓練時那一份 `build_features()`。`src/serving/payload.py` 只做四件事：檢查欄位、
對齊型別、補兩個 payload 不該提供的欄位、把沒給的收聽特徵補成 null。

線上／離線不一致最典型的成因，是服務為了少一個依賴自己算一遍
`days_since_last_tx` —— 而 `20170301 - 20170228 = 73` 這個坑本專案**已經踩過兩次**
（§4.3 與 §7.15 各一次）。測試因此逐列比對 payload 路徑與 `build_features()` 的
輸出，**含 dtype**：`mean_paid: 149` 從 JSON 進來會被推成 Int64，而訓練時它是
Float64，多數運算會自動 upcast 所以看不出來 —— 但「多數」不是「全部」。

兩個欄位刻意不由呼叫端提供：

  - `is_churn` 是**標籤**。`build_features()` 需要它才組得出 `FeatureSet`，服務端
    填 null 且從不讀取。讓 API 收標籤等於做一個「請告訴我答案」的介面。
  - `in_members` 是**推導欄位**（`city.is_not_null()`）。讓呼叫端填，就可能出現
    「city 有值但 in_members = false」這種訓練資料裡不存在的組合。

#### 四、真實資料的煙霧測試抓到一個 bug：集合相同、順序不同

上面那些守門全部通過、服務層的測試全綠，而**第一次對真實 artifact 打 `/predict`
就 500 了**：

    特徵欄位與 artifact 不一致。
      缺少：[]
      多出：[]
      順序是否相同：False

成因：收聽特徵在離線那一邊的欄序來自 `build_log_features()` 的輸出（那張
parquet 的欄序），在 payload 這一邊來自一個 **JSON 物件 —— 它沒有有意義的順序**。
服務只能挑一個（當時挑了欄名排序），而那與訓練時不同。

**如果沒有那兩道比對，這個 bug 不會報錯。** CatBoost 的 `Pool` 依位置認特徵：
欄名一模一樣、一欄不多一欄不少、型別全對，只是順序不同 —— 於是每一欄的值都餵給
了別的特徵，模型照樣回一個 0~1 的機率，原因碼照樣通順。

修法是**依名字重排**，而順序的唯一來源是 artifact 的 `features.names`（服務把它
傳給 `feature_row()`）。依名字重排與「假設兩邊順序剛好一樣」是兩件不同的事。

三件值得記下來的：

  1. **合成資料驗不到它。** 合成 cohort 沒有收聽特徵，所以 nodata 測試裡的
     payload 只有 23 欄交易特徵，順序天生一致。補了一條 slow 測試拿三位真人走完
     整條路徑，逐格比對（含 dtype、null 與欄序）。
  2. **「缺少 []、多出 []」這兩行對這個 bug 完全沒有資訊** —— 它們比的是集合。
     真正指出病因的是第三行「順序是否相同：False」。那一行本來只在服務層的守門
     有，事後補到 `candidates.py` 的轉接層 —— 兩層都可能先叫，而先叫的那一層若
     只印集合差異，讀者會以為欄位沒問題。
  3. 這是**測試綠燈不等於服務會動**的一個乾淨例子。真實 artifact 的煙霧測試是
     另一種驗證，不是同一種的重複。

線上／離線等價因此另外量了一次：同一位真人，`msno` 介面與完整 payload 介面的機率
**逐位元相同**（三位用戶差 0.0e+00，Top-3 原因碼字串完全一致）。

#### 五、省略 `logs` 不是中性預設，是一個主張

`_attach_logs()` 對「不在收聽特徵表裡」的人的處理是 `log_has_logs = 0`、其餘收聽
欄位保持 null —— 那正是 **18.0% 的 cohort 用戶**的長相（§7.14）。所以 payload 省略
`logs` 時，模型收到的不是「不知道」，而是「這個人 90 天沒聽歌」。

它照樣會回一個機率，那個機率也完全合理，**只是回答了另一個問題**。因此回應帶警告
而不是安靜地算完。同理，`registration_init_time > cutoff` 時 `build_features()` 會
把整列 members 屬性退回缺失（`_registered_after_cutoff`）—— payload 明明給了
city、原因碼卻說「查不到會員資料」，這件事也要講出來。

#### 六、載入時的三道守門，以及匯出時的一條恆等式

    模型檔 sha256 vs metadata     兩者不同源時，服務會用 A 模型算分、報 B 的身分
    模型檔自己記得的欄名          Pool 從 pandas 建，CatBoost 把欄名存進了模型檔
    特徵程式的邏輯指紋            不符就**拒絕啟動**，不是印一行警告

第三道是 §7.11 的教訓第二次出現：那次是「程式改好了、快取還是舊的」，這次是
「程式改好了、模型還是舊的」。欄名與欄數可以完全一樣而語意已經變了（例如某欄改了
缺失的填法），沒有任何東西會抱怨。`src/features/build.py` 因此多了
`feature_build_fingerprint()`，與 `cohort_fingerprint()` / `log_features_fingerprint()`
是三件不同的事。

匯出的最後一步是**載回來對答案**：同一批列，載回來的模型必須給出逐位元相同的
機率。容差訂 **0** 而不是 1e-9 —— `.cbm` 存的是同一棵樹的同一組葉子值，不是近似；
訂一個容差等於預先接受一個不該存在的差。實測前 5,000 列最大差 **0.0e+00**（T=0 與 T−7 兩份 artifact 各驗一次）。

匯出順帶重現了 §7.12 與 §7.15 的分數：T=0 的 `mar` **0.15367**、T−7 的 `mar_t7`
**0.17921**，與當時的數字逐位相同 —— 匯出走的是同一條 `load_cohort_features()` +
`split_for_early_stopping()` + `fit_adopted()`，所以這是一個可以期待的等式，而它成立。

存與載都留在 `src/models/candidates.py`（`Fitted.save` / `load_catboost` 共用同一個
`catboost_fitted()`），理由與該模組不把 booster 露出去給 SHAP 用完全相同：**類別
特徵的轉接只能有一份**。兩份遲早分歧，而分歧的症狀是「線上機率與離線機率不同」。

#### 七、兩種介面，以及 `msno` 那一種的邊界

`{"features": …}` 是真正的服務介面 —— 不依賴任何本機快取，Docker 與雲端容器平台上
一樣能跑。`{"msno": …}` 是給營運查詢與 Demo 用的：它查**特徵**，機率與 SHAP 仍是
即時算的（§7.14 明文禁止查表）。

但那份特徵來自一個**固定 cutoff 的歷史快照**。真正的部署要以「現在」為時點重算，
那需要一份線上交易表，本專案沒有。所以回應帶著 `feature_source`（含那個 cutoff）
與一句警告，而設定關閉這個介面時回 **503 並說明**，不是安靜地回空結果。

#### 八、`expiry_dated` 這個旗標在兩種 artifact 上的意思不同

`last_is_cancel` 被標成「到期日訊號」（§7.14），那個標註是**相對 T=0 的 cutoff**
定義的。在 T−7 的 artifact 上，同一欄講的是「T−7 之前最後一筆交易」—— 在評分時點
**看得到**，所以它不是一個用不了的解釋。

因此服務照實回報 `horizon` / `expiry_dated`，但**只在 `lead_days = 0` 時多一句
警告**。把旗標當成「不可用」的同義詞會在 T−7 的部署上誤報，而誤報的旗標會被學會
忽略 —— M5 在 `git_dirty` 上踩過這個坑（那次它永遠是 True，因為腳本自己先改了圖）。

#### 九、每一筆請求都驗加總恆等式

M5 離線驗過 48,853 列（最大差 6.9e-15），但那證明的是**那一次執行**的歸因與預測
同源。服務是另一條路徑：模型從 `.cbm` 載回來、特徵由 payload 組成、欄位順序由
artifact 的清單決定。「歸因指到錯的欄位」在這條路徑上完全可能發生，症狀還是同一
個 —— 機率正確、原因碼通順、講的是別人的事。一列的成本可以忽略，所以預設每次都驗。

#### 十、後續交付

Docker 與線上 Demo 已完成，容器部署於 Hugging Face Spaces（部署步驟見 `deploy/README.md`）。
其餘後續：PSI 漂移監控見 §7.17、`MODEL_CARD.md` 見 §7.18、Kaggle 管線見 §7.19、
紅線 4 的 GroupKFold 四段切分見 §7.20。

---

### 7.17 M6 PSI 漂移監控：三個監控都說「穩定」，而基準率動了 39.9%（2026-08-10）

    uv run python scripts/drift_report.py     # make drift

模型從 artifact 載入，**不重訓**。輸出 `reports/drift.json` 與圖 16。

#### 一、這一節要回答的問題不是「有沒有漂移」，是「我們的監控會漏掉什麼」

部署時拿不到標籤（`is_churn` 要等到期後 30 天），所以監控只能看輸入與輸出的
分布。本專案的特殊處是**我們有 Mar 的標籤** —— 於是可以把「監控看到什麼」與
「實際發生什麼」並排，量出前者漏掉多少。

實測（參考期 `feb_t7` 991,774 列 → 當期 `mar_t7` 969,386 列）：

| 監控 | 值 | 慣例判讀 |
|---|---|---|
| 特徵 PSI | 59/61 欄 < 0.10 | 穩定 |
| 分數 PSI | **0.0254** | 穩定 |
| 名單大小（`p > p*` 的比例） | 3.89% → 3.83%（**−1.3%**） | 幾乎沒動 |
| **實際流失率** | 6.36% → 8.89%（**+39.9%**） | ← 部署時看不到 |
| **當期平均預測 vs 實際** | 6.33% vs 8.89%（**低估 28.8%**） | ← 部署時看不到 |

**三個拿得到的監控全部回報「穩定」，而模型的機率低估了將近三成。**

這不是 PSI 沒做對，是 PSI 定義上就看不到它：它比的是**特徵與分數的分布**，而
這裡發生的是「同一種人、行為變了」。§7.10 查明校準器失效的成因正是這個基準率
漂移 —— 現在可以確定：**那次失效不會被上線的監控看見。**

所以 MODEL_CARD 要寫的不是「我們有 PSI 監控」，而是「PSI 監控回報穩定不等於
模型還準；基準率要靠標籤到齊後的回測，或一個外生指標（例如當月整體續訂率）」。

#### 二、0.1 / 0.25 這兩個門檻在這個樣本數下沒有意義

慣例是「< 0.1 穩定、0.1~0.25 中度、> 0.25 顯著」。這三句話沒有分布假設，也
**沒有樣本數修正**。所以本節先量一個基準線：把參考期隨機切兩半、算它自己的
PSI（`noise_floor()`，5 輪 × 61 欄）。

    完全沒有漂移時的 PSI：p95 = 0.000074

於是：

  - 分數 PSI 0.0254 是雜訊地板的 **344 倍** —— 慣例說「穩定」
  - 61 欄裡有 **58 欄**超過雜訊地板，而其中 59 欄落在慣例的「穩定」帶

兩件事都是真的，而它們講的是不同的問題：「變了嗎」（幾乎每一欄都變了，49 萬
對 49 萬的樣本數下這件事幾乎必然）與「變得重要嗎」（慣例門檻的意圖）。**慣例
門檻不是檢定，它是一個沒有寫出樣本數前提的經驗值。** 因此報告與圖 16 一律把
「幾倍雜訊地板」放在「落在哪一帶」旁邊，兩個都印。

同 §7.12 重量雜訊尺度的理由：一個沒有比較基準的門檻，過與不過都只是在讀慣例。

#### 三、分箱的三個決定，每一個都對應一種會回報「穩定」的失效

**箱界只能從參考期算。** 這與紅線 5 說的 encoding 統計量是同一類東西。若從當期
（或兩期合併）算，位移後的資料會再一次被切成等量十箱、每箱 10%，PSI 因此接近
0 —— **一個真實的位移會回報「非常穩定」**。測試餵一份整體位移 20,000 的資料進
去：正確做法得到 PSI > 1，自己重新定箱界得到 0。

**旗標欄不能走分位數。** `is_free_plan` 只有 0/1，等量分位數產生重複箱界，於是
九成樣本落進同一箱。實測 3% → 30% 這種漂移在分位數分箱下幾乎看不到，所以
`n_unique <= bins` 的欄位改用取值分箱。

**缺失自成一箱。** 服務最可能遇到的漂移就是缺失率變了（上游 join 壞掉、某欄停止
供應），而把 null 丟掉再算 PSI 會得到 0。同理，類別欄在當期出現參考期沒見過的
取值時另開一個 `__unseen__` 箱 —— 實測 `last_payment_method_id` 命中這一條
（§7.4 記錄的那個 Mar 才出現的付款方式）。

#### 四、單邊空箱的 PSI 是 epsilon 的函數，所以要標記

參考期某箱是 0 而當期不是，`ln(p/q)` 是負無限；慣例是給一個下限（本專案
`EPSILON = 1e-6`）。那時 PSI 的**大小由 epsilon 決定**，只能讀成「出現了參考期
沒有的東西」，不能讀成漂移的量。所以每一列帶 `epsilon_floored`，epsilon 的值
進報告。

⚠️ 而這個旗標差點變成永遠亮著的那種：`__missing__` 與 `__unseen__` 是**結構性**
的箱（每欄都有，不管用不用得到），一個完全沒有缺失的欄位兩邊都是 0。不先把
「兩邊都空」的箱丟掉，61 欄會全部被標記 —— 而一個永遠亮著的旗標等於沒有旗標
（M5 在 `git_dirty` 上踩過這個坑，見 §7.14）。丟掉之後實測只有 1 欄亮燈。

#### 五、分數 PSI 的參考期不能用訓練集的樣本內預測

樣本內的分數分布比實際更尖銳（模型見過那些標籤），拿它當參考期，差異裡就混進了
「樣本內 vs 樣本外」而那不是漂移。本節用 **Feb 內部那塊 early stopping 切分**
（198,355 列，沒有被擬合；停點看過它，所以不是完全乾淨的樣本外 —— 唯一真正乾淨
的樣本外是 Mar，而 Mar 是當期）。

對照量出來了：用樣本內當參考期得到 0.0236，差 **−0.0018**。方向與預期一致，量級
不大 —— 但那是「本專案這個模型」的量，不是一條可以外推的規則，所以報告兩個都印。

#### 六、順帶的一個觀察（未查證，不當結論）

PSI 最大的兩欄是 `days_since_last_tx`（1.27）與 `tenure_days`（0.56），比第三名
（0.009）高兩個數量級。而 `days_since_last_tx` 正是 §7.15 量到「T−7 損失最大」的
那一欄（gain 48.4% → 13.6%）。

兩件事同時指向同一欄很難說是巧合，但**成因沒有查證**：兩個月的方案天數組成不同
（Feb 只有 28 天）都可能造成它。這裡只記錄現象。

---

### 7.18 M6 `MODEL_CARD.md`：把限制寫成可查的數字，包括那個沒做的評估（2026-08-10）

[`MODEL_CARD.md`](MODEL_CARD.md)。這一節記的是寫它時的四個決定。

#### 一、它與 `GET /model` 的分工

`artifact.json` 已經是機器可讀的 provenance（哪一份模型、哪些特徵、什麼假設、哪一版
程式）。MODEL_CARD 不重複那些欄位 —— 它寫 JSON 承載不了的東西：**這個模型不該被拿來
做什麼**，以及每一條限制底下的實測數字。

「用途、限制、已知偏誤」若寫成三段形容詞，讀者無從判斷嚴重程度。所以卡上每一條限制
都帶一個可回溯的數字，並標明它是**哪個模型在哪個 cohort** 上量的 —— 本專案有兩份
artifact，而它們的分數不可互相比大小（§7.15）。

#### 二、「⛔ 不可用於」放在效能之前

五條：不可當成投放的因果效應（uplift）、不可用來決定金額或任何差別待遇、不可與
Kaggle 排行榜比大小、不可對「現在」評分（沒有線上交易表）、不可外推到其他產業或
年份。

順序是刻意的。一張先講 0.17921 再講限制的卡，讀者會先形成「這個模型很準」的印象，
而限制變成註腳。**越界的方式不是不知道分數，是不知道那個分數不能回答的問題。**

#### 三、上線模型的校準偏差重新量了一次，因為舊數字是另一個模型的

§7.13 量的 −27.15% 是 **T=0 模型**在 `mar` 上的偏差。卡上要寫的是上線那一份，所以
重新量了 `catboost_lead7d` 在 `mar_t7` 上的數字：

| 群 | 平均預測 | 實際 | 相對偏差 |
|---|---|---|---|
| 全體（969,386） | 6.33% | 8.89% | **−28.8%** |
| 重複用戶（881,235） | 4.18% | 5.85% | −28.6% |
| 新進用戶（88,151） | 27.88% | 39.34% | −29.1% |
| 投放名單（37,174） | 77.00% | 83.28% | **−7.5%** |

**三個群的相對偏差幾乎相同（−28.6% ~ −29.1%），那正是基準率平移的形狀** —— 與
§7.10 判定「校準器失效的成因是 cohort 間的基準率漂移」一致，這次是在上線模型上
再確認一次。而名單那一段只低估 7.5%，所以 §7.13 修正過的那條規則仍然成立：
**全體的偏差不可以往下套到名單上**。

#### 四、公平性寫成「沒有評估」，而不是省略

模型吃了 `gender_code` / `bd_clean` / `city` / `registered_via`，而本專案**沒有做
群組公平性評估**。卡上把它列為一個已知的空白，並附三個已量到的事實讓「要不要在
評估前就先移除」變成一個有依據的決定：

1. 這些欄位合計 0.93% 的 gain，其中 `gender` 只有 **0.024%** —— 移除性別幾乎免費
2. **「有沒有填」比「填什麼」有訊號得多**：性別缺失者流失率 4.86%，而男 8.84% /
   女 8.64% 幾乎沒有差異。模型從這一欄學到的主要是「有沒有填資料」
3. T=0 名單（48,853 人）的 Top-1 原因分組**沒有任何一位**落在會員資料組
   （續訂設定 46.6% / 取消紀錄 46.0% / 訂閱資歷 5.5% / 方案與金額 1.9%）

於是建議是具體的：評估完成前只把模型用在「誰先收到優惠」這種只增不減的干預；若營運
會直接讀原因碼，關掉會員屬性類的句子（代價實測接近 0）；若合規上對性別有疑慮，直接
移除該欄重訓（代價 0.024% gain）。

**寫「未評估」比寫「無偏見」誠實得多，而且它讓下一個人知道該做什麼。**

---

### 7.19 M6 Kaggle 管線：第三種 cutoff 規則，以及「資料不夠」這個守門缺口（2026-08-10）

    uv run python scripts/export_model.py --design fixed   # make artifact-fixed
    uv run python scripts/predict_kaggle.py                # make kaggle

§3.3 把「Apr cohort 的 late submission 分數」列為 M6 的門檻，因為那是唯一與官方
私榜可比的數字。這一節記的是**做出提交檔之前必須先解決的一個設計問題**，那個問題
是使用者提出來的：4 月到期的人往前推 7 天會看到 3 月。

#### 一、看到 3 月不是問題，資料在 3/31 結束才是

在 3/25 評分時看 3 月的交易是**過去**，完全合法。真正的問題在另一頭：

| 事實 | 值 |
|---|---|
| `transactions` 最後一筆 / `user_logs_v2` 最後一天 | **20170331** |
| 測試名單（`sample_submission_v2.csv`） | 907,471 人 |
| 到期日 4/1~4/7 → T−7 的 cutoff 落在資料**內** | 202,939（22.36%） |
| 到期日 4/8 之後 → cutoff 落在資料**外** | **704,531（77.64%）** |

對那 77.64% 的人，`transaction_date <= cutoff` 這條截斷變成**空操作**（所有交易都
比 cutoff 早），特徵改由資料集的結尾決定：

    days_since_last_tx    最多被放大 23 天（上線模型 13.6% 的 gain）
    近 7 天收聽窗口        整段不存在 → 模型讀成「這個人沒在聽歌」（假的主張）

**而八條紅線一條都不會叫**：`assert_asof_respected()` 必然通過（cutoff 比所有交易
晚）、紅線 2 同理、cohort 錯置檢查通過（cutoff 確實在宣告的區間內）、欄位檢查通過。
這是「基準點合法，但基準點背後沒有資料」——與 §7.11 的 Int8 溢位同一家族（那次是
基準點本身是垃圾），所以補了第九條守門 `assert_data_covers_cutoffs()`。

順帶的推論：**連 T=0 版本在測試集上也做不到** —— 4 月到期日當天的交易在資料裡
根本不存在。競賽的測試集結構本身就是「用 3/31 為止的資料預測 4 月到期的人」。

#### 二、第三種 cutoff 規則：固定評分日

    lead0  cutoff = 到期日            離線基準（§7.2）
    lead7  cutoff = 到期日 − 7 天      提前固定天數（§7.15）
    fixed  cutoff = 上個月最後一天      **提前天數隨到期日變成 1~31 天**

`fixed` 是「在 2017-03-31 這一天，替所有 4 月要到期的人評分」。每一欄特徵都 as-of
一個資料完整的時點，而它也是真實的部署節奏（月底跑一次）。

提前天數變動之後，「**還有幾天到期**」就成了一個有訊號而且看得到的量：3/31 那天，
4/2 到期與 4/29 到期的人處境完全不同。所以固定評分日的 cohort 多一欄 `expire_date`，
`build_features()` 據此多產生 `days_to_expire`（**62 個特徵**，另兩種設計是 61）。
提前固定天數的設計裡那一欄是常數（0 或 7），加它只是多一欄零 gain —— 所以不加。

⚠️ **三份 artifact 的分數不可互相比大小**：cohort 成員、特徵集、提前天數分布都不同。

#### 三、本地對照，以及它一開始偏悲觀 3.5 個百分點

Apr cohort 沒有標籤（`sample_submission_v2.csv` 的 `is_churn` 全是 0 佔位值，
`labels_are_real=False` 讓 `build_cohort()` 把它填成 **null**，任何本地算分數的嘗試
都會壞掉而不是回一個看起來合理的數字）。

所以「該期待幾分」來自**結構相同的本地對照**：`feb_fixed → mar_fixed`。而第一版的
對照是錯的：

| | 只用標籤檔定義成員 | as-of 篩選後 |
|---|---|---|
| feb_fixed | 990,836 人（流失率 6.34%） | **887,525 人（5.88%）** |
| mar_fixed | 968,436 人（8.88%） | **869,873 人（7.67%）** |
| 常數基準 | 0.30449 | 0.27329 |
| **log loss** | 0.20096 | **0.17342（−36.5% vs 基準）** |

成因是**成員的定義不一致**。實測「觀測到的到期日是否落在目標月份內」：

    apr_fixed（Kaggle 測試集）  100.00%   ← 官方就是 as-of 3/31 定義的
    feb_fixed（只用標籤檔）      89.45%
    mar_fixed（只用標籤檔）      89.69%

標籤檔是官方用**整個月**的交易決定的：一位 1/31 看起來 2019 年才到期的長約用戶，
可能 2/10 取消而把到期日縮到 2/28；一位斷約一年的用戶可能 2/10 回來續訂。這兩種人
在 1/31 都看不出會進「二月批次」—— 而**測試集裡沒有這種人**（100% as-of 可見）。

所以本地 cohort 多了 10.5% 測試集不會有的成員，而他們正是最難預測的那些（到期日
看起來在 90 天後、或已經過期一年），流失率也更高（篩掉後 8.88% → 7.67%）。修法是
加一條 as-of 篩選：**看得出到期日不在目標月份的排除，看不出的（同日多筆衝突、
6,580 人）留下** —— 後者是真實的服務情境，Kaggle 一樣要求給他們機率。

⚠️ 這件事的教訓不是「數字變好了」，是**一個結構不匹配的對照會給出一個看起來合理的
預期**。0.20096 那個數字沒有任何地方會報錯。

#### 四、提交檔與事前登記

    測試名單            907,471 列（Kaggle 要求全部都在）
    有特徵的            907,470 人（1 人在評分日之前一筆交易都沒有 → 填訓練 cohort
                        的流失率 5.8841%，那是部署時唯一知道的先驗）
    預測平均            5.1885%（中位數 1.29%、p99 59.1%）
    超過 p* = 0.4534    2.46%
    分數 PSI vs mar_fixed  0.0059（穩定）—— 唯一拿得到的品質檢查

**事前登記的預期：Apr 的提交分數應與 0.17342 同一量級。** 兩者的評分規則、特徵集、
提前天數分布（中位數 17 天）都相同，差別只在月份與基準率。官方私榜第 20 名是
0.10834，**算在同一個 Apr cohort 上，所以那個比較是合法的** —— 而本專案 Mar 的
0.15367 / 0.17921 與它不可比（§3.3）。

先印預期再算分數，順序有意義：分數出來之後才說「我本來就覺得會這樣」不是預先登記。

提交本身**不自動化**：那要接受競賽規則，是帳號層級的動作，腳本只印出指令。分數回來
之後要填進 README 的成果表與 §3.3，**無論結果如何** —— 那才是這條門檻的意義。

#### 五、順帶抓到的兩個錯

**一、`max(membership_expire_date)` 不是「他現在的到期日」。** 第一版用 max 取到期
日，於是一筆宣告 2023-08-17 到期的離群交易讓 `days_to_expire` 變成 2,330 天。正解是
**最後一筆交易宣告的那個**，同日多筆而不一致時給 null（與六個 `last_*` 欄位共用
§7.11 的 C′ 規則）。順帶還要擋哨兵值：19700101 會算出 **−17,197 天**。

**二、比對「設計」不能比整個 `cutoff_definition` 字串。** 推論腳本第一版檢查
`cutoff_definition == "fixed_score_date_20170331"`，於是一份**完全正確**的 artifact
被擋下來 —— 因為它是用 1/31 訓練的（`fixed_score_date_20170131`），而每個月的評分日
本來就不同。要比的是「這個模型期待的特徵是怎麼算出來的」，那由**種類**決定
（`Artifact.scoring_design`）。守門擋對了，判準錯了。

---

### 7.20 M6 合併 cohort 重訓：紅線 4 量不到代價，而「樂觀 13%」的來源在別的地方（2026-08-11）

    uv run python scripts/final_model.py      # make final

八條紅線的最後一條。M0 把它掛成 skip、M3 判定「本專案沒有合併多 cohort 的地方」
並把阻塞里程碑改到 M6，理由是「上線前通常會用全部資料重訓最終模型」。這一節就是
那一刻：合併 `feb_fixed + mar_fixed` 重訓，產出 `catboost_fixed_full`。

#### 一、合併之後，重疊從「不是洩漏」變成「是洩漏」

| | 列數 | 流失率 |
|---|---|---|
| `feb_fixed` | 887,525 | 5.8841% |
| `mar_fixed` | 869,873 | 7.6703% |
| **合併** | **1,757,398 列 · 973,588 人** | **6.7682%** |

跨期出現 **783,810 人** —— 佔 `mar_fixed` 的 90.11%（與 §4.4 的 90.81% 同一個
結構，成員因固定評分日略有出入），佔合併總人數的 80.51%。

§4.2 的 Feb → Mar 切分下這不是傳統意義的洩漏（標籤來自不同時間窗）。合併之後
在同一份資料上隨機切分，它就是了：同一個人的 Feb 列進訓練、Mar 列進驗證。

實測**違規那一臂每折的驗證集有 75.2%（約 293,797 人）模型在訓練時已經見過**；
合規那一臂是 0。這兩個數字由 `fold_report()` 獨立算出，不共用守門的邏輯。

#### 二、量到的代價：小於這個實驗的解析度

同一份合併資料、同樣 4 折、同一組超參數，只換切分規則：

| | log loss | 每折跨邊人數 |
|---|---|---|
| 合規 `StratifiedGroupKFold(groups=msno)` | 0.150868 ± 0.000614 | 0 |
| 違規 `StratifiedKFold`（不看 msno） | 0.150873 ± 0.000544 | 293,797 |
| **差** | **−0.000005（−0.003%）** | |

**這個差是折間標準差（0.00058）的 0.008 倍。** 正確的讀法是「這個實驗量不到」，
不是「等於 0」—— 4 折給出的解析度就是這麼粗（同 §7.12 用 σ 尺度讀 multi-seed、
§7.17 用雜訊地板讀 PSI 的理由）。

⚠️ **這是一個負面結果，而它不推翻紅線 4。** 為什麼這裡兌現不了：

1. **`msno` 不是特徵。** 模型看到的是 62 個聚合特徵，沒有任何一欄能唯一指認一個人。
2. **同一個人的兩列不是重複列。** `n_tx`、`days_since_last_tx`、年資、收聽特徵全部
   差一個月，5.22% 的人連標籤都翻轉。它們是**相關的觀測**，不是同一個觀測。
3. **深度 6 的對稱樹沒有記住個人的容量。** 「記住特徵簽章就能拿分」需要能把單一
   用戶隔離出來的容量，64 個葉子在 130 萬列上做不到。

換句話說，紅線 4 擋的是一張**保險**：這個模型剛好沒有去兌現它。同樣的重疊配上
高容量模型、或對高基數的用戶級欄位做 target encoding（紅線 6 那條路），兌現的
就是實打實的洩漏。**規定值不值得遵守，與這次量到多少無關** —— 遵守的成本是一行
`groups=msno`，而不遵守時沒有任何跡象會告訴你這次兌現了沒有。

#### 三、真正大的那個差在別的地方

| 估計方式 | log loss |
|---|---|
| 時間外（`catboost_fixed`：`feb_fixed` → `mar_fixed`） | **0.17342** |
| 合併後同分布 CV（本節） | **0.15087** |
| **同分布估計的樂觀幅度** | **13.0%** |

**紅線 4 守的那個洞在這裡是空的（0.008σ），而沒有人守的這個洞是 13%。**

合併重訓用掉了兩個帶標籤的 cohort，本地從此**算不出任何新的時間外分數**。所以
`catboost_fixed_full` 的 metadata 裡 `eval_is_out_of_time: false`，`metrics` 帶著
`out_of_time_reference` 指回上一份 artifact —— 一個服務端也答得出「這個模型有多好」
的出處。要報告「這個模型有多好」，該引用的仍然是 0.17342。

這與 §7.17 是同一個形狀的教訓：**能拿到的那個數字回報一切正常，而真正在動的那個
量不在監控裡。** 差別只在這次能量出來 —— 因為上一份 artifact 還留著。

#### 四、四段切分：每一段對應一個決定

§7.6 留下的那句「M6 重訓最終模型時應改採四段切分」。四段全部綁 `msno`：

| 段 | 列數 | 人數 | 流失率 | 這一段決定什麼 |
|---|---|---|---|---|
| `train` | 1,230,177 | 681,511 | 6.7683% | 模型權重 |
| `es` | 175,741 | 97,359 | 6.7685% | 停在第幾輪（1,974） |
| `sel` | 175,740 | 97,359 | 6.7680% | 要不要採用校準器 |
| `cal` | 175,740 | 97,359 | 6.7680% | fit 校準器 |

**切出來卻用不到的段等於白白縮小訓練集**，所以四段各有指定用途，沒有備而不用的。
設定檔宣告的是「佔全體」的比例，條件比例由 `four_way_group_split()` 換算並斷言
四段加總為 1 —— `configs/calibration.yaml` 那個手算的 `0.176`（= 0.15/0.85）
沒有人覆核得動，而算錯只會讓某一段大小不對，不會有任何錯誤訊息。

#### 五、校準器：結論不變，而理由被驗證了

在 `cal` 上 fit、在 `sel` 上判斷（分兩段是重點：同一塊資料上判斷，isotonic 幾乎
不可能變差，那個判斷沒有內容）：

    sel log loss   0.15050 → 0.15067（+0.00016）　→ 不採用
    sel 平均預測   6.749%　 實際 6.7680%　→ 低估 0.28%

§7.10 判定校準器不上線，**成因是 cohort 之間的基準率漂移，不是模型本身的機率
偏掉了**。這次是那個因果宣稱的獨立檢驗：同分布之下模型的平均預測幾乎等於實際
流失率（§7.17 記錄的時間外低估是 28.8%），isotonic 沒有東西可修，只剩下 fit
雜訊帶來的一點點退步。**結論相同，而這次是從另一個方向到達的。**

採用門檻（sel log loss 必須嚴格變好）寫成 `CALIBRATION_ADOPT_THRESHOLD` 常數，
**先寫下來再看數字** —— 看到 0.0002 的變化再決定「這樣算不算有幫助」，等於用
結果決定標準。

#### 六、附帶發現：重訓換掉的不只是模型，還有投放門檻

`p*` 由 `C_offer / (r_save × LTV_saved)` 推導，而 `LTV_saved` 裡的
`expected_months = 1 / 月流失率` 取自**訓練 cohort 的流失率**。合併之後那個
流失率從 `feb_fixed` 的 5.8841% 變成兩期混合的 6.7682%：

| | `catboost_fixed` | `catboost_fixed_full` |
|---|---|---|
| 流失率來源 | `feb_fixed` 5.8841% | 兩期混合 6.7682% |
| `expected_months` | 16.99 | **14.77** |
| `LTV_saved` | 2,205.8 元 | **1,911.6 元** |
| **`p*`** | 0.4534 | **0.5231（+15.4%）** |

**門檻升高會直接讓投放名單變小，而原因與模型的排序能力完全無關。** 這種變動
不會有人去找 —— 它藏在一個看起來只跟模型有關的動作（重訓）後面。

要用哪一期的流失率是一個未定的決定，列進 §9 第 4 項：兩期混合（現行）比較穩，
只取最近一期 `mar_fixed` 的 7.6703% 前瞻性比較合理但更雜訊。**改它不必重訓**
（`p*` 不改變任何人的機率），但要重寫 metadata。

#### 七、違規那一臂不是特別寫的壞程式碼

它用的是 `split_for_early_stopping()`：M1 至今一直在用、在單一 cohort 上完全
正確的那個函式。合規與違規只差一個參數（`groups=` 傳不傳），輸出的形狀、折數、
覆蓋範圍全部相同。

**這正是紅線 4 的性質：犯法的寫法與守法的寫法長得一模一樣，差別只在資料被合併
過。** 因此守門 `assert_groups_disjoint()` 檢查的是**性質**（沒有任何 msno 跨段）
而不是「你呼叫了哪個類別」—— 靜態檢查擋不住 `groups=df["city"]` 這種傳錯欄的
寫法，而那是最可能的犯法方式：程式看起來完全正確，分數只是變好一點。

守門在正常路徑上每折都跑，不是只在測試裡跑（§7.5 的教訓）。

#### 八、紅線狀態：八條到齊

| 紅線 | M3 之後 | M6 之後 |
|---|---|---|
| 4（合併 cohort 用 GroupKFold） | ⏸ 改等 M6 | ✅ **已實作**，含 5 條紅線測試 + 15 條切分機制測試 |

`tests/test_no_leakage.py` 從此**一條 skip 都不剩**。CI 的零 skip 檢查從 268 條
升到 **287** 條。

#### 九、工程面

- **`src/models/grouped.py`**：合併（四種會安靜出錯的合併都擋）、四段群組切分、
  兩種折法、守門、`fold_report()`
- **切分不依賴列順序**：先按 `msno` 排出標準順序再切。`train_test_split` 依位置
  切，餵進不同順序的資料會切出不同的人，而合併是最容易改變列順序的一步
- **`cohort` 欄是出身證明不是特徵**：與 `build_log_features()` 帶的 `cutoff` 同
  地位。Apr cohort 沒有這個值，一旦進了 X，模型會學到部署時算不出來的東西
- **`random_folds_violating_red_line_4()`**：函式名字長且刺眼是刻意的，它不該
  出現在任何正式流程裡
- 9 次 CatBoost 訓練，約 55 分鐘（最終模型 7.4 分 + 8 折各約 6.2 分）

---

## 8. 技術選型

| 用途 | 選擇 | 理由 |
|---|---|---|
| 大資料聚合 | **Polars (lazy) / DuckDB** | 30 GB 無法用 pandas，這是本專案的第一道工程門檻 |
| 模型 | LightGBM 主力，XGBoost / CatBoost 對照 | 表格資料的業界標準；CatBoost 對高基數類別（`payment_method_id`）有原生處理 |
| 校準 | ~~Isotonic / Platt~~ → **不上線** | log loss 要求機率可信，但實測校準器讓每一項指標都變差 —— 低估的成因是 cohort 間的基準率漂移，校準器 fit 在已知標籤的月份上看不到它。程式碼保留待 M6 重用，見 §7.10 |
| 解釋 | ~~`shap` 套件 (TreeExplainer)~~ → **CatBoost 原生 TreeSHAP** | 產生原因碼。改用原生的三個理由：①同一個精確 TreeSHAP 演算法，不是近似；②不多一個依賴（本專案的規則是每個套件都要說得出哪一步需要它）；③`Pool` 與 `cat_features` 的轉接留在 `src/models/candidates.py`，露出 booster 會讓那張轉接表被寫第二份。見 §7.14 |
| 實驗追蹤 | MLflow | 每次訓練的參數、指標與 artifact 都要可追溯 |
| 服務 | **FastAPI**（已交付）+ Docker（待做） | 模型從一份 artifact 載入而不重訓，metadata 必填 `cutoff_definition`；`/predict` 的機率與 SHAP 即時算，不查表。見 §7.16 |
| Demo | 視覺化首頁 + Swagger，容器部署 | 讓不讀 API 文件的人也看得到結果 |
| 依賴管理 | uv + `pyproject.toml` | 鎖定版本，環境可重建 |
| 設定管理 | Hydra / YAML in `configs/` | 禁止硬編碼超參數 |

---

## 9. 待決事項

| # | 問題 | 需要誰決定 | 阻塞哪個階段 |
|---|---|---|---|
| ~~1~~ | ~~KKBOX 是音樂串流，是否符合「影音平台」的專案定義？~~ ✅ **已定案（2026-08-12）**：專案定義改為**訂閱制音樂串流**，不再宣稱是影音平台。理由見下方 §9.1 | 使用者已決定 | ~~全部~~ 已解除 |
| ~~2~~ | ~~實際可投入週數~~ ✅ **已無意義**：M0–M6 全部完成，範圍沒有被時間壓縮掉 | — | ~~M4 之後的範圍取捨~~ 已解除 |
| ~~3~~ | ~~`r_save` 與 `C_offer` 的假設值區間~~ ✅ **已定案**：r_save 15%、C_offer 150 元、LTV 由資料推 2,054 元，掃描區間見 `configs/business.yaml` | 使用者已決定 | ~~M4~~ 已解除 |
| ~~4~~ | ✅ **已定案（2026-08-12）：用最近一期 `mar_fixed` 的 7.6703%**，不用合併平均。`scripts/final_model.py` 已改，理由寫在該處註解：兩期流失率是上升的，拿混合值當未來預期等於低估流失、高估 LTV、把門檻設得太低，也就是投放給太多不該投放的人。✅ **已於 2026-08-12 `make final` 重跑並生效**：`p*` 0.5231 → **0.5929**、LTV 1,911.6 → 1,686.8、預期在留月數 14.77 → 13.04、`months_source` 改為 `mar_fixed_churn_rate`。模型本身未受影響（見 §9.2）。原問題保留於下 —— ~~**合併模型的 `p*` 該用哪一期的流失率推導？**（§7.20 附帶發現）`expected_months = 1 / 月流失率`，而合併重訓把它從 `feb_fixed` 的 5.8841% 換成兩期混合的 6.7682% → LTV 2,205.8 → 1,911.6 元 → **`p*` 由 0.4534 升到 0.5231（+15.4%）**。門檻升高會直接讓投放名單變小，而**原因與模型的排序能力無關**。候選：①兩期混合（現行）；②只用最近一期 `mar_fixed` 的 7.6703%（前瞻性較合理，但單月較雜訊，`p*` 會再高一截）。改這個**不必重訓**（`p*` 不影響任何人的機率），但要重寫 metadata | 使用者 | M6 的名單大小 |

### 9.1 專案定義：訂閱制音樂串流

**本專案處理的是訂閱制音樂串流平台（KKBOX）的流失預測，不是影音平台。**

原本的專案敘述寫「針對影音平台歷史數據，找出決策規律，使企業營運最佳化」，
而 §1.3 曾把這列為阻塞全部工作的待決事項——若判定不符，就得換題。

定案是**改敘述而不是換資料**，理由有三個：

1. **資料本身沒有模糊空間。** `user_logs` 的欄位是 `num_25`／`num_50`／`num_75`／
   `num_985`／`num_100`（歌曲播放完成度分級）與 `num_unq`（不重複歌曲數）。
   這是音樂收聽行為，寫成「影音」是不準確的描述，不是可以兩邊解釋的灰色地帶。
2. **把它說成影音平台沒有任何好處。** 唯一的作用是讓專案標題貼近原本的敘述，
   而代價是在口試時被追問「你的資料哪裡有影像」時無法回答。
3. **要遷移的能力與內容型態無關。** 訂閱制流失預測的骨架——as-of 特徵切斷、
   時間外驗證、機率轉投放門檻——對音樂、影音、電信資費、電商會員都是同一套。
   §6 的期望淨收益公式不含任何「內容是什麼」的項。

**Demo Day 上的講法**：這是訂閱制留存問題，KKBOX 是台灣的音樂串流平台。
同一套方法搬到影音串流或電信資費續約，要換的是特徵定義（收聽 → 觀看／通話），
不是驗證設計或決策框架。README「已知限制」已載明結論外推需重新驗證。

### 9.2 `model.cbm` 的雜湊不是可重現性檢查

2026-08-12 為了套用 §9 第 4 項而重跑 `make final`。`p*` 只是決策門檻，**不進訓練**，
所以模型本該完全一樣。實測：

| | 舊（2026-08-11，備用機） | 新（2026-08-12，主力機） |
|---|---|---|
| 四折 log loss | `[0.15157, 0.15089, 0.15093, 0.15007]` | **完全相同** |
| CV mean（完整精度） | `0.1508678539048512` | **完全相同** |
| 樹數 / 葉節點數 | 1,974 / 126,336 | **完全相同** |
| 葉值 | — | **`np.array_equal` 為 True** |
| 檔案大小 | 2,615,900 bytes | **完全相同** |
| **`model.cbm` sha256** | `57ef2be9b6f4eb66` | **`c9a1ae6035ee4e9a`** ← 不同 |

逐位元比對兩個檔案：**2,615,900 個位元組裡只有 33 個不同**，而那段區塊的欄位名稱是
`model_guid`（值形如 `7093c24d-e1ab5112-c894f2e7-25dd1272`）。**CatBoost 每次
`save_model` 都會寫入一個隨機 GUID。**

三個結論：

1. **訓練跨機器完全可重現。** 備用機與主力機是不同的 CPU，四折分數對到小數第 16 位。
   這比 `make verify-rebuild` 的「同機連續兩次」更強。
2. **拿 `model.cbm` 的 sha256 當可重現性檢查會永遠誤報。** 正確的比對對象是葉值或預測值。
3. 本專案沒有踩到這個坑：`model_sha256_16` 只出現在 `/health`、`reports/drift.json`、
   `reports/kaggle_submission.json` 裡當**身分標記**（這份輸出是哪個模型檔產生的），
   不是當重現性斷言用。這個區別值得寫下來，因為兩者長得一樣。

---

## 10. 三個必答問題的預備答案

驗收時會隨機抽問。答不出來就是還沒真的懂。

**Q1. 你為什麼這樣切驗證集？換成隨機切會發生什麼事？**
> 用 Feb cohort 訓練、Mar cohort 驗證，因為訓練→驗證的時間關係與驗證→測試同構，分數才可外推。換成隨機切，`transactions_v2` 裡 3 月的交易紀錄會直接進到 2 月到期用戶的特徵裡——那筆交易就是標籤本身，CV 分數會虛高到接近完美，上線後全崩。

**Q2. 這個特徵是怎麼算出來的？為什麼有用？**
> 待 M2 完成後，針對實際採用的特徵回填。每個特徵都要能講出業務直覺，不能是「AI 產生的」。

**Q3. 分數從 A 進到 B，是哪一步貢獻的？你怎麼確認的？**

實驗軌跡表（每一列都對應一次 MLflow run，可回溯超參數與程式碼版本）：

| 階段 | 時間外 log loss | 相對前一步 | 加了什麼 | 代價 |
|---|---|---|---|---|
| 基準 | 0.30746 | — | 常數預測（Feb 流失率 6.3923%） | — |
| **M1** | **0.15925** | **−48.20%** | 23 個交易與用戶屬性特徵、LightGBM 527 輪 | 分數 81.8% 來自四個特徵，其中最強的 `last_is_cancel` 在提前 7 天預測時會消失 |
| **M2** | **0.15705** | **−1.38%** | 38 個 `user_logs` 收聽行為特徵（共 61 個） | **投入與回報極不相稱**：處理 31.9 GB 只換來 1.38%（§7.11 修正後從 2.93% 腰斬），且這 38 個特徵僅佔約一成的 gain |
| **M3** | **0.15367** | **−2.16%** | 換模型（CatBoost）。另外三個實驗：null importance 篩選（變差）、target encoding（待重測）、超參數搜尋（單次量測） | **四個實驗只有換套件通過雙向驗證與配對 multi-seed 8/8**（§7.6、§7.12），代價是 21 倍訓練時間。見 §7.4 |
| **M4** | **0.15645 → 0.17172（校準後）** | **+9.76%**（同源比較，⚠️ 不可與上一列的 0.15367 相減 —— 校準流程的訓練集只有 70%，見 §7.10 開頭的註記） | isotonic 機率校準（fit 在 Feb-sel） | **不上線**。「校準通常略微犧牲排序能力」這句話低估了代價：59% 的用戶被壓成同一個機率值；而低估的成因是基準率漂移，校準器修不掉。見 §7.10 |
| M6 | 待測 | — | `cutoff = 到期日 − 7 天` | **分數必然下降，但那才是能上線的數字** |

**怎麼確認的**：每一步只改一件事，且時間外切分（Feb 訓練 → Mar 驗證）全程不變。M1 之所以能歸因於「交易特徵有效」而非別的原因，是因為資料、切分、指標都與基準線相同，唯一的差異是模型與特徵。

M1 這一步另外做了兩層佐證：5-fold 標準差 0.00031 證明分數不是抽樣運氣；特徵重要度指出貢獻集中在哪四個特徵，與 M0 EDA 分群分析的結論互相對照。

---

## 附錄 A：本文件的事實來源

| 事實 | 來源 | 狀態 |
|---|---|---|
| 標籤定義、各檔 schema、時間涵蓋範圍 | Kaggle 競賽 Data 頁面官方說明 | ✅ 已確認 |
| 評估指標 Log Loss 與 clip 規則 | Kaggle 競賽 Evaluation 頁面 | ✅ 已確認 |
| 私榜成績（0.07974 / 0.08926 / 0.09344 / 0.09886 / 0.10834） | Kaggle 競賽 Leaderboard（Private，最終成績） | ✅ 已確認 |
| `members.csv` → `members_v3.csv` 移除 `expiration_date` | Kaggle 競賽 Data 頁面官方說明 | ✅ 已確認 |
| `transactions.csv` = 21,547,746 列 | **本機實測 2026-08-06** | ✅ 已驗證（第三方「約 2,150 萬」正確） |
| `transactions_v2.csv` 是增量檔，74.76% 為 2017-03 | **本機實測** | ✅ 官方文件未載明，實測發現 |
| 流失率 Feb 6.3923% / Mar 8.9942% | **本機實測** | ✅ 已量測 |
| 兩期 `msno` 交集：標籤檔 881,701 · cohort 881,693（皆 90.81%） | **本機實測** | ✅ 已量測，兩把尺的差別見 §2.0 |
| 新進用戶流失率 39.84% vs 重複用戶 5.87% | **本機實測** | ✅ 已量測，見 §4.5 |
| M1 基準 log loss 0.30746 | **本機實測推算** | ✅ 已計算 |
| `members_v3` 無 `expiration_date` 欄位 | **本機實測** | ✅ 已驗證，紅線 3 前提成立 |
| `user_logs.csv` = 392,106,543 列 · 30.5 GB | **本機實測 2026-08-08** | ✅ v0.2 標「待測」，已回填 |
| `user_logs_v2.csv` = 18,396,362 列 · 1.43 GB | **本機實測 2026-08-08** | ✅ v0.2 估 ~3 GB，高估一倍 |
| 兩份 `user_logs` 日期範圍相接、涵蓋 2015-01-01 ~ 2017-03-31 | **本機實測 2026-08-08** | ✅ 與官方說明一致 |
| `members_v3` 查不到 115,770 位 Feb cohort 用戶（11.66%） | **本機實測 2026-08-08** | ⚠️ **推翻 v0.2 的推論**，見 §2.1 |
| 交易日晚於到期日 158,766 列，95.93% 為取消紀錄 | **本機實測 2026-08-08** | ✅ 官方文件未載明 |
| 實付 0 元 1,218,324 列，53.97% 定價本為 0 | **本機實測 2026-08-08** | ✅ 官方文件未載明 |
| 全部 10 個檔案合計 8,950,563,771 bytes（壓縮） | Kaggle API + 本機逐檔驗證 | ✅ 已下載並逐 byte 比對 |
| 模型在 Feb-sel 上 ECE 0.00038、相對偏差 +0.33% | **本機實測 2026-08-10** | ✅ 已量測，**推翻「低估是校準問題」的假設**，見 §7.10 |
| isotonic 校準後 Mar log loss 0.15645 → 0.17172（+9.8%） | **本機實測 2026-08-10** | ✅ 已量測，據此決定不上線 |
| 校準後 58.99% 的用戶被壓成同一個機率值 | **本機實測 2026-08-10** | ✅ 已量測，影響 §6.2 的「前 K%」定義 |

---

## 附錄 B：版本沿革

本文件從 v0.2 到 v0.14 的逐版更新紀錄。每一條對應一次交付，或一次被實測推翻的推論；正文的數字已全部更新為現行基準，這裡保留的是變更的軌跡與理由。

> **v0.14 更新**：**紅線 4 交付，八條到齊**（§7.20）—— `make final` 合併 `feb_fixed + mar_fixed`（1,757,398 列 · 973,588 人，**783,810 人跨期出現**）重訓最終模型 `catboost_fixed_full`，四段切分（train/es/sel/cal）全部綁 `msno`，`tests/test_no_leakage.py` 從此一條 skip 都不剩（CI 零 skip 268 → 288）。**核心結果是一個負面結果**：違規對照組（同一份資料、同樣 4 折、只把 `StratifiedGroupKFold` 換成 `StratifiedKFold`）每折驗證集有 **75.2% 的用戶模型已經見過**，而兩臂的 log loss 差只有 **0.008 倍折間標準差** —— **量不到**。三件事值得記：①**這不推翻紅線 4**：`msno` 不是特徵、同一個人的兩列不是重複列（差一個月，5.22% 標籤翻轉）、深度 6 的樹沒有記住個人的容量，三者合起來讓這個模型兌現不了那個洩漏；規定值不值得遵守與這次量到多少無關，因為不遵守時沒有任何跡象會告訴你這次兌現了沒有。②**真正大的差在別的地方**：時間外 0.17342 vs 合併後同分布 CV 0.15087，**同分布估計樂觀 13.0%** —— 紅線 4 守的洞是空的，而「合併之後失去時間外驗證集」這個沒有人守的洞是 13%；artifact 因此帶 `eval_is_out_of_time: false` 與指回上一份 artifact 的 `out_of_time_reference`。③**校準器的結論不變而理由被獨立驗證**：同分布之下平均預測 6.749% vs 實際 6.768%（低估 0.28%，§7.17 記錄的時間外低估是 28.8%），isotonic 沒有東西可修 —— §7.10「成因是 cohort 間的基準率漂移、不是模型本身機率偏掉」因此從另一個方向再成立一次。
>
> **v0.13 更新**：**M6 的 Kaggle 管線交付**（§7.19）—— 提交檔已產生（907,471 列），**公榜分數待回填**。起因是一個設計問題：4 月到期的人往前推 7 天會看到 3 月。看到 3 月不是問題（那是過去），**資料在 20170331 結束才是**：`cutoff = 到期日 − 7 天` 對 **77.64%** 的測試用戶會落在資料之後，那時 as-of 截斷變成空操作，特徵改由資料集的結尾決定（`days_since_last_tx` 被放大、收聽窗口整段不存在而被讀成「沒在聽歌」）—— 而**八條紅線一條都不會叫**。因此補了第九條守門 `assert_data_covers_cutoffs()`，並新增第三種 cutoff 規則「**固定評分日**」（在上個月最後一天替所有下個月到期的人評分，提前天數 1~31 天，多一個看得到的 `days_to_expire` 特徵 → 62 欄）。三件事值得記：①**本地對照一開始偏悲觀 3.5 個百分點**（0.20096 → **0.17342**），成因是成員定義與測試集不一致 —— 實測測試集 100% 是 as-of 定義的，而只用標籤檔的 cohort 只有 89.5%，多出來的 10.5% 是測試集不會有、又最難預測的人；②**事前登記**：Apr 提交分數應與 0.17342 同量級，而官方私榜第 20 名 0.10834 算在同一個 cohort 上、那個比較合法；③順帶抓到兩個錯 —— `max(到期日)` 被 2023 年的離群值與 19700101 哨兵綁死（正解是「最後一筆交易宣告的那個」+ 同日衝突給 null），以及推論腳本比對整個 `cutoff_definition` 字串而擋下一份完全正確的 artifact（每個月的評分日本來就不同，要比的是**種類**）。
>
> **v0.12 更新**：**`MODEL_CARD.md` 交付**（§7.18）。它與 `GET /model` 的分工是「JSON 放 provenance、卡放不該被拿來做什麼」，所以「⛔ 不可用於」五條放在效能數字**之前** —— 一張先講 0.17921 的卡會讓限制變成註腳。三件值得記的：①**上線模型的校準偏差重新量過**（§7.13 的 −27.15% 是 T=0 模型的數字）：全體 −28.8%、重複用戶 −28.6%、新進用戶 −29.1%、**投放名單只有 −7.5%** —— 三群的相對偏差幾乎相同，正是基準率平移的形狀，而「全體的偏差不可往下套到名單」這條規則因此再一次成立；②**公平性寫成「沒有評估」而不是省略**，並附上三個已量到的事實（會員屬性合計 0.93% gain、`gender` 只有 0.024%、T=0 名單的 Top-1 原因沒有一位落在會員資料組），讓「要不要先移除」變成有依據的決定；③重訓前必須確認的三件事寫進卡裡（紅線 4 的 GroupKFold、`cutoff_definition`、快取的邏輯指紋）。
>
> **v0.11 更新**：**M6 的 PSI 漂移監控交付** —— `make drift` 產出 `reports/drift.json` 與圖 16，新增 §7.17。核心結果是一句壞消息：**三個部署時拿得到的監控（特徵 PSI 59/61 欄「穩定」、分數 PSI 0.0254「穩定」、名單大小 −1.3%）全部回報一切正常，而實際流失率動了 +39.9%、當期平均預測低估 28.8%** —— 也就是 §7.10 那個讓校準器失效的基準率漂移**不會被上線的監控看見**。這條限制進 MODEL_CARD，並且要靠標籤到齊後的回測或一個外生指標接住。另外四件事：①先量**雜訊地板**（同一 cohort 切兩半，p95 = 0.000074）才判讀 —— 分數 PSI 是它的 344 倍卻落在慣例的「穩定」帶，慣例門檻沒有樣本數修正這件事因此是可量的，不是一句提醒；②**箱界只能從參考期算**（紅線 5 的同一類東西）—— 從當期重新分箱會讓一個真實位移回報 PSI ≈ 0，測試釘住；③**旗標欄不可走分位數、缺失自成一箱**，否則「3% → 30%」與「缺失率從 0 變成 30%」都會得到 0；④`epsilon_floored` 差點成為永遠亮著的旗標（結構性的空箱要先丟掉，否則 61 欄全亮）—— 同 M5 `git_dirty` 的教訓。
>
> **v0.10 更新**：**M6 的 `/predict` 與模型 artifact 交付** —— `make artifact-t7` 匯出一份帶身分證的模型，`make serve` 起 FastAPI（兩種介面：`msno` 與完整 payload），新增 §7.16。五個決定值得單獨記：①**服務端不算特徵** —— payload 帶 cohort 表的欄位，轉換一律呼叫訓練時那一份 `build_features()`，測試逐列比對兩條路徑的輸出（含 dtype）；②**`cutoff_definition` 是 artifact 的必填欄位**，少了它一個 T=0 的模型可以被當成能上線的模型部署出去，而回應看起來完全正常；③**`p*` 存進 artifact 不在服務端現算** —— 它由 cohort 的價格分布推導，一列資料算不出來；④**載入時比對特徵程式的邏輯指紋，不符就拒絕啟動**（§7.11 的教訓第二次出現：那次是快取舊、這次是模型舊）；⑤**匯出完立刻載回來對答案，容差 0**（`.cbm` 是無損的，訂容差等於預先接受一個不該存在的差）。另修一處重複：`expiry_dated` 這個旗標在 T−7 的 artifact 上不代表「用不了」，只在 `lead_days = 0` 時警告 —— 誤報的旗標會被學會忽略。
>
> **v0.9 更新**：**M6 的 `lead_days` 這一塊交付** —— `make lead-time` 量出 §4.3 要求的對照，新增 §7.15 與圖 15。共同子集（969,386 人、同一組標籤）**0.15173 → 0.17921，退步 +18.11%**。三個附帶發現：①**掉出 cohort 的 1,573 人流失率 70.25%**（全體的 7.8 倍）—— 能上線的模型結構性地看不到風險最高的那一小撮，那是部署約束不是 bug；②**損失最大的訊號是 `days_since_last_tx`**（gain 48.4% → 13.6%）而不是 §7.2 預期的 `last_is_cancel`（16.3% → 12.7%，因為旗標並未完全消失）—— 但 §7.2 的數字是 LightGBM 量的，跨套件的 gain 不可比，這不是誰錯了；③**新進用戶在 T−7 反而更準（−6.25%）**，方向與 §7.8 的預先登記假設一致，但成因未查證、不得當結論引用。另修一個 Int8 溢位（`dt.month()` 回傳 Int8，`2 * 100` 溢位成 −56），值得記的是**紅線 1 的守門通過了、`assert_cutoffs_within_window()` 才是擋下它的那一條** —— 基準點自己是垃圾時，所有以它為準的檢查都會同意你。
>
> **v0.8 更新**：**M5 交付** —— `make explain` 產出投放名單（48,853 人，定義是 `p > p*` 而不是「前 5%」）與逐人 Top-3 中文原因碼，新增 §7.14 與圖 14。四件事值得單獨記：①**每句原因碼帶量測時點**，`last_is_cancel` 標為到期日訊號 —— M6 的 T−7 版本不可沿用它，實測影響 **48.79% 的人、42.35% 的解釋強度**；②輸出分成**營運呈現**（加相對門檻，弱到不像理由的不印）與**底層稽核**（全部候選 + `suppression_reason`，一列不刪）兩份，門檻的代價一起記在 manifest；③TreeSHAP 改用 **CatBoost 原生**而非 `shap` 套件（§8 已更新），加總恆等式逐列驗過，最大差 6.9e-15；④**修正 §7.13 與 README 把 `p*` 當成投放比例的錯** —— C_offer = 50 的投放比例是 10.3% 而不是 16.2%（後者是門檻機率），名單因此被高估了 57%。`p*` 的推導抽成 `resolve_assumptions()` 供 M4／M5 共用。
>
> **v0.7 更新**：**M4 全數交付** —— 業務指標（期望淨收益曲線、敏感度熱圖）完成，新增 §7.13。基準情境的最佳投放比例是 5.0%，而不需模型的規則「全部投給新進用戶」在同一組假設下虧 243 萬 —— §4.5 第 3 點因此有了數字。§9 第 3 項（r_save / C_offer 未定案）解除。另 `make encode` 在新基準上重跑並補強合規控制（§7.4 第三點）。
>
> **v0.6 更新**：**同日交易沒有定序，M1–M4 第三次全面重測**（§7.11），並以配對 multi-seed 重量雜訊尺度（§7.12）。§7.2 / §7.3 / §7.4 / §7.6 / §7.10 / §10 的數字全部換成新基準，舊值移出推理、集中於 §7.11 的作廢表。σ 由 0.00048 改為 0.00044，且量的不再是同一件事（邊際 → 配對）。採用的模型仍是 CatBoost，優勢由 0.86% 擴大到 2.16%；收聽特徵的價值由 2.93% 腰斬到 1.38%。
>
> **v0.5 更新**：**M4 的校準這一半已結案，結論是校準器不上線** —— 新增 §7.10 記錄完整數字。連帶修正三處與它衝突的舊敘述：§6.1 的 `p_churn` 由「校準後機率」改為「未校準機率並標註方向已知的 27.1% 低估」、§6.2 補上「前 K%」在校準後失去定義的限制、§6.3 增列第二條限制。§7.2「M4 校準的必要性已可量測」加註只對了一半 —— 低估是真的，但成因是 cohort 間的基準率漂移，不是校準器修得掉的那一種。§8 校準列與 §10 Q3 軌跡表同步更新。
>
> **v0.4 更新**：**修正 §2.0／§2.1／§2.3 把「cohort 人數」誤植為「原始 CSV 列數」** —— `train.csv` 是 992,931 列（非 992,912）、`train_v2.csv` 970,960（非 970,959）、兩檔 `msno` 交集 881,701（非 881,693）、`sample_submission_zero.csv` 970,960。原本的數字是 `build_cohort()` 對齊 cutoff 後的人數，屬於 §4.4 之後才該出現的另一套尺；`tests/test_data_contract.py` 一直斷言的是正確值，因此**是文件與自己的守門測試打架，測試沒有錯**。§2.0 新增一段說明兩套數字的分工，避免再被改回去。另更正 §3.3 M3 門檻（見該節）。
>
> **v0.3 更新**：`user_logs` 兩檔已下載並實測，§2.0 的「待測」全部回填（合計 **4.1 億列**）。**更正 §2.1 對 `members_v3` 覆蓋率的錯誤推論** —— 原文推斷「join 後不會有大量缺失」，實測 11.66% 查不到。新增 §2.1 末段「交易資料的兩項結構性現象」與對應的前處理要求。§2.3 斷言表增列第 12 條，並標註全部斷言已由 `tests/test_data_contract.py` 實作。§5.1 新增一條與 as-of 截斷有關的避雷提醒。
>
> **v0.2 更新**：資料已下載並實測，§2.1 所有「待驗證」數字已回填真實值。新增 §4.5 cohort 組成分析（實測發現的重大結構特性）。M1 驗收門檻改為實測基準 0.30746。
